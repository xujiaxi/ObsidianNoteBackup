# 🎯 面试复习清单

## 📅 今日复习（2026-09-06）

### 需要回顾

- [ ] **滑动窗口 & 字符串**：Longest Substring Without Repeating（LC3）、Minimum Window Substring（LC76） — **核心：** 通用模板——外层 `while` 右指针扩张、不满足条件时内层 `while` 收缩左指针，窗口内容用频次数组维护；「找最长」在**扩张后**更新答案、「找最短覆盖」在**收缩中**更新答案。LC3 用 `int[128]` 频次数组替代 HashMap，右字符入窗后频次 > 1 即重复 → 收缩左边界直到无重复，`ans = max(ans, right-left+1)`；LC76 用 need/window 双哈希 + `matched` 计数（window 中满足 need 的字符**种类数**），`matched == need.size()` 时窗口合法 → 收缩左边界并更新最短，移出字符后若 `window[c] < need[c]` 则 `matched--`。**面试口述：**「最长无重复 / 最多 k 种字符」→ 条件不合法才收缩；「最短覆盖子串」→ 条件一满足就收缩；两类都是均摊 O(N)。**坑：** 收缩顺序是「先移出字符、再 `left++`」，反了会移错字符；右指针每轮只走一步否则死循环；答案统一用 `right-left+1` 计算，别另维护计数器；`matched` 只在频次跌破 need 阈值时才减，不是每次移出都减。**（本组为轮换复习，昨日已复习图论/链表/树，今日轮到滑动窗口/股票DP/间隔设计，距上次复习本组隔 1 天）**
- [ ] **动态规划（股票系列）**：Best Time to Buy and Sell Stock（LC121 一次）、LC122 无限次、LC123 至多两次、LC188 至多 k 次、LC309 冷冻期、LC714 手续费 — **核心：** 全部套**状态机 DP** 框架——每天两个核心状态 `hold`（持有）与 `cash`（空仓），转移 `hold = max(hold, cash - price)`（继续持有或买入）、`cash = max(cash, hold + price)`（继续空仓或卖出）。LC121 只交易一次退化为维护 `min_price` 单变量；LC122 无限次直接跑上面两式（或贪心累加所有正差价）；LC714 手续费在卖出时扣：`cash = max(cash, hold + price - fee)`；LC309 冷冻期把「空仓」拆成 `sold`（今天刚卖、明天禁买）与 `rest`（可买），一行多元赋值 `hold, sold, rest = max(hold, rest-price), hold+price, max(rest, sold)`；LC123/LC188 加一维交易次数，k 很大（`k ≥ n/2`）时退化为无限次。**面试口述：**「买卖股票 + 次数/冷冻/手续费限制」→ 先问清「至多 k 次还是恰好 k 次」，再把限制翻译成状态拆分或转移剪枝；写转移前先画状态图。**坑：** 初始化 `hold = -prices[0]`（第一天就买入）而不是 0；LC309 三状态**必须多元赋值**（sold 依赖旧 hold，依次更新会串值）；LC188 滚动数组的 k 维从大到小遍历，或直接用二维数组；冷冻期返回 `max(sold, rest)` 而不是 `hold`。**（本组为轮换复习，昨日已复习图论/链表/树，今日轮到滑动窗口/股票DP/间隔设计，距上次复习本组隔 1 天）**
- [ ] **间隔 / 设计题（堆）**：Meeting Rooms II（LC253）、Design Tic-Tac-Toe（LC348）、Design Hit Counter（LC362） — **核心：** LC253「最少会议室 = 同一时刻最多重叠会议数」——按 `start` 排序 + 最小堆存各会议室的**结束时间**，新会议 `start ≥ 堆顶`（最早结束的会议室空闲）就 `pop` 复用否则 `push`，堆大小即答案；等价写法：把开始/结束时间拆开排序后双指针扫描。LC348 O(1) 判胜——玩家 1 落子记 `+1`、玩家 2 记 `-1`，维护每行/每列/两条对角线计数器，落子后更新对应行列、某计数器 `绝对值 == n` 即胜，不用每次遍历棋盘。LC362 时间戳单调递增 → deque 存 hit 时间，`getHits` 先把 `≤ timestamp-300` 的过期时间从队头弹出再返回队列长度；大量同秒 hit 可合并存 `(timestamp, count)` 省空间。**面试口述：**「重叠峰值 / 最多同时多少」→ 排序 + 最小堆或 sweep line；「O(1) 判游戏胜负」→ 计数器数组替代 O(N²) 扫描；「滑动时间窗计数」→ 队列按时间戳淘汰过期数据。**坑：** LC253 堆里存的是**结束时间**不是开始时间，`start >= heap[0]` 才能复用会议室；LC348 主对角线条件 `row == col`、副对角线 `row + col == n - 1` 容易漏判；LC362 `getHits` 先清理过期记录再返回，过期边界是 `≤ timestamp-300`。**（本组为轮换复习，昨日已复习图论/链表/树，今日轮到滑动窗口/股票DP/间隔设计，距上次复习本组隔 1 天）**

### 重点坑

- [ ] **滑动窗口**：收缩左边界顺序「先移出字符、再 `left++`」，反了会移错字符导致答案偏大；右指针每轮只走一步，漏走 = 死循环；LC3 在扩张后更新最长、LC76 在收缩中更新最短，记反会漏答案；LC76 的 `matched` 只在 `window[c]` 跌破 `need[c]` 时递减，不是每次移出都减。
- [ ] **股票状态机 DP**：初始化 `hold = -prices[0]` 而非 0；LC309 的 sold 依赖**旧** hold → 必须 Python 多元赋值或临时变量，拆行依次更新会让「今天卖出、明天就能买入」破坏冷冻期；LC188 滚动数组 k 从大到小遍历，防串用当天刚更新的状态；手续费/冷冻期这些限制条件忘写 = 转移方程少一个分支。
- [ ] **间隔 / 设计**：LC253 堆内存结束时间、比较的是「新会议 start」和「最早结束时间」，写成存 start 或比 end 就全错；LC348 副对角线 `row + col == n - 1` 漏判会导致斜向取胜检测不到；LC362 返回前不清理过期数据会把 5 分钟前的 hit 也算进去。
- [ ] **通用**：Java 比较 `Integer` 用 `.equals()` 而非 `==`（IntegerCache −128~127 之外失效）；`PriorityQueue` 默认**小顶堆**，要最大堆记得 `Comparator.reverseOrder()`；Python 二维数组用列表推导式 `[[0]*n for _ in range(n)]`，`[[0]*n]*n` 各行共享同一引用；写完 DP 先验证 base case（第 0 天 / 空输入）。

### 建议刷的新题

- [ ] **滑动窗口**：Longest Repeating Character Replacement（LC424，Medium）— 关联已掌握知识点：LC3/LC76 滑窗模板（已完成）。**核心：** 窗口合法条件「窗口长度 − 窗口内最高频字符数 ≤ k」：右指针扩张，不满足时左指针收缩（或用「窗口只增不减」写法——不满足时只移 left 一步），`maxFreq` 取历史最大值即可。**坑：** 需要替换的字符数 = `len - maxFreq`，别写成 `len - freq[右指针字符]`；历史最大值写法下收缩时不要减 maxFreq，否则会低估合法窗口。
- [ ] **DP 入门**：Climbing Stairs（LC70，Easy）— 关联已掌握知识点：股票 DP 的滚动变量技巧（已完成）。**核心：** `f[n] = f[n-1] + f[n-2]`（斐波那契），滚动两个变量 O(1) 空间；Easy 题适合面试 warm-up，用来口述「状态定义 → 转移方程 → 滚动优化」的完整链路。**坑：** base case 写清（`f[1]=1, f[2]=2`，或约定 `f[0]=1`）；n 很小时特判防越界。
- [ ] **DP**：House Robber（LC198，Medium）— 关联已掌握知识点：股票系列「持有/不持有」状态机思维（已完成）。**核心：** `dp[i] = max(dp[i-1], dp[i-2] + nums[i])`——抢当前家就必须跳过前一间（相邻约束）；滚动变量 O(1) 空间；进阶 LC213 环形版 = 去掉头 / 去掉尾各跑一次取 max。**坑：** 写成 `dp[i-1] + nums[i]` 会相邻连抢；空数组 / 单元素要特判。
- [ ] **间隔**：Merge Intervals（LC56，Medium）— 关联已掌握知识点：LC253 排序 + 扫描套路（已完成）。**核心：** 按 `start` 排序后线性扫描——当前区间 `start ≤ 已合并区间 end` 就合并（`end = max(end, cur.end)`），否则把已合并区间收进答案并开启新区间，O(N log N)。**坑：** 必须先排序再动手；合并时 end 取 max 而不是直接覆盖（区间可能完全包含）；与 LC253 同为「先排序再扫描」的 interval pattern，建议连着做形成肌肉记忆。
- [ ] **堆 / 哈希**：Top K Frequent Elements（LC347，Medium）— 关联已掌握知识点：LC253 最小堆用法 + 频次计数（已完成）。**核心：** HashMap 统计频次 → 维护 size 恰为 k 的**最小堆**（按频次比较），堆顶即当前第 k 高频，遍历完堆内元素就是答案；进阶可用频次桶排序做到 O(N)。**坑：** 比较器按「频次」比较不是按元素值；求「最大 k 个」用最小堆淘汰最小的，误用最大堆会留错元素；题目只要求返回任意顺序的 k 个数。

## 历史复习记录
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 3 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 0 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
**总共 LeetCode 完成：31 道题。掌握基于这些题型的核心思路，可以应对类似面试问题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
