# 🎯 面试复习清单

## 📅 今日复习（2026-09-17）

### 需要回顾

- [ ] **图论 BFS/DFS**：Number of Islands（LC200）、Clone Graph（LC133）、Course Schedule（LC207） — **核心：** 图题先判三件事——**有向还是无向**、**求连通/可达还是求环**、**用 DFS 还是 BFS**。**LC200 沉岛算法（Sinking Island）**：外层双重循环扫到 `'1'` 才 `count++`，并立刻发起 DFS/BFS 把整块陆地改成 `'0'`（**原地改标记兼作 visited**，省一个 visited 数组）；用 `direction = [(1,0),(-1,0),(0,1),(0,-1)]` 或 4 个 `if` 做四联通扩展；**BFS 版**用 `deque` + `popleft`，注意入队时就标记（否则同一格会被重复入队，队列膨胀）。**LC133 Clone Graph**：用 `dict` 维护「**原节点 → 克隆节点**」的映射，该 map **同时兼任 visited**；拿到一个节点先查 map，不在就 `deepcopy` 出克隆体并**先写进 map 再递归它的邻居**（顺序反了会无限递归）；无向图邻居要双向连接。**LC207 Course Schedule**：本质是**有向图判环**，两种解法都要会——① **三色标记法（DFS）**：`white` 未访问 / `gray` 正在访问 / `black` 已完成，**只有碰到 `gray`（访问中的节点）才代表有环**（碰到 `black` 是合法的交叉边，误判就错了）；② **Kahn's Algorithm（BFS 拓扑排序）**：统计 `indegree`，把所有入度 0 的节点入队，逐个出队并让邻居入度 −1，入度归零再入队；**若最终出队数量 < 节点总数 ⇒ 有环**。**面试口述：**「岛屿 / 连通块 / 区域填充」→ 网格 DFS-BFS + 原地标记；「深拷贝图」→ 哈希映射 + 递归，map 兼 visited；「课程表 / 任务调度 / 依赖顺序」→ 有向图环检测，主动报出三色标记和 Kahn 两种方案并对比（DFS 好写好查环、BFS 能顺便输出拓扑序）。**坑：** LC200 忘记标记导致重复计数甚至死循环；`count++` 写进了 DFS 里（应该只在外层遇到新岛屿时加一次）；LC133 先递归后写 map；LC207 只用一个布尔 `visited` 就下结论；**无向图判环不能照搬三色标记**（要记 `parent` 排除回头路，或用 Union-Find）；网格边界 `if 0 <= r < m and 0 <= c < n` 写反；深递归可能 `RecursionError` → 换 BFS 或提高递归深度。**（本组为轮换复习，09-16 复习滑动窗口/DP 股票/间隔设计，今日轮回到图论/链表/树，距上次复习本组隔 2 天）**
- [ ] **链表**：Reverse Linked List（LC206）、Linked List Cycle（LC141）、Merge Two Sorted Lists（LC21）、Remove Nth Node From End（LC19） — **核心：** 链表题的三件套是**哑节点（dummy）、快慢指针、先存 `next` 再改指针**。**LC206 反转**：迭代三指针 `prev / curr / next`，循环体固定顺序——`nxt = curr.next` → `curr.next = prev` → `prev = curr` → `curr = nxt`（**先存 next 再断链，顺序错了直接丢链表**）；也可递归，`head.next.next = head; head.next = None`，返回值一路传上来。**LC141 判环（Floyd 龟兔赛跑）**：`slow` 每轮 1 步、`fast` 每轮 2 步，循环条件必须是 **`while fast and fast.next`**（两个都要判，少一个会 `NoneType` 报错）；相遇即有环。**追问「环的入口」**：相遇后把一个指针放回 `head`，两指针同速前进，再次相遇点即入口（数学上由 `a + n(b+c) = 2(a+b)` 推得）；**追问「环长」**：相遇后继续走一圈计数。**LC21 合并两个有序链表**：dummy 哨兵 + `tail` 指针，`while l1 and l2` 比较取小；**收尾直接 `tail.next = l1 or l2`**（剩下的整段已经有序，不用再循环，这是最高频的手写 bug）。**LC19 删除倒数第 N 个**：dummy 处理「删头节点」的边界，`fast` 先走 **`n + 1` 步**（不是 n 步，这样 `fast` 到底时 `slow` 正好停在**待删节点的前驱**）——这是本题的唯一考点。**面试口述：**「反转 / 判环 / 找中点 / 倒数第 k 个 / 合并」全部是 dummy + 双指针的组合题，先说思路再动手；追问空间复杂度时说明迭代法 O(1)、递归法 O(n) 栈空间。**坑：** 反转时先改指针后存 next（断链）；LC141 循环条件漏判 `fast.next`；LC19 快指针只走 n 步导致停在待删节点本身；删除操作用 `dummy.next` 返回而不是 `head`；`findMiddle` 时循环条件用 `while fast.next and fast.next.next`（偶数长度取前中点，视题意而定）；空链表 / 单节点没测。**（本组为轮换复习，09-16 复习滑动窗口/DP 股票/间隔设计，今日轮回到图论/链表/树，距上次复习本组隔 2 天）**
- [ ] **树与递归**：Maximum Depth（LC104）、Same Tree（LC100）、Invert Binary Tree（LC226）、Level Order Traversal（LC102）、Construct Binary Tree from Preorder+Inorder（LC105）、Validate BST（LC98）、LCA of BST（LC235）、LCA of Binary Tree（LC236） — **核心：** 树题先把**递归三板斧背熟**：① base case（`if not root: return ...`）；② 处理当前节点的逻辑；③ 递归左右子树并**组合返回值**（组合方式决定题意：`max(l, r) + 1` = 深度，`l and r` = 同构，`l + r` = 路径数）。**LC104 最大深度**：`1 + max(depth(l), depth(r))`，空节点返回 0。**LC100 Same Tree**：`p`、`q` 都空 → True，只有一个空 → False，值不等 → False，否则递归比较左右（**这个模板是 LC226/LC572/LC101 的公共地基**）。**LC226 反转二叉树**：`root.left, root.right = invert(root.right), invert(root.left)`；**注意不能用中序**（先反转左子树会破坏「左」的定义，导致右子树被反转两次）。**LC102 层序遍历**：BFS + `deque`，**每层进循环前先 `size = len(q)` 并只处理这 size 个节点**，把它们的子节点入队，这样每层自然分组；DFS 版给 `level` 参数也能做。**LC105 前序+中序重建**：前序第一个是根，用 `dict`（值 → 中序下标）O(1) 定位根在中序的位置，**左子树长度 = `mid - in_left`**，据此切前序数组的两个区间——**切分长度算错是本题唯一失败点**。**LC98 Validate BST**：**必须传上下界 `(low, high)` 或走中序检查严格递增**；只比较父子节点是经典翻车点（`[5,1,4,None,None,3,6]` 就是反例，3 比根 5 小但在右子树里）。**LC235 LCA of BST**：利用 BST 有序性——两值都小于当前节点就往左、都大就往右、**一个在左一个在右（或等于当前节点）⇒ 当前节点即 LCA**，O(h) 且不用递归到底。**LC236 LCA of Binary Tree**：普通二叉树没有有序性，用「后序遍历 + 左右都找到才返回自己」的写法——`if root is None or root in (p, q): return root`，递归拿到 `left`/`right`，**两边都有返回值 ⇒ 当前节点是分叉点（LCA）**；只有一边有就往上传递那一边。**面试口述：**「求深度 / 判断结构相同 / 反转」→ 后序递归；「按层输出 / 找层宽」→ BFS + 每层 size；「重建树」→ 哈希定位根 + 算左子树长度；「最近公共祖先」→ 先问「是 BST 还是普通二叉树」，答法完全不同。**坑：** LC98 只比直接父子（必错）；LC105 左右子树长度算错或索引偏移；LC102 忘记先记 `size`（直接把当层全部节点当成一层）；LC226 用中序顺序；LC236 忘了「自身是 p 或 q」要立即返回；空树 / 单节点 / 倾斜树（退化成链表）要测；深递归的栈溢出风险，迭代化要会用显式栈。**（本组为轮换复习，09-16 复习滑动窗口/DP 股票/间隔设计，今日轮回到图论/链表/树，距上次复习本组隔 2 天）**

### 重点坑

- [ ] **图论 BFS/DFS**：LC200 访问过的格子**必须立刻标记**（原地 `'1'→'0'`），否则重复计数；`count++` 只写在外层扫到新岛屿时，别写进 DFS/BFS 里；BFS 要**入队时标记**而不是出队时标记（否则同一格重复入队、队列爆炸）；LC133 的 `{原: 克隆}` map **先写入再递归邻居**；LC207 判环**只有碰到 `gray`（访问中）才算环**，碰到 `black` 是合法边；**无向图不能直接套三色标记**（必须排除 `parent` 或改用并查集）；网格边界判断别写反；深递归会 `RecursionError`/`StackOverflowError`，面试要能立刻给出 BFS 替代方案。
- [ ] **链表**：反转 / 重排类题目一律**先保存 `next` 再改指针**；LC141 循环条件 `while fast and fast.next`（**两个条件缺一不可**）；LC19 快指针先走 **`n + 1`** 步；涉及删除或可能改头的题目**一律上 dummy 哨兵**，返回值统一用 `dummy.next`；LC21 收尾用 `tail.next = l1 or l2` 而不是继续循环；写完后拿「空链表 / 单节点 / 两节点」用例脑跑一遍。
- [ ] **树与递归**：LC98 **必须传上下界或走中序**，只比父子是经典错；LC226 不能用中序反转；LC102 每层先 `size = len(q)`；LC105 的 `mid - in_left` 决定前序切分点，算错必崩；LC236 的判据是「**左右子树都返回了非空**」；递归函数签名先想清楚「返回什么、什么时候返回」再动手——签名定错，整个题重写。
- [ ] **通用**：Java 比较 `Integer` 用 `.equals()` 而非 `==`（IntegerCache −128~127 之外失效）；`PriorityQueue` 默认小顶堆（Python `heapq` 同理），Python 要最大堆存负数；Python `[[0]*n]*n` 各行共享引用，要用列表推导式；递归/DP 先验证 base case 与边界（空输入 / 单节点 / 首元素）；**口头表达固定顺序：先给 brute-force、再报时空复杂度、最后讲优化**；动手前先把「不变量 + 返回值语义」说清楚。

### 建议刷的新题

- [ ] **树**：Subtree of Another Tree（LC572，Easy）— 关联已掌握知识点：LC100 Same Tree 的结构比较模板（已完成）。**核心：** 两层递归——外层遍历 `root` 的每个节点，内层调用 LC100 的 `isSameTree(s, t)` 判断「以当前节点为根的子树」是否与 `subRoot` 完全相同；`isSubtree` 的递归式 = `isSameTree(root, subRoot) or isSubtree(root.left, subRoot) or isSubtree(root.right, subRoot)`；空树判定是 base case 的关键。整体最坏 O(m·n)。**面试口述：**「判断一棵树是否是另一棵的子树」→ 拆成「遍历 + 相同树判定」两个已有模板的组合题，主动说明复杂度与更优的 KMP/哈希序列化解法（加分项）。**坑：** 只比较根节点值就返回 True（必须整棵结构+值都相同）；`isSameTree` 里忘了「都为空 → True」这个 base case；`subRoot` 为空时按定义是任何树的子树（返回 True）；把「子树」和「子结构」混淆。
- [ ] **链表**：Reorder List（LC143，Medium）— 关联已掌握知识点：LC206 反转 + LC141 快慢指针找中点（均已完成）。**核心：** 三步拼装题——① **快慢指针找中点**（`slow` 走 1 步、`fast` 走 2 步，循环 `while fast and fast.next`），在 `slow` 处断开成前后两条链；② **把后半段整体反转**（直接套 LC206 三指针）；③ **两条链交替合并**（前一半节点在前，用临时变量存好各自的 `next` 再接管）。时间 O(n)、空间 O(1)。**面试口述：**「L0→Ln→L1→Ln-1…」这种重排题 → 先说「找中点 + 反转后半 + 合并」三步走，再动手。**坑：** 找中点循环条件写错导致断链位置偏移（奇数长度时后半段应比前半段短或等长，按题意定）；断开时忘了 `slow.next = None`；合并时先丢 `next` 再改指针；节点数 ≤ 2 时特判。
- [ ] **树**：Kth Smallest Element in a BST（LC230，Medium）— 关联已掌握知识点：LC98 Validate BST 的**中序遍历升序**性质（已完成）。**核心：** BST 的中序遍历天然升序，所以**中序遍历到第 k 个访问的节点就是答案**；两种写法——① **递归/显式栈中序 + 计数器**，边走边 `k--`，`k == 0` 时立刻返回（提前剪枝，别遍历完整棵树）；② **跟随性优化（Follow-up）**：如果 BST 会被频繁修改且要多次查询，在节点里维护「左子树节点数 `size`」，按 `size` 判断往左走还是 `k -= size + 1` 后往右走，查询 O(h)。**面试口述：**「BST 里第 k 小 / 第 k 大」→ 中序升序（第 k 大就用「右-根-左」的反向中序），并主动提 follow-up 的 `size` 维护方案。**坑：** `k` 是 1-indexed，计数器先减后判还是先判后减要理清；找到答案后要**立刻 return**而不是继续递归（否则白跑 O(n)）；用 `visited` 列表存全部再取第 k 个虽然对，但要主动说明可以 O(h) 优化；节点数不足 k 的边界。
- [ ] **堆 / 链表**：Merge K Sorted Lists（LC23，Hard）— 关联已掌握知识点：LC21 合并两条有序链表 + LC253 的**最小堆**用法（均已完成）。**核心：** 两条路——① **最小堆**：把 k 个链表的头节点压入堆，每次 `heappop` 出最小值接到结果尾部，再把该节点的 `next` 压回去，堆空即结束；O(n log k) 时间 / O(k) 空间。② **分治两两合并**：把 k 条拆成两半递归合并（等价于 LC21 跑 log k 轮），O(n log k) 且空间更省——**面试里两种都讲出来最能加分**。③ 暴力逐个合并是 O(n·k)，要主动否定。**面试口述：**「合并 k 个有序序列」→ 先给 LC21 单次合并做 brute-force，再优化到「堆」或「分治」，并对比复杂度与适用场景（k 小用分治、流式数据用堆）。**坑：** 堆里存 `(val, node)` 元组时，**Python 无法比较自定义节点对象**——必须加下标 `(val, i, node)` 或给节点实现比较，否则 TypeError；空链表要跳过（别把 `None` 压堆）；`lists` 为空 / 只有一条链的边界；分治版的递归深度是 log k（安全），但别写成扫描式合并。
- [ ] **树 / 设计**：Serialize and Deserialize Binary Tree（LC297，Hard）— 关联已掌握知识点：LC102 层序遍历 + LC105 的「重建树」思路（均已完成）。**核心：** 序列化和反序列化必须用**同一套遍历序**且**标记空节点**，否则无法唯一还原——① **前序 + `#` 占位**：`serialize` 用前序遍历，空节点输出 `"#"`；`deserialize` 用队列/迭代器**按前序顺序消费**，遇到 `#` 返回 `None`，否则 `new TreeNode(val)` 后递归建左右。② **BFS 层序**：队列逐层输出，`null` 也输出，反序列化时同时维护父节点队列按序挂载。**面试口述：**「设计一个把结构序列化成字符串再还原的算法」→ 先讲清「必须保留空节点信息才能唯一确定二叉树」这一关键点，再给前序/BFS 两套方案。**坑：** 序列化时省略空节点导致无法唯一还原；反序列化的消费顺序与序列化遍历序不一致；分隔符与 `#` 的处理（用 `,`join 再 `split(',')`）；单节点 / 空树；LC449 是 BST 版（BST 可省空节点，另说）。

## 历史复习记录
- 2026-09-17：图论 BFS/DFS、链表、树与递归
- 2026-09-16：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-15：图论 BFS/DFS、链表、树与递归
- 2026-09-14：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-13：图论 BFS/DFS、链表、树与递归
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 3 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 0 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
**总共 LeetCode 完成：31 道题。掌握基于这些题型的核心思路，可以应对类似面试问题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
