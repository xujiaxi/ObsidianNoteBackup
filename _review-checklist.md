# 🎯 面试复习清单

## 📅 今日复习（2026-09-12）

### 需要回顾

- [ ] **滑动窗口 & 字符串**：Longest Substring Without Repeating Characters（LC3）、Minimum Window Substring（LC76）、Longest Common Prefix（LC14） — **核心：** 滑动窗口通用模板 = 外层 `while/for` 扩展右指针 `right`，满足条件时内层 `while` 收缩左指针 `left`。LC3 用哈希表记「每个字符最后出现的位置」做去重，遇重复字符时 `left = max(left, last[c] + 1)`（取 max 是防 left 回退，关键！），窗口长 = `right - left + 1`；LC76 是滑动窗口的**终极形态**，用 `need`（目标频率）与 `window`（当前频率）两张表 + `valid` 计数器记录「已达标字符种类数」，当 `valid == need.size()` 时先**记录并更新最优解**、再收缩 left 直到条件被破坏；LC14 纵向扫描——逐列比较所有串的同一位置，一旦越界或字符不等立即截断，比横向两两求公共前缀更简洁。**面试口述：**「最长/最短的连续子串或子数组，且带『窗口内某种计数/频率约束』」→ 滑窗；「找字符串公共部分」→ 纵向扫描同步推进。**坑：** 收缩窗口必须用 **`while`（一直缩到合法）而不是 `if`**（只缩一步是经典错误）；LC3 更新 left 一定要 `max(left, ...)` 否则 left 会倒退导致答案偏大；LC76 先记录结果再收缩、收缩到刚好非法时停止（别把合法窗口也缩掉了）；LC14 空输入/单串/最短串长度为 0 都要能处理。**（本组为轮换复习，上次 09-11 复习图论/链表/树，今日轮到滑动窗口/DP/间隔设计，距上次复习本组隔 4 天）**
- [ ] **动态规划（股票系列）**：Best Time to Buy and Sell Stock（LC121）、II（LC122）、III（LC123）、IV（LC188）、with Cooldown（LC309）、with Transaction Fee（LC714） — **核心：** 股票系列是**状态机 DP** 的同一模板变体，状态定义 `dp[i][k][0/1]` = 第 i 天、最多交易 k 次、当前「持有 1 / 不持有 0」时的最大收益。转移：`dp[i][k][1] = max(dp[i-1][k][1], dp[i-1][k][0] - price)`（买或继续持有）；`dp[i][k][0] = max(dp[i-1][k][0], dp[i-1][k][1] + price)`（卖或继续空仓）。**LC121** 只能交易一次 → 退化成「维护历史最低价、每天算 `price - minPrice` 取最大」；**LC122** 可无限次 → 贪心累加所有正差值 `sum(max(0, p[i]-p[i-1]))`；**LC123**（k=2）/ **LC188**（k 任意）用 k 维 DP，注意 k 的**遍历方向**（LC188 内层 k 循环方向别写错）；**LC309** 冷冻期 → 卖出后隔一天才能买，把 `dp[0]` 的买入口用 `dp[i-2]` 表达；**LC714** 手续费 → 在卖出时（或买入时统一）扣 fee，别两边都扣。**面试口述：**「一段价格序列上做买/卖获取最大收益，带次数/冷冻期/手续费约束」→ 立刻上状态机 DP；先问清「能交易几次、能不能同一天买卖、有无冷冻/手续费」。**坑：** 初始化易错——`dp[k][1] = -∞`（或 `-price`）、`dp[k][0] = 0`，第一天持有必须扣掉买入价；无限次用贪心（LC122）时别误用 DP 导致复杂度虚高；LC309 冷冻期的「隔一天」是 `i-2` 不是 `i-1`；LC714 手续费只扣一次（统一放卖出侧最不易错）；Java 里用 `int` 溢出注意（本题不溢出但心态要有）。**（本组为轮换复习，上次 09-11 复习图论/链表/树，今日轮到滑动窗口/DP/间隔设计，距上次复习本组隔 4 天）**
- [ ] **间隔 / 设计题（堆）**：Meeting Rooms II（LC253）、Design Tic-Tac-Toe（LC348）、Design Hit Counter（LC362） — **核心：** **LC253** 求「同时进行的最多会议数」= **最小堆**：先把所有区间按开始时间排序，遍历每个会议把「已结束（`end <= 当前 start`）」的堆顶弹出，再把当前 `end` 压入堆，堆大小即为某一时刻并发峰值；也可用「扫描线 + 计数」。**LC348** 用**行/列/两条对角线**的计数数组做 O(1) 胜负判定：每落一子对应行、列、对角线的计数器 `+1`（玩家 1）/ `-1`（玩家 2），任一项绝对值达到 `n` 即获胜——把 O(n) 的逐行检查压成 O(1)。**LC362** 统计最近 300 秒内的 hit 数：用**队列**存时间戳，查询时把所有 `<= now - 300` 的过期时间戳弹出，返回剩余队列长度；进阶可用**环形数组** + 时间桶做到 O(1) 空间。**面试口述：**「最多同时几个区间重叠 / 会议室最少数量」→ 排序 + 最小堆 or 扫描线；「设计一个 O(1) 判胜负 / 滑动窗口计数的结构」→ 计数器数组 / 队列时间桶。**坑：** LC253 判断「已结束」用 `<=` 还是 `<` 要看题意（会议 [1,5) 与 [5,10) 不重叠 → 用 `<=`）；LC348 的对角线索引要分「主对角线 `row - col == 0`」和「副对角线 `row + col == n - 1`」两条分别计数；LC362 的 `getHits` 是**查询**操作，本身**不算 hit**（除非题目把 `hit` 和 `getHits` 分开），别把查询也计进去；队列里弹过期数据要循环弹到底（不是只弹一个）。**（本组为轮换复习，上次 09-11 复习图论/链表/树，今日轮到滑动窗口/DP/间隔设计，距上次复习本组隔 4 天）**

### 重点坑

- [ ] **滑动窗口**：收缩条件用 `while`（缩到刚好不满足）而非 `if`；LC3 更新 `left` 必须 `left = max(left, last[c] + 1)`，否则 left 倒退答案偏大；LC76 收缩前先记录最优、`valid` 只在频率恰好达标/破坏时增减（别重复增减）；窗口类题先问清「子串 vs 子序列」（子序列不能滑窗）。
- [ ] **股票 DP**：初始化 `dp[k][1] = -inf`/`-price`、`dp[k][0] = 0` 别搞反；无限次交易用贪心累加正差值、有限次/冷冻/手续费才上状态机；LC309 冷冻期用 `i-2`；LC714 手续费只扣一次；状态转移「买/持有」「卖/空仓」两组各取 max，漏一组就错。
- [ ] **间隔 / 设计**：LC253 区间排序按 start、堆里存 end，重叠判断的 `<=`/`<` 按题意定；设计题优先想「把 O(n) 判定摊成 O(1) 计数」（LC348 行列对角线计数、LC362 时间桶）；注意查询类方法本身**不计入**数据（LC362 `getHits` 不产生 hit）。
- [ ] **通用**：Java 比较 `Integer` 用 `.equals()` 而非 `==`（IntegerCache −128~127 之外失效）；`PriorityQueue` 默认**小顶堆**，要最大堆记得 `Comparator.reverseOrder()`；Python 二维数组用列表推导式 `[[0]*n for _ in range(n)]`，`[[0]*n]*n` 各行共享同一引用；递归/DP 先验证 base case（空输入 / 第 0 天 / 空窗口）。

### 建议刷的新题

- [ ] **滑动窗口 / 字符串**：Longest Repeating Character Replacement（LC424，Medium）— 关联已掌握知识点：LC3 无重复字符最长子串的滑动窗口 + 频率表（已完成）。**核心：** 窗口内「除出现最多的字符外的其他字符数」≤ k 时窗口合法，即 `windowLen - maxFreq <= k`；右扩时维护窗口内频率最大值 `maxFreq`，一旦 `windowLen - maxFreq > k` 就收缩 left，答案取最大窗口长。**面试口述：**「最多替换 k 个字符使子串全部相同」→ 滑窗 + 频率表判「窗口内多数字符」。**坑：** `maxFreq` 只需**单调不减**更新（收缩时不用回退，历史峰值仍合法）；`max(0, ...)`/k=0 边界；别用「每次重算窗口最大频率」的 O(n·26) 也能过但慢。
- [ ] **字符串 / 哈希**：Group Anagrams（LC49，Medium）— 关联已掌握知识点：LC3 频率字典 + LC14 字符串逐位处理（已完成）。**核心：** 把每个单词归一化成「可作哈希键的签名」再分组——要么排序后当 key（O(n·k log k)），要么用 26 位字符计数数组转成 tuple/字符串当 key（O(n·k)，更优）。**面试口述：**「按某等价关系把字符串分组」→ 构造规范键 + 哈希表。**坑：** 用 `int[26]` 数组当 key 时 Python 里要转 tuple（list 不可哈希），Java 里要 `Arrays.toString` 或自定义字符串键；空串、单串要能处理；别用双层 `equals` 暴力 O(n²)。
- [ ] **动态规划**：Coin Change（LC322，Medium）— 关联已掌握知识点：股票系列的状态机 / DP 目录（已完成）。**核心：** 完全背包——`dp[amount] = min(dp[amount], dp[amount - coin] + 1)`，硬币可重复取 → 内层正序遍历金额；答案 `dp[amount]`，凑不出返回 -1。**面试口述：**「用最少的物品凑出目标 / 凑不成返回 -1」→ 完全背包 DP，先想状态「凑到金额 x 的最优解」。**坑：** `dp` 初始化成 `∞`（`amount+1` 或 `float('inf')`）、`dp[0] = 0`；内层**正序**遍历（完全背包允许重复用），若倒序就变成 0-1 背包；最后 `dp[amount] > amount` 时返回 -1（防 ∞ 泄漏）。
- [ ] **间隔**：Merge Intervals（LC56，Medium）— 关联已掌握知识点：LC253 Meeting Rooms II 的区间排序 + 重叠判断（已完成）。**核心：** 按 start 排序后一次线性扫描：若当前区间 `start <= res[-1].end` 则合并（`end = max(end, 当前.end)`），否则直接追加为新区间。**面试口述：**「合并所有重叠区间」→ 排序 + 一次扫描，注意合并时 end 要取 max（区间可能是包含关系，如 [1,10] 与 [2,3]）。**坑：** 合并判断用 `<=`（端点相接视为重叠，按题意确认）；`end` 一定要 `max` 而不是直接覆盖，否则被包含的小区间会把大区间的尾巴截短；排序按 start、区间相等/空数组边界。
- [ ] **堆**：Top K Frequent Elements（LC347，Medium）— 关联已掌握知识点：LC253 最小堆用法（已完成）。**核心：** 先哈希统计频率，再取频率前 k 大——① **最小堆**：遍历频率表 push 进大小为 k 的小顶堆，超 k 就 pop 最小，最后堆内即前 k 高频，O(n log k)；② **桶排序**：按频率建桶（频率 ≤ n），从高频桶往低扫取满 k 个，O(n) 时间。**面试口述：**「Top K / 出现次数前 k 多」→ 堆（O(n log k)）或桶排序（O(n)），比全排序 O(n log n) 更优。**坑：** 这里要**最小**堆（不是最大堆，Java `PriorityQueue` 默认小顶堆正好合用，别加 `reverseOrder`）；比较器要按频率比（存 `(元素, 频次)`）；k 等于不同元素个数时要能全返回；桶排序的桶大小是 `n+1`（频率最大到 n）。

## 历史复习记录
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 3 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 0 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
**总共 LeetCode 完成：31 道题。掌握基于这些题型的核心思路，可以应对类似面试问题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
