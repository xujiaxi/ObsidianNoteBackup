# 🎯 面试复习清单

## 📅 今日复习（2026-09-23）

> 对齐 `plans/two-week-plan-2026-09-18.md` **Day 6 · 树与矩阵**：[LC297 Serialize/Deserialize Binary Tree] + [LC270 Closest BST Value（含 LC530 与普通二叉树版）] + [LC54 Spiral Matrix + 链表输入变种]；Design 走 **M4 Search**（`system-design/search-system.md`：倒排索引 / 分词 / 分片 / 相关性打分 / NRT 增量索引 / 深翻页）。热手组回到「**树与递归 / 图论 BFS-DFS / 链表**」——上一组（滑动窗口 & 字符串 / 股票 DP / 间隔 & 设计题堆）昨天 09-22 刚过，本组上次为 09-21（隔 2 天），两组严格交替、今天回归；且本组的「树」正好与 Day 6 的主菜重合，可以先热手再上新题。

### 需要回顾

- [ ] **树与递归（BST 与遍历）**：LC104 Maximum Depth、LC226 Invert、LC100 Same Tree、LC102 Level Order、LC105 Construct from Preorder+Inorder、LC98 Validate BST、LC235 LCA of BST、LC236 LCA of Binary Tree — **核心：** 树是递归结构，先写「**递归三要素**」再动笔——① **base case** `if not root: return None/0/True`；② 递归左右子树；③ **返回值语义**（向上汇总什么）。**三种序遍历的差别只在「处理当前节点」这一行放在递归前还是后**：前序（根左右）适合自顶向下传参（LC105、序列化）；中序（左根右）在 **BST 上严格升序**（LC98、LC230 找第 k 小）；后序（左右根）适合自底向上汇总（LC236 LCA、LC124 最大路径和）。**LC104**：`1 + max(depth(left), depth(right))`，base 返回 0。**LC226**：`root.left, root.right = invert(root.right), invert(root.left)`（或先存后换），忘 base case 会 NPE。**LC100**：「两边都空 → True」「只有一个空 → False」「值不等 → False」「否则递归左右」，**前两条必须分开判**，合并写会把 `None vs None` 判成 False。**LC102**：`deque` + **进内层循环前先把本层长度固定** `for _ in range(len(q))`（写 `for node in q` 会边遍历边追加下一层 → 层数错乱）；空树返回 `[]` 而不是 `[[]]`。**LC105**：前序首位定根、中序里根的位置把区间切成左右两半；**用哈希表预存 `val → 中序下标`** 把每次 `index()` 的 O(n) 降到 O(1)（整体 O(n)），**递归传下标区间 `(preL, preR, inL, inR)` 而不是列表切片**（切片是 O(n) 拷贝，总复杂度会被拉到 O(n²)）。**LC98**：**不能只比左右孩子的值**——必须传**开区间上下界** `dfs(node, low, high)`，左子树收紧上界 `node.val`、右子树收紧下界 `node.val`（反例 `[5,1,4,null,null,3,6]`，局部满足全局不满足）；边界用 `None` 或 `±inf`，别写死 `±2**31`（题目值域可能顶到上界）。**LC235 / LC236**：BST 版可以**用大小关系指路 O(h)**（两目标同侧就往那侧走，否则当前即 LCA）；普通二叉树版必须**后序汇总**（左找到返回左、右找到返回右、左右都找到则当前节点为 LCA），且 `root is p or root is q: return root` 这句是**必需**的——LCA 定义允许「一个节点是另一个的祖先」。**面试口述：** 开场就说「树题先定**遍历序 + 返回值语义**，再写代码」；LC236 主动对比「BST 有 O(h) 特解，普通树只能 O(n) 后序」。**复杂度：** DFS/BFS 统一 O(n) 时间、O(h) 空间（递归栈，链状最坏 O(n)）；LC105 额外 O(n) 哈希。**坑：** LC102 内层写 `for node in q`；LC98 只比父节点/左右孩子（最经典假解）；LC105 用切片或忘建哈希表；LC100「都空」与「一个空」没分开；**HackerRank / CoderPad 递归默认上限 ~1000**，链状树要先换迭代版。
- [ ] **图论 BFS/DFS**：LC200 Number of Islands、LC133 Clone Graph、LC207 Course Schedule、LC323 Number of Connected Components、LC934 Shortest Bridge — **核心：** 三件事先定——① 图的表示（矩阵 / 邻接表）；② **visited 策略**（原地淹没 / 哈希 / 三色）；③ BFS 还是 DFS。**LC200**：矩阵 DFS「沉岛」——进函数先判越界或非陆地 `return`，**立刻 `grid[r][c] = '0'` 淹没，再递归四个方向**（顺序反了会重复访问甚至栈溢出）；外层双循环遇 `'1'` 就 `islands += 1` 并 DFS。**LC133 Clone Graph**：`visited` 字典**必须「先建克隆节点并入表」再递归邻居**（`if node in visited: return visited[node]` 放最前），否则遇到 A→B→A 环会无限递归——**这是本题唯一的坑，也是面试官必看的点**。**LC207**：① **Kahn（BFS 拓扑）**——邻接表 + 入度数组，入度 0 全入队，出队 `count += 1` 后把邻居入度 `-1`、减到 0 再入队，最后 **`return count == n`**（有环时 count < n）；② **DFS 三色**——0 未访问 / 1 访问中 / 2 已完成，碰到状态 1 说明**当前路径上又遇到自己 → 有环**，状态 2 直接剪枝。**面试官会追问「为什么 Kahn 不需要 visited 数组」→ 入度归零天然防重**。**LC323**：Union-Find 优先（**路径压缩 + 按秩合并**），初始 n 个分量、每 union 成功一次 `components -= 1`。**LC934（面经出现 6 次）**：**两阶段**——阶段 1 从一个岛 DFS 把该岛标成 `-1` 并收集边界点入队；阶段 2 从队列做**多源 BFS** 逐层扩水（`-2` 标记），第一次碰到另一个岛（`== 1`）时返回**桥的最短长度**（`step` 每扩一层 +1，注意 off-by-one——**问的是「要填几格水」不是「扩了几层」**，两个值差 1，面试要讲清）。**面试口述：** 一句话决策表「**连通性 / 数分量 → DFS 或 UF；无权最短路 / 最少步数 → BFS（第一次到达即最短，别拿 DFS 求最短路）；依赖顺序 → 拓扑排序**」；再主动说 DFS 吃系统栈、BFS 吃堆队列（广度爆炸时 BFS 内存更差）。**坑：** **BFS 用 `list.pop(0)` 而不是 `deque.popleft()` → 退化成 O(N²)**；LC207 的 Kahn 忘判 `count == n`（有环时静默返回半截拓扑序，样例还大多能过）；LC133 先递归后入表；LC200 先递归后淹没、或 `'1'`（字符）与 `1`（数字）搞混；LC934 阶段 1 / 2 的标记值混用；`[[0] * n] * n` 建图共享引用。
- [ ] **链表**：LC206 Reverse Linked List、LC141 Linked List Cycle、LC21 Merge Two Sorted Lists、LC19 Remove Nth Node From End — **核心：三大铁律**——① **断链预警**：改 `cur.next` 之前先把 `nxt = cur.next` 存下来；② **空指针**：用 `fast.next.next` 前必须 `fast and fast.next` 都不为 None；③ **Dummy Node**：凡涉及「换头 / 删头 / 合并」先 `dummy = ListNode(0); dummy.next = head`，最后返回 `dummy.next`。**LC206**：迭代三指针 `nxt = cur.next; cur.next = prev; prev = cur; cur = nxt`，**返回 `prev` 而不是 `cur`**（循环结束时 cur 已是 None）；递归版 `new_head = reverse(head.next); head.next.next = head; head.next = None; return new_head`。**LC141**：快慢指针，`while fast and fast.next:` **两个条件缺一不可**（只判 `fast` 会在偶数长度链上对 `None.next` 取值 → NPE；只判 `fast.next` 在奇数长度链尾部走两步会越界），相遇即有环；追问「**环的入口**」→ 相遇后让一个指针回 head、两指针同速前进，再次相遇点即入口（Floyd，用「相遇点到入口 == head 到入口」这段数学说清）。**LC21**：dummy + 双指针，`while l1 and l2` 逐个接小者，**循环结束后必须把没走完的那条整条接上**（`cur.next = l1 or l2`）。**LC19**：**dummy 是必需的**（要删的可能就是头节点）；**快指针先走 n+1 步**（不是 n——这样 slow 停在待删节点的**前一个**，才能 `slow.next = slow.next.next`），然后快慢同走到 fast 为 None。**面试口述：** 「链表题通用套路 = **dummy + 快慢指针 + 先存后改**」；主动补一句「链表不能随机访问，能用 O(1) 空间就别开额外数组」。**坑：** LC206 返回 `cur`；LC141 少写 `fast.next` 判空（偶数链 NPE）；LC19 快指针只走 n 步（差一步删错节点）或忘 dummy（删头直接错）；LC21 忘了接上剩余部分；改指针前没存 `next` 导致断链后找不到后继。

### 重点坑

- [ ] **BST 验证的经典假解（今天最容易翻车的一点）**：LC98 **只比较「父节点与左右孩子」是错的**——反例 `[5,1,4,null,null,3,6]`：根 5、右孩子 4 满足局部关系，但 4 在 5 的左子树里不合法。正解是**自顶向下传开区间上下界** `dfs(node, low, high)`，左子树收紧上界、右子树收紧下界；边界别写死 `±2**31`，用 `None` 或 `±inf`。同理 LC230「BST 第 k 小」的正解是**中序遍历计数**（BST 中序 = 升序，遍历到第 k 个立刻剪枝返回），不是排序后再取。
- [ ] **递归的返回值语义（树题的根）**：写之前先口述「这个函数向上返回什么」——LC104 返回深度、LC100 返回布尔、LC236 返回「子树里找到的目标节点或 None」。三个高频坑：① LC100 把「都空」和「只有一个空」合并判（`None` 与 `None` 被误判成 False）；② LC102 内层循环写 `for node in q` 而不是 `for _ in range(len(q))`（**本层长度必须在进入内层前固定**，否则新入队的下一层节点被当成本层）；③ LC105 用列表切片递归（O(n) 拷贝 → 总复杂度 O(n²)），要用**下标区间 + 哈希表定位根**。
- [ ] **图遍历里「标记访问」的时机（顺序错了就是假解）**：进 DFS 后**先标记再递归**——LC200 必须「进函数 → 越界/非陆地判空 → **立刻淹没** → 再递归四方向」，先递归后淹没会重复访问甚至栈溢出；LC133 克隆图必须「**先建克隆节点并写入 `visited`**，再递归邻居」，否则 A→B→A 的环无限递归。另外 **BFS 一律用 `collections.deque`**，`list.pop(0)` 是 O(N) 会把整个 BFS 拖成 O(N²)（Time Limit Exceeded 的经典原因）。
- [ ] **拓扑排序必须验环（最隐蔽的假解）**：LC207 的 Kahn 版最后一定要 **`return count == n`**（等价写法 `len(order) == n`）——忘了这句，有环输入会「静默」返回一个不完整的拓扑序，而大多数样例仍然能过。DFS 版三色要写对：**状态 1（访问中）被再次遇到才是环**，状态 2（已完成）是剪枝直接返回 False。
- [ ] **链表三铁律 + Dummy（一次写对的前提）**：① 改 `cur.next` 前先存 `nxt`；② 用 `fast.next.next` 前先判 `fast and fast.next`；③ 涉及换头 / 删头（LC19、LC21）一律先建 `dummy`。具体到题：LC19 快指针要**先走 n+1 步**（不是 n）；LC206 迭代版**返回 `prev` 而不是 `cur`**；LC21 循环后**必须把没走完的链表整条接上**（`cur.next = l1 or l2`）。
- [ ] **通用（平台差异，Day 6 必查）**：**递归深度平台差异**——LeetCode 的 `sys.getrecursionlimit()` = 550000，但 **HackerRank / CoderPad 默认 ~1000**：链状树 / 长路径（LC124、LC297）预估深度 > 1000 就主动写迭代版（这正好是 Day 6 的 LC297 会踩的点，**提前想好 BFS 版序列化**）。**`[[0] * n] * n` 建矩阵各行共享引用**，必须 `[[0] * n for _ in range(n)]`（Day 6 的 LC54 就会用到）。表达顺序固定：**brute-force → 时空复杂度 → 优化方向**，动手前先说清「不变量 + 返回值语义」。

### 建议刷的新题

- [ ] **树 / 序列化（Day 6 主菜，Tier S）**：Serialize and Deserialize Binary Tree（LC297，Hard）— 关联已掌握知识点：LC102 的层序遍历 + LC105 的前序建树。**核心：** 序列化与反序列化**必须用同一套遍历序**；前序版用 `#`（或 `null`）**给空节点占位**——**「为什么必须保留空节点」是本题的命门**：不占位时 `[1,2]` 与 `[1,null,2]` 无法区分，树就不唯一了；反序列化用 `iter(data)` / `deque` 逐元素消费（**别用全局下标变量，容易在递归里漏自增**），前序版递归 `build()`、BFS 版把子节点（含 null）成对入队再逐层接回。**追问：** ① 为什么必须保留空节点（见上）；② 前序 vs BFS 两版的取舍（BFS 不吃递归栈、前序代码更短——**正好接上今天的平台递归深度话题**）；③ 若要求格式「人类可读 + 无歧义」，分隔符要怎么设计（节点值可能含分隔符 → 长度前缀或转义）。**坑：** 分隔符冲突；`split()` 后没处理空串；只序列化非空节点导致二义性；递归版在 CoderPad 上深度超限；反序列化时下标变量忘记前进。
- [ ] **树 / BST**：Kth Smallest Element in a BST（LC230，Medium）— 关联已掌握知识点：LC98 的「中序 = 升序」+ LC235 的 BST 指路。**核心：** ① 中序遍历 + 计数器，**遍历到第 k 个立刻剪枝返回**（O(n) 时间、O(h) 空间）；② **迭代中序（显式栈）** 把空间稳在 O(h) 且不惧深树；③ follow-up：**「BST 频繁增删 + 频繁查第 k 小」→ 每个节点维护子树大小**，用「左子树大小 + 1」二分下降做到查询 O(h)；**追问：** 「第 k 大」怎么改（中序反过来：右 → 根 → 左）；k 变成 0-based 会怎样。**坑：** 用闭包 / 全局变量计数却在剪枝后没真正停下；中序写成前序；忘了 k 从 1 开始；不剪枝导致遍历整棵树。
- [ ] **矩阵（Day 6 主菜）**：Spiral Matrix（LC54，Medium）+ **链表输入变种**（面经 23）— 关联已掌握知识点：LC200 的网格边界判断 + LC206 的指针/方向管理。**核心：** 四指针 `top / bottom / left / right` 收缩，按「右 → 下 → 左 → 上」四段走，**每走完一段立刻收缩对应边界并检查 `top > bottom` / `left > right` 提前退出**（只在最外层判一次的话，奇数行/列会重复打印中间那一行）；**链表变种**：先把链表节点按螺旋顺序填进 `n × m` 矩阵——先建矩阵、复用同一套四指针，用 `node = node.next` 驱动（**先确认链表长度再决定矩阵维度**）。**追问：** 终止条件该放哪（每段之后都要判，不只在最外层）；非方阵 `m ≠ n` 时哪一段退化；若要求**反向螺旋**怎么改（换方向数组 + 起始边界）。**坑：** 边界检查只写最外层 → 单行/单列重复输出；`for` 区间端点混用 `<=` / `<`；「先判断再收缩」与「先收缩再判断」搞反；**`[[0] * m] * n` 建矩阵共享引用**（必须列表推导式）。
- [ ] **图 / 网格 DFS 回溯**：Word Search（LC79，Medium）— 关联已掌握知识点：LC200 的网格 DFS 模板 + 回溯。**核心：** 外层枚举每个格子作为起点、内层 DFS 枚举匹配长度；**原地标记** `board[r][c] = '#'` 表示「当前路径已用」，**回溯时务必还原** `board[r][c] = ch`——**这是与 LC200「沉岛」最大的区别：LC200 是永久淹没，本题是路径回溯**；剪枝：首字符不匹配直接跳过、匹配长度够立即返回 True。**追问：** 若允许重复使用同一格（不回溯）会怎样；**进阶 LC212 Word Search II → Trie + 网格 DFS**（词典建 Trie，一次遍历同时搜多个词，并用 `node.children.pop()` 做 Trie 剪枝），这正是 Day 3 的 LC208 的直接应用。**坑：** 忘了回溯还原字符（最经典）；越界/已访问判断写在标记之后；用额外 `visited` 数组而不做原地标记（多 O(mn) 空间，面试要能说清取舍）；起点循环里忘了「每个起点都要独立 DFS」。
- [ ] **链表 / 堆（Tier S，面经 21）**：Merge k Sorted Lists（LC23，Hard）— 关联已掌握知识点：LC21 Merge Two Sorted Lists + LC253 的最小堆。**核心：** ① **最小堆**：把 k 个链表头入堆（元组 `(val, i, node)`，**`i` 是必需的**——ListNode 不可比较，值相同时要能比出大小），每次弹出接到结果尾部、再把它 `.next` 入堆，O(N log k)、O(k) 空间；② **分治 / 两两归并**：像归并排序一样两两合并，O(N log k) 也可以做到 O(1) 额外空间（**面试常被追问「不用堆怎么做」**）；③ 朴素逐个合并是 O(N·k)，要主动说它的退化。**追问：** **「链表太大放不进内存」→ 外部归并排序**（分批读入排序落盘 + 多路归并 + 读缓冲），这是面经 21 明确问的；堆和分治在什么场景下各有优势（k 很大且流式 → 堆；追求最小额外空间 → 分治）。**坑：** 堆元组写成 `(node.val, node)`（值相等时比较 ListNode 直接抛异常）；忘了把弹出的节点的 `.next` 放回堆；k == 0 或某个链表为空没处理；用 O(N·k) 的逐个合并却报 O(N log k)。

## 历史复习记录
- 2026-09-23：树与递归、图论 BFS/DFS、链表
- 2026-09-22：滑动窗口 & 字符串、动态规划（股票系列）、间隔 & 设计题（堆）
- 2026-09-21：图论 BFS/DFS、链表、树与递归
- 2026-09-20：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-17：图论 BFS/DFS、链表、树与递归
- 2026-09-16：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-15：图论 BFS/DFS、链表、树与递归
- 2026-09-14：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-13：图论 BFS/DFS、链表、树与递归
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 4 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 1 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
> ⚠️ 待同步：`blind-75-overview.md` 里 LC323 Number of Connected Components 仍是 `[ ]`，但笔记 `leetcode/union-find/0323-number-of-connected-components.md` 已存在且复习清单一直按「已掌握」复习 → 勾选后应为 **23 / 76**。

**总共 LeetCode 完成：33 道题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
