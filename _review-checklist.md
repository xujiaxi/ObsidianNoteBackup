# 🎯 面试复习清单

## 📅 今日复习（2026-09-21）

> 对齐 `plans/two-week-plan-2026-09-18.md` **Day 4 · 字符串与哈希**：[LC68 Text Justification] + [LC1062 / 1044 Longest Repeating Substring] + [LC408 Valid Word Abbreviation]；Design 走 M2 Notification。热手组按轮换切到「图论 / 链表 / 树」——本组距上次复习 4 天（09-17），上一组的滑窗 / 股票 DP / 间隔设计堆昨天（09-20）刚过，不重复；三组交替，本组今天回归。

### 需要回顾

- [ ] **图论 BFS/DFS**：Number of Islands（LC200）、Clone Graph（LC133）、Course Schedule（LC207）、Shortest Bridge（LC934）、Number of Connected Components（LC323） — **核心：** 先按题型选算法——**连通分量 → DFS**（最省代码）、**最短路径 / 多源扩散 → BFS 逐层**、**拓扑与入度 → Kahn（BFS，迭代无栈溢出）**、**环检测 → 三色 DFS 或 Kahn 数节点**。**LC200 沉岛**：`grid[r][c] = '0'` 原地淹没，DFS 开头一句 `越界 or 是水 → return` 兜住一切；**面试先问「可以改原数组吗」**，不许改就退回 `visited` 集合。**LC133 深拷贝**：`visited = {原节点: 克隆}`，**必须 `visited[node] = clone` 先入表、再遍历 `neighbors` 递归**——顺序反了 A→B→A 就无限递归；`node is None` 直接返回 `None`。**LC207 三色**：0 未访问 / 1 访问中 / 2 已完成，撞到 1 → 有环；Kahn 版则是 `count == n` 判无环（**不需要 visited，入度归零天然防重**）。**LC934 最短桥**：标准两段式——先从一个岛 DFS 把整块标成 `2`，**顺手把临水的边界格一起入队**，再做**多源 BFS** 逐层扩水，**层数（`for _ in range(len(q))` 每层计数）就是最短距离**；「DFS 标记阶段就把水格入队」是本题最优雅的一步。**LC323 并查集**：`find` 带**路径压缩**、`union` 带**按秩 / 按大小合并**，每成功合并一次 `count -= 1`（初值 n），复杂度近似 O(α(n))。**复杂度：** 图 / 矩阵 DFS、BFS 都是 O(V + E)（矩阵题记 O(m·n)），空间 O(V)（visited + 队列 / 栈）。**面试口述：** 主动讲「邻接表 vs 邻接矩阵对复杂度的影响」，以及 BFS 的队列**必须用 `deque`**（`list.pop(0)` 是 O(n)，整体会被拖成 O(n²)）。**坑：** 入队时忘标 visited → 同一格重复入队（**BFS 要入队即标记**）；Clone Graph 先递归后入表导致爆栈；Kahn 忘判「有环则 `count < n`」；沉岛改了不许改的数组；`dirs` 手写四个 `if` 漏方向；并查集忘路径压缩退化成 O(n)。**（本组为轮换复习：09-20 复习的是滑动窗口 & 字符串 / 股票 DP / 间隔设计堆；本组距上次复习 4 天）**
- [ ] **链表**：Reverse Linked List（LC206）、Linked List Cycle（LC141）、Merge Two Sorted Lists（LC21）、Remove Nth Node From End（LC19） — **核心：** 三条铁律——① **断链预警**：改 `curr.next` 之前先存 `nxt = curr.next`；② **空指针检查**：用 `.next.next` 前保证 `fast` 与 `fast.next` 都非空；③ **Dummy Node**（`dummy = ListNode(0); dummy.next = head`）把「头节点变动 / 删除头节点」的特殊分支一次性消掉。**LC206 反转**：`prev, curr = None, head`，循环里固定四步 **`nxt = curr.next → curr.next = prev → prev = curr → curr = nxt`**，顺序不能错；递归版是 `head.next.next = head; head.next = None`（空间从 O(1) 变 O(n)）。**LC141 快慢指针**：`slow = slow.next`、`fast = fast.next.next`，**循环条件写 `while fast and fast.next`**（顺序反了 `fast.next.next` 先炸）；相遇即有环，进阶 Floyd 找入环口：相遇后一个指针回到 head，两指针同速再遇处就是入环点。**LC21 合并**：dummy + 尾指针 `tail`，两侧都非空则接较小者，**收尾直接把没走完的那条链整条挂上**（不要循环一个个搬）。**LC19 倒数第 n 个**：dummy 起步让 `fast` 先走 **n+1** 步，再 `slow` / `fast` 同速走到 `fast is None`，此时 `slow` 停在被删节点的**前一个**，做 `slow.next = slow.next.next`——**不能停在待删节点上**（那样删不掉）。**复杂度：** 全部 O(n) 时间、O(1) 空间（递归反转的 O(n) 栈空间要主动说明）。**面试口述：** 链表题一律「先在纸上画 dummy + 讲清指针顺序」，再落笔；被问「能不能不开额外空间」时指出快慢指针 / 多指针就是 O(1) 双指针法。**坑：** 反转时没存 `nxt` 直接丢链；`while fast.next and fast` 顺序写反；LC19 的 `fast` 少走一步、或 `slow` 停在待删节点；LC21 用循环硬搬剩余链（多写还易错）；忘记 `return dummy.next` 而是 `return head`（head 可能已被删 / 已变）；**1 个节点 / 2 个节点 / n 等于表长 / 空表** 这些边界没测。**（本组为轮换复习：09-20 复习的是滑动窗口 & 字符串 / 股票 DP / 间隔设计堆；本组距上次复习 4 天）**
- [ ] **树与递归**：Maximum Depth（LC104）、Same Tree（LC100）、Invert Binary Tree（LC226）、Level Order Traversal（LC102）、Validate BST（LC98）、LCA of BST（LC235）、LCA of Binary Tree（LC236）、Construct from Preorder + Inorder（LC105） — **核心：** 树题 80% 是同一套「**base case → 递归左右 → 向上汇总**」，**先想清楚递归函数的返回值语义再写代码**，说不出来就是还没想清。**LC104 深度** `1 + max(depth(l), depth(r))`；**LC100 Same Tree** 两处 base case（都空 True、只有一个空 False）再比 `val` 并递归两侧；**LC226 反转** 交换左右子树后递归（前序 / 后序都行，中序会错）。**LC102 层序**：`deque` + **`for _ in range(len(q))`** 逐层取出，**绝不把计数器变量当循环条件**；入队前判 `if node.left` / `if node.right`。**LC98 验证 BST**：**只能用「上下界 (low, high)」传递法**——`dfs(node, low, high)` 要求 `low < node.val < high`，左子树带上界 `node.val`、右子树带下界；**只比较「父 > 左子 && 父 < 右子」是错的**（反例 `[5,1,4,null,null,3,6]`）。**LC235 BST LCA**：从根下降，`p.val`、`q.val` 都小于当前值就向左、都大就向右、**一左一右（或等于当前值）即当前节点**，O(h)。**LC236 普通树 LCA**：后序——左右各找一次，**两边都非空 → 当前节点就是答案；只有一边非空 → 把非空那边往上传**；`None` 表示这边没有。**LC105 建树**：`preorder[0]` 是根，用哈希表把 `inorder` 的「值 → 下标」预处理成 O(1)，按中序左右段长度递归；**必须带 `pre_lo/pre_hi/in_lo/in_hi` 下标传参、不要真的切数组**（否则时间 / 空间都退化成 O(n²)）。**复杂度：** DFS 类 O(n) 时间 / O(h) 空间，层序 O(n) / O(w)，BST 下降 O(h)。**面试口述：** 主动讲「递归空间复杂度是树高 h，退化成链时 h = n，Python / Java 都可能 `StackOverflowError`」——正好接上今天深树的追问。**坑：** LC98 用局部比较而非全局上下界（最经典的假解）；LC236 忘了「两边都非空才返回当前节点」、或找到后继续往上覆盖；LC105 每次切数组导致 O(n²) 或没处理重复值；层序忘了 `for _ in range(len(q))` 分不出层；base case 漏了「只有一个孩子」；递归有的分支 `return` 有的忘 `return`（返回 None 静默出错）。**（本组为轮换复习：09-20 复习的是滑动窗口 & 字符串 / 股票 DP / 间隔设计堆；本组距上次复习 4 天）**

### 重点坑

- [ ] **树 / BST 的上下界传递 & 返回值语义（今天最容易翻车的一点）**：验证 BST 不是「父 > 左子 && 父 < 右子」这种局部判断，**必须把 `(low, high)` 区间一路往下传**（左子树缩上界、右子树抬下界）；同理 LC236 的返回值语义要统一——`None` = 这边没找到，非空 = 指向找到的那个节点（**在第一个分叉点就返回，不要继续往上覆盖成祖先**）。**写递归前先回答「这个函数的返回值代表什么」，答不出来就是还没想清。**
- [ ] **链表指针顺序（写错必炸）**：反转固定四步 `nxt = curr.next → curr.next = prev → prev = curr → curr = nxt`；**凡是「要改 `curr.next`」的地方，改之前必先存下旧 `next`**。快慢指针条件 `while fast and fast.next`，顺序写反直接 `AttributeError`。LC19 的 `fast` 要多走 **n+1** 步（dummy 起步），`slow` 必须停在待删节点的**前一个**；收尾统一 `return dummy.next`。
- [ ] **方向数组 & BFS 分层（网格题通病）**：四方向写成 `dirs = ((1, 0), (-1, 0), (0, 1), (0, -1))` 循环，不要手写四个 `if`（易漏易错）；BFS 逐层计数用 **`for _ in range(len(queue))`**，**绝不把计数器变量当循环条件**（`while counter > -1` 是恒真循环，只是靠题面保证才退出，面试官一眼看出）；入队时就要标记 visited，否则同一格会被重复入队。
- [ ] **字符串题通病（今天 Day 4 的主菜）**：LC68 文本对齐的三大分支——**单行 / 最后一行 / 普通行**；最后一行与单行**左对齐、词间只放一个空格**（不两端对齐），普通行把空格**均分 + 余数从最左边开始多分一个**（`space = total // gaps`、`extra = total % gaps`），**单词数 = 1 时不能除零**（直接补满空格）；**单个单词长度 > maxWidth 这个边界要先处理**。LC408 缩写校验：**数字不能有前导 0**（`"01"` 直接 False）、多位数要累积 `num = num * 10 + d`、数字不能越过单词末尾。rolling hash（LC1062 / 1044）要主动讲**大质数模数 + 溢出处理 + 哈希冲突时用真实子串比对兜底**。
- [ ] **递归深度 & 平台差异（09-18 实测结论）**：LeetCode 的 `sys.getrecursionlimit()` 是 **550000**（300×300 网格 DFS 最深 90000 也安全），但 **HackerRank / CoderPad 默认只有 ~1000** → **同一份代码在 LC 过、在面试平台 RE**。规则：**预估最深递归 > 1000 就主动写迭代版**（显式栈或 BFS），或者在写之前就把这个平台差异说出来；Python ≤ 3.10 调高 `recursionlimit` 有段错误风险（3.11+ 纯 Python 调用被内联才相对安全）。
- [ ] **通用**：Java 比较 `Integer` 用 `.equals()` 而非 `==`（IntegerCache 只覆盖 −128~127）；`PriorityQueue` / `heapq` 默认小顶堆，Python 最大堆存负数；`[[0] * n] * n` 各行共享引用，必须用列表推导式；**哈希 key 不能是 list**（`sorted(s)` 返回 list → 用 `''.join(sorted(s))` 或 `tuple(sorted(s))`），而 **`frozenset(s)` 会丢字符计数**，把 `"aab"` 和 `"abb"` 误判成同组（LC49 / LC242 必踩）；**表达顺序固定为：brute-force → 时空复杂度 → 优化方向**，动手前先说清「不变量 + 返回值语义」。

### 建议刷的新题

- [ ] **哈希 / 字符串**：Valid Anagram（LC242，Easy）— 关联已掌握知识点：LC1 Two Sum 的哈希计数 + 今日「哈希 key 设计」坑。**核心：** ① `Counter(s) == Counter(t)`（最好写，O(n)）；② **长度 26 的计数数组**（+1 / −1 后判全零，O(n) / O(1)，面试最加分）；③ `sorted(s) == sorted(t)`（一行但 O(n log n)）。**追问：** 输入含 Unicode / 大小写混排怎么办（用 `Counter`，数组只能覆盖 26 个小写字母）；「进阶：最多删一个字符能否成为异位词」→ 先比长度、再双指针找第一个差异位。**坑：** 用 `set` 或 `frozenset` 比较（`"aab"` / `"abb"` 误判）；忘了先比长度；计数数组只加不减 / 忘记判全零。
- [ ] **哈希 / 字符串**：Group Anagrams（LC49，Medium，**Tier S**）— 关联已掌握知识点：LC242 的 key 归一化 + LC1 哈希映射 + 「read vs write 权衡」。**核心：** 同一组异位词的 key 必须与字符顺序无关——① `''.join(sorted(w))` 作 key（O(n·k log k)，好写，面试首选）；② `tuple(cnt)`（26 长度计数数组，O(n·k)，更快但费空间）；③ 质数乘积作 key（讲得出即可，别真写，会溢出）。**变种（面经第 4 题，本 plan 最重要的一问，Day 3 尚未落地）**：要支持 `add(word) / remove(word) / largestGroup()`，就必须回答「**读多还是写多**」——**read-optimized**：写入时就算好 key 存进 `dict[key] -> set`，`largestGroup` 用「按 size 分桶」的计数结构做到 O(1)；**write-optimized**：写入只追加，读时再分组。要能给出指标支撑（读写比、SLA、组的数量级）。**面试口述：** 先讲「把异位词归一化成同一个 key」这个母题，再说变种是「设计一个支持增删 + 查询最大组的数据结构」。**坑：** 用 `frozenset` 当 key（丢计数、错分组）；用 list 当 key（`TypeError: unhashable type`）；忘了对 `sorted` 结果 join；变种里删除时没同步维护分桶计数；HackerRank **没有 `sortedcontainers`**，`largestGroup` 要手写堆或桶数组。
- [ ] **滑动窗口 / 字符串**：Longest Repeating Character Replacement（LC424，Medium）— 关联已掌握知识点：LC3 / LC76 的**万能滑窗模板**（外 `while` 扩右、内 `while` 收左）。**核心：** 窗口 `[l, r]` 的判据是 `(r - l + 1) - maxFreq > k` → 收缩左指针；**`maxFreq` 只增不减**（只有更大的窗口才可能刷新答案，收缩时不回退也不会算错最优解，这是本题最关键的一步优化）；整体 O(n)。**追问：** 为什么 `maxFreq` 不用在收缩时重新计算？如果 k 巨大 / 窗口里全是同一字符会怎样？**面试口述：** 这是 LC3（求最长）与 LC76（求最短）之后的**第三个滑窗变体**，做完这条线就闭环；主动对比「扩到违规就停」的长窗口模式。**坑：** 每步重算窗口内 `max(count.values())` → O(26n) 常数大且必被追问；用 `if` 收缩而不是 `while`（条件可能连续不满足要连收）；把 k 理解成「最多替换 k 个不同字符」；空串 / k ≥ 长度这些边界没测。
- [ ] **链表 / 堆**：Merge K Sorted Lists（LC23，Medium，**Tier S**）— 关联已掌握知识点：LC21 合并两个有序链表 + LC253 最小堆 + 今天的堆基础。**核心：** ① **最小堆**——把 k 个链表头入堆 `(val, i, node)`（**必须带可比较的 `i`**，ListNode 本身不可比较），每次 `heappop` 接在 dummy 尾部，若该节点有后继就继续入堆，O(N log k)；② **分治两两合并**——`merge(lists[i], lists[i + m])` 迭代版（bottom-up 归并，常数更小、不用堆），O(N log k)；③ **外部归并排序追问**：链表超过内存时按块排序落盘 + k 路归并读，堆里存 `(val, 块号, 块内偏移)`，本题就是它的内存版。**面试口述：** 三种做法全报出来并比较；被问「为什么不用堆」时说分治的常数和缓存友好性。**坑：** 堆里放 node 元组不带下标 → `TypeError`；弹出后忘了把它的 `next` 入堆（漏链）；输入里的空链表没跳过；分治时 `lists[i + m]` 越界；忘记 `return dummy.next`。
- [ ] **图论 BFS/DFS**：Pacific Atlantic Water Flow（LC417，Medium）— 关联已掌握知识点：LC200 沉岛 + LC934 的「**从边界反向 / 多源扩展**」思路。**核心：** 正着想（对每个格子判断能否到两个洋）会退化成 O(mn·4^(m+n)) 的暴力，**反过来源头从两个洋的边界做多源 DFS / BFS**，记录各自能到达的格子，最后取两个 visited 的交集；**水往低处流 ⇒ 反向扩要走向「高度 ≥ 当前格」的邻居**（`grid[nr][nc] >= grid[r][c]`）。**追问：** 为什么反向可行（可达性对称）；太平洋是第一行 + 第一列、大西洋是最后一行 + 最后一列（别写错边界）。**坑：** 方向判据写反（正向找更小 vs 反向找更大）；两个洋共用同一个 visited 数组互相污染；求交集前就收集答案；**200×200 网格的 DFS 最深约 40000 → HackerRank / CoderPad 上必须写迭代栈或 BFS 版**（呼应上面的递归深度坑）。

## 历史复习记录
- 2026-09-21：图论 BFS/DFS、链表、树与递归
- 2026-09-20：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-17：图论 BFS/DFS、链表、树与递归
- 2026-09-16：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-15：图论 BFS/DFS、链表、树与递归
- 2026-09-14：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-13：图论 BFS/DFS、链表、树与递归
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 4 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 1 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
**总共 LeetCode 完成：33 道题。掌握基于这些题型的核心思路，可以应对类似面试问题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
