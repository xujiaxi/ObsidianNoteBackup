# 🎯 面试复习清单

## 📅 今日复习（2026-09-20）

> 对齐 `plans/two-week-plan-2026-09-18.md` **Day 3 · 设计数据结构**：[LC146 LRU Cache] + [LC208 Implement Trie] + [LC49 变种 add/remove/largestGroup]；Design 走 M1 Metrics Part 2。下面的「间隔 / 设计题（堆）」一组正好是今天的热手组。

### 需要回顾

- [ ] **滑动窗口 & 字符串**：Longest Substring Without Repeating Characters（LC3）、Minimum Window Substring（LC76） — **核心：** 万能模板 = **外 `while` 扩右指针，内 `while` 收缩左指针**；常数优化靠 **`valid` 计数（已满足的字符种类数）**，不要每步去比较两个 `Counter`（Python 里 `Counter == Counter` 是 O(26)，会把 O(n) 拖成常数很大的 O(n·26)，面试官会追问）。**LC3**：`window` 用 `set` → `while s[r] in window: window.remove(s[l]); l += 1`，再 add、`ans = max(ans, r - l + 1)`；或用 `dict` 记字符最后位置，`l = max(l, last[c] + 1)`（**`max` 不能省，否则遇到更靠左的重复字符会让 `l` 回退**）。**LC76**：先 `need = Counter(t)`，扩右时 `if window[c] == need[c]: valid += 1`（**必须在 +1 之后比较、且只加一次**），当 `valid == len(need)` 时收缩：**先记录 `start / minlen` 再移出 `s[l]`**，移出后若 `window[c] < need[c]` 则 `valid -= 1`，`l += 1`；无解返回 `""`（`minlen` 保持 `inf` 就直接返回空串）。**面试口述：** 先给 brute-force（O(n²) 枚举子串 / O(n·len(t)) 逐个校验），再滑窗到 O(n)；主动区分「求最长」的窗口是**扩到违规就停**、「求最短」的窗口是**合规再尽量收紧**。**坑：** LC3 忘记 `max` 导致 `l` 回退；LC76 用两个 `Counter` 相等判断（TLE 且面试官一眼看出没优化）；`valid` 判断时机错（在自增之前比、或同一字符重复加 valid）；收缩时先移出再记答案（丢掉正确答案）；收缩用 `if` 而不是 `while`（条件可能连续不满足）；`t` 比 `s` 长、`t` 里有重复字符、空串这些边界没测。**（本组为轮换复习：09-17 复习的是图论 / 链表 / 树，今日轮回到滑动窗口 & 字符串 / 股票 DP / 间隔设计堆；三组交替，本组距上次复习 3 天）**
- [ ] **动态规划（股票系列）**：Best Time to Buy and Sell Stock I / II / III / IV（LC121 / 122 / 123 / 188）、with Cooldown（LC309）、with Transaction Fee（LC714） — **核心：** 整个系列只有**一台状态机**——`hold`（当天持股时的最大现金）与 `cash`（当天不持股的最大现金），每天的转移永远只有两条：**卖出** `cash = max(cash, hold + p)`，**买入** `hold = max(hold, cash - p)`。**LC121（k=1）**：`hold = max(hold, -p)`、`cash = max(cash, hold + p)`，也可退化成「维护历史最低价」一行。**LC122（无限次）**：注意 **`hold` 必须用「上一轮（昨天）的 `cash`」来更新**，用 `prev_cash` 快照暂存——否则「当天卖出又买回」会凭空产生利润。**LC123 / LC188（限 k 次）**：把 `buy[j] / sell[j]` 做成 j = 1..k 的滚动数组，初始化 `buy = [-inf] * (k + 1)`、`sell = [0] * (k + 1)`，每日对 j 逆序更新；**LC188 必答优化**：`k >= n // 2` 时退化成 LC122 的贪心式状态机（否则 k 巨大直接 TLE，这是本题的隐藏考点）。**LC309（冷冻期）**：买入用「**前天**的 cash」→ `hold = max(hold, prev_cash - p)`，同样靠快照变量实现。**LC714（手续费）**：fee **只在卖出时减一次**（或只在买入时加），两边都减就重复扣费。**面试口述：** 先写通用状态机 `hold/cash`，再按题意加维度（次数 k、冷冻期、手续费），并给出「时间 O(n) / 空间 O(1)（滚动变量）」；被问「能不能贪心」时回答：只有无限次交易（122）才成立（相邻正差求和），有限次必须 DP。**坑：** **转移顺序**——同一轮里先更新 `cash` 再更新 `hold` 却没存快照，等于允许同一天买卖；初始化错（第 0 天 `hold = -p[0]`、`cash = 0`）；返回 `cash` 而不是 `hold`（最后必须空仓）；LC188 忘了 `k >= n//2` 的退化分支；把 k 理解成「最多 k 次交易」还是「k 次买卖」；空数组 / 单元素 / 全程下跌这些边界。**（本组为轮换复习：09-17 复习的是图论 / 链表 / 树，今日轮回到滑动窗口 & 字符串 / 股票 DP / 间隔设计堆；三组交替，本组距上次复习 3 天）**
- [ ] **间隔 / 设计题（堆）**：Meeting Rooms II（LC253）、Hit Counter（LC362）、Design Tic-Tac-Toe（LC348） — **核心：** 「重叠区间」的两个标准解法——① **事件扫描线**：把每个区间拆成 `(start, +1)` 与 `(end, -1)`，排序后求**前缀和的峰值**即为所需资源数（**关键 tie-break：同一时刻要让 `end` 先结算**，用 `(time, delta)` 且 delta 为 −1 优先，否则「一进一出」会被误算成两间房）；② **按 start 排序 + 最小堆存 end**：每来一个 `start` 先把堆里所有 `end <= start` 的弹掉（**必须 `while` 弹干净**），再 push 当前 `end`，过程中 `len(heap)` 的最大值就是答案；O(n log n)。**LC252 Meeting Rooms** 是同一族的判重题（按 start 排序后检查 `prev_end > cur_start`）。**设计题 LC362 Hit Counter**：存 `(timestamp, count)` 的有序结构或 **300 秒环形数组**（`idx = ts % 300`），取数时判断槽位是否过期；**同一秒的重复 hit 要合并计数只算一次**（这是本题唯一的坑）。**设计题 LC348 Tic-Tac-Toe**：用 `rows / cols / diag / anti_diag` 计数数组，玩家 1 记 +1、玩家 2 记 −1，某条线绝对值到 n 即获胜；`move()` O(1)、`win()` 无需遍历棋盘。**堆基础：** Python `heapq` 是**小顶堆**（最大堆存负数）；TopK 用固定大小 k 的小顶堆 + `heappushpop`；**堆里放元组/自定义对象时必须带可比较的下标**（`(val, i, node)`），否则 `TypeError`；HackerRank / CoderPad **没有 `sortedcontainers`**，要手写堆或桶。**面试口述：** 设计题一律先问「读多还是写多 / 数据规模 / 要不要线程安全」再选结构——这套说法和今天 LC49 变种的 read vs write 取舍是**同一套答题逻辑**（主动把两者联系起来是加分项）。**坑：** 扫描线没处理「同一时刻先出后进」导致峰值偏大；堆版用 `if` 只弹一个 end；环形数组取数据时忘了判断槽位过期 / 忘了时间戳去重；设计题默认单线程却不说明；把 O(n) 的操作藏在听起来 O(1) 的方法名后面（面试官必追）。**（本组为轮换复习：09-17 复习的是图论 / 链表 / 树，今日轮回到滑动窗口 & 字符串 / 股票 DP / 间隔设计堆；三组交替，本组距上次复习 3 天）**

### 重点坑

- [ ] **设计数据结构（今天的主菜）**：设计题**先问需求再动笔**——「读多还是写多、数据量级、是否要线程安全、要不要支持删除」。LRU Cache（LC146）= `dict + 双向链表`，用**哨兵 head/tail** 消掉所有空指针判断，改指针顺序固定为「**先接好新节点、再断旧链**」；Trie（LC208）= 节点里 `children: dict` + `is_end`，`search` 与 `startsWith` 的差别只在最后是「查 `is_end`」还是「直接返回 True」；Hit Counter / Tic-Tac-Toe 都是「**用状态数组把昂贵操作摊平到 O(1)**」这同一思路。**别一开口就写代码，先讲清不变量。**
- [ ] **哈希 key 设计**：Python 里 `sorted(s)` 返回 **list，不可哈希**做 key → 用 `''.join(sorted(s))` 或 `tuple(sorted(s))`；计数型 key 用 `tuple(cnt)`（长度 26 的计数数组）比排序更快但费空间；`Counter` 可作 key，但 **`frozenset(s)` 会丢掉字符出现次数**，把 `"aab"` 和 `"abb"` 误判成同组——LC49 变种里这是必踩的错。
- [ ] **递归深度 & 平台差异（09-18 实测结论）**：LeetCode 的 `sys.getrecursionlimit()` 是 **550000**（300×300 网格 DFS 最深 90000 也安全），但 **HackerRank / CoderPad 默认只有 ~1000** → **同一份代码在 LC 过、在面试平台 RE**。规则：**预估最深递归 > 1000 就主动写迭代版**（显式栈或 BFS），或者在写之前就把这个平台差异说出来；Python ≤ 3.10 调高 `recursionlimit` 有段错误风险（3.11+ 纯 Python 调用被内联才相对安全）。
- [ ] **方向数组 & BFS 分层（网格题通病）**：四方向写成 `dirs = ((1, 0), (-1, 0), (0, 1), (0, -1))` 循环，不要手写四个 `if`（易漏易错）；BFS 逐层计数用 **`for _ in range(len(queue))`**，**绝不把计数器变量当循环条件**（`while counter > -1` 是恒真循环，只是靠题面保证才退出，面试官一眼看出）；入队时就要标记 visited，否则同一格会被重复入队。
- [ ] **通用**：Java 比较 `Integer` 用 `.equals()` 而非 `==`（IntegerCache 只覆盖 −128~127）；`PriorityQueue` / `heapq` 默认小顶堆，Python 最大堆存负数；`[[0] * n] * n` 各行共享引用，必须用列表推导式；**表达顺序固定为：brute-force → 时空复杂度 → 优化方向**，动手前先说清「不变量 + 返回值语义」。

### 建议刷的新题

- [ ] **哈希 / 设计**：Group Anagrams（LC49，Medium）— 关联已掌握知识点：LC1 Two Sum 的**哈希映射**思路 + 今日 Day 3 的「read vs write 权衡」。**核心：** 同一组异位词的 key 必须与字符顺序无关——① `''.join(sorted(w))` 作 key（O(n·k log k)，好写，面试首选）；② `tuple(cnt)`（26 长度计数数组，O(n·k)，更快但费空间）；③ 质数乘积作 key（讲得出即可，别真写，会溢出）。**变种（面经第 4 题，本 plan 最重要的一问）**：要支持 `add(word) / remove(word) / largestGroup()`，就必须回答「**读多还是写多**」——**read-optimized**：写入时就算 key 存进 `dict[key] -> set`，`largestGroup` 用一个「按 size 分桶」的计数结构做到 O(1)；**write-optimized**：写入只追加，读的时候再分组。要能给出指标支撑（读写比、SLA、组的数量级）。**面试口述：** 先讲「把异位词归一化成同一个 key」这个母题，再说变种是「设计一个支持增删 + 查询最大组的数据结构」。**坑：** 用 `frozenset` 当 key（丢计数、错分组）；用 list 当 key（`TypeError: unhashable type`）；忘了对 `sorted` 结果 join；变种里删除时没同步维护分桶计数；HackerRank **没有 `sortedcontainers`**，`largestGroup` 要么手写堆要么用桶数组。
- [ ] **设计 / 前缀树**：Implement Trie (Prefix Tree)（LC208，Medium）— 关联已掌握知识点：LC49/LC242 的「字符串 → 结构」映射 + 今天的**设计题套路**（先定不变量、再定接口）。**核心：** 每个 TrieNode 只需两样东西——`children: dict`（或 `[None] * 26`）和 `is_end: bool`；`insert` 沿字符逐层建；`search(word)` 走到底后还要**额外判 `is_end`**（否则「前缀存在」≠「单词存在」），`startsWith(prefix)` 走到底直接返回 True——**这两个方法的唯一差别就是最后那一次判断**，是本题的考点。**追问预演：** ① `dict` vs 定长数组的取舍（稀疏用 dict 省内存、稠密用数组更快）；② Add and Search Word（LC211）就是在 `search` 里加 `'.'` 通配 → 变成 DFS 回溯；③ Word Search II（LC212）用 Trie + 网格 DFS 剪枝（做过这题，Trie 这条线就闭环了）。**面试口述：**「前缀匹配 / 自动补全 / 词典查询」→ 空间换时间，插入与查询 O(L)。**坑：** `search` 忘记判 `is_end`；`startsWith` 也去判 `is_end`（把前缀当单词）；`children` 用了可变默认参数或多个节点共享同一个 dict（Python 经典陷阱）；空串 / 单字符 / 插入后再插入更长的词没测。
- [ ] **设计 / 字符串**（Premium）：Encode and Decode Strings（LC271，Medium）— 关联已掌握知识点：LC49 变种的**读写权衡** + 设计题（LC348/LC362）的「接口先定契约」。**核心：** 用 `','` 拼接 + `split(',')` 是**错的**（字符串本身可能含逗号）——标准解是**长度前缀**：`encode` 输出 `"3:abc4:defg"` 这种「长度 + `:` + 内容」，`decode` 先读长度、按 `:` 定位、再按长度切片；另一条路是**转义分隔符**（遇到 `,` 和 `\` 前加 `\`，解码时反向还原）。**追问：** 「含 Unicode 怎么办」（按字节长度还是字符长度要讲清）、「流式解码」（不用一次性 `split`，边读边解，额外空间 O(1)）——流式这一问能把答案抬到 Strong Hire。**面试口述：** 关键是主动指出「**给定的接口必须能表达任意字符串**」，所以分隔符法必须先证明可转义。**坑：** 直接 `split(',')`；长度用 `len(str)` 但在变长编码下按字节切；`:` 本身出现在内容里；空字符串 / 含分隔符 / 多字节字符这些边界没测。
- [ ] **堆 / 设计**：Find Median from Data Stream（LC295，Hard）— 关联已掌握知识点：LC253 的**双堆 / 最小堆**用法 + 设计题的状态维护（LC362）。**核心：** 维护**两个堆**——`small`（大顶堆，存较小的一半，Python 存负数）与 `large`（小顶堆，存较大的一半），不变量：`len(small) == len(large)` 或 `len(small) == len(large) + 1`；`addNum` 先 push 进 `small`，再把 `small` 的堆顶挪到 `large`（**先过一遍对方堆再平衡**，这是保证大小关系正确的关键技巧，比写一堆 if 判断干净得多）；`findMedian` 看两堆长度，奇数取 `small` 顶、偶数取两顶平均。复杂度 O(log n) / O(1)。**面试口述：** 先说 brute-force（每次排序 O(n log n)，或用 `sortedcontainers`），再说明「为什么要两个堆」——各自只暴露一半数据的最值。**坑：** 忘了 `small` 存负数导致大顶堆变错；平衡时只挪了元素没维护大小关系（不变量是在「插入顺序 + 先进对方堆」下才成立的）；`len(small)` 只能等于或比 `large` 多 1，写成 ≥ 会让中位数算错；`findMedian` 时两堆都空；CoderPad **没有第三方库**，堆要手写或 `heapq` + 负数。
- [ ] **堆 / TopK**：Top K Frequent Elements（LC347，Medium）— 关联已掌握知识点：LC1 的哈希计数 + LC253 的**最小堆** + 今天复习的堆基础。**核心：** ① `Counter` 计数后维护**大小为 k 的小顶堆**（`heappushpop` 保持容量），O(n log k)；② **桶排序**（下标 = 频次）做到 O(n)，并说明「频次上界是 n，所以桶可行」；③ quickselect 平均 O(n) 但最坏 O(n²)。**追问（面经第 25 题的核心）：** 流式数据 / k 会变 / 内存受限时**只能用堆**——要能讲出为什么桶排序和 quickselect 在这些约束下不适用（桶需要全量数据、quickselect 需要随机访问）。**面试口述：** 三种解法全报出来并横向比较，比只写一个堆更稳。**坑：** 堆里存 `(freq, val)` 元组时要保证可比较（val 若是不可比较对象要加下标）；`heapq.nlargest(k, cnt.items(), key=lambda x: x[1])` 是捷径但要能讲清底层；k 大于不同元素个数；返回顺序无关（题目不要求排序）。

## 历史复习记录
- 2026-09-20：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-17：图论 BFS/DFS、链表、树与递归
- 2026-09-16：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-15：图论 BFS/DFS、链表、树与递归
- 2026-09-14：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-13：图论 BFS/DFS、链表、树与递归
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 4 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 1 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
**总共 LeetCode 完成：33 道题。掌握基于这些题型的核心思路，可以应对类似面试问题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
