# 🎯 面试复习清单

## 📅 今日复习（2026-09-15）

### 需要回顾

- [ ] **图论 BFS/DFS**：Clone Graph（LC133）、Course Schedule（LC207）、Number of Islands（LC200） — **核心：** 图论题第一步永远是**建模**——「点/边是什么？有向还是无向？要判环还是要连通块？」，模型错了后面全错。**LC200 Number of Islands（沉岛算法）**：遍历网格，遇到 `'1'` 就 `count++` 并立刻从该点 DFS/BFS，把联通的所有陆地**就地改写为 `'0'`**（沉岛），这样天然去重、不用额外的 `visited` 集合；外层双重循环负责发现新岛屿，内层只负责「淹没」。方向数组 `dirs = [(1,0),(-1,0),(0,1),(0,-1)]` 比写四段 if 干净。**LC133 Clone Graph**：`HashMap{原节点 → 克隆节点}` 一个结构干两件事——**既是 visited（防环/防重复递归），又是结果映射**（邻居要指向克隆节点而不是原节点）。DFS 写法：先查 map 命中则直接返回；否则 new 一个克隆放进 map（**必须先放再递归邻居**，否则环上无限递归），再遍历 `node.neighbors` 递归克隆并挂到克隆节点上。**LC207 Course Schedule**：本质是**有向图的环检测**，两条路——① **三色标记法 DFS**：`white` 未访问 / `gray` 在当前递归栈上 / `black` 已完成，递归时把节点涂灰，若邻居是**灰色**说明成环（回边），若是白色则递归，返回前涂黑；`graph[a].append(b)` 表示「修 a 之前必须先修 b」（边指向先修课），DFS 里沿边往下走。② **Kahn's Algorithm（BFS 拓扑排序）**：统计每个节点入度，入度为 0 的入队，出队时把邻居入度 −1、减到 0 就入队；**出队节点数 < 总节点数 ⇒ 有环**。**面试口述：**「岛屿/连通块」→ 网格 DFS/BFS + 沉岛；「课程安排/编译依赖/谁先谁后」→ 有向图环检测（三色标记 或 拓扑排序）；「无向图连通分量个数」→ Union-Find 或 DFS「染色计数」。**坑：** 深度递归在数据量大时 `StackOverflowError`（Java）/`RecursionError`（Python）→ 换 BFS + 队列（`deque`）或显式栈；LC207 中**灰色才代表有环**，只看 `visited` 布尔值会把「已完成」和「在栈上」混为一谈；**无向图**判环不能照搬有向图三色标记（父子间的那条边会被误判成环），必须记录 `parent` 或改用 Union-Find；岛屿题 `count++` 只在外层循环发现新 `'1'` 时发生，DFS 内部绝不 `count++`；`graph` 用**邻接表**（`defaultdict(list)`）而不是邻接矩阵（O(V²) 浪费空间）。

- [ ] **链表**：Reverse Linked List（LC206）、Linked List Cycle（LC141）、Merge Two Sorted Lists（LC21）、Remove Nth Node From End Of List（LC19） — **核心：** 链表题的全部难点都在**指针改写顺序**和**头节点边界**，而 **Dummy Node（哨兵节点）** 是解决后者的一把万能钥匙——凡是「可能删除/替换头节点」的题（LC19 删头、LC21 合并、以及各种「重排」），先 `dummy = ListNode(0); dummy.next = head`，最后返回 `dummy.next`。**LC206 反转链表**：迭代版三指针 `prev = None`、`cur = head`，循环体只做三件事且**顺序不能乱**——`nxt = cur.next`（先存后路）→ `cur.next = prev`（掉头）→ `prev, cur = cur, nxt`（整体前移），结束时 `prev` 就是新头。递归版：`new_head = reverse(head.next)`，然后 `head.next.next = head; head.next = None; return new_head`，**base case `if not head or not head.next: return head` 不能漏**。**LC141 环形链表**：Floyd 快慢指针——`slow` 走 1 步、`fast` 走 2 步，**有环则必相遇**（相对速度 1，快指针每轮逼近慢指针一格，不会跳过），循环条件 `while fast and fast.next`（两个都要判，`fast.next.next` 才不会越界）；想求**环入口**就先相遇、再把一个指针放回头部同速前进，二次相遇点即入口。**LC21 合并两个有序链表**：`dummy + tail`，每次比较 `l1.val` 与 `l2.val`，把较小的接到 `tail.next` 并前移，循环结束后把**剩下那条非空链表整段接上**（`tail.next = l1 or l2`）。**LC19 删除倒数第 N 个节点**：**dummy + 快慢指针 + 固定间隔**——快指针先从 dummy 走 `n + 1` 步（多走 1 步是为了让慢指针**停在待删节点的前驱**），然后快慢同速前进直到快为空，此时 `slow.next = slow.next.next` 完成删除；用 dummy 的意义在于「删头节点」时前驱就是 dummy，无需特判。**面试口述：**「反转/合并/删除第 k 个」→ 先声明 dummy，再画图确认指针改写顺序；「判断有无环 / 找中点 / 找倒数第 k 个」→ 快慢指针（Floyd）。**坑：** 反转时**先丢 `next` 再改指针**，后路一断整条链就找不回来了；LC19 快指针走的是 **`n + 1`** 步而非 `n` 步（走 `n` 会让 slow 停在待删节点本身，无法删除）；循环条件漏写 `fast.next`（空链表 / 单节点会 NPE）；LC21 收尾忘了 `tail.next = l1 or l2`（尾巴丢了）；递归写链表题注意**深度 = 链表长度**，长链表会栈溢出；`while` 与 `if` 后面返回的是 `dummy.next` 而不是 `dummy`。

- [ ] **树与递归**：Invert/Flip Binary Tree（LC226）、Maximum Depth of Binary Tree（LC104）、Same Tree（LC100）、Binary Tree Level Order Traversal（LC102）、Construct Binary Tree from Preorder and Inorder（LC105）、Validate Binary Search Tree（LC98）、Lowest Common Ancestor of BST / of Binary Tree（LC235 / LC236） — **核心：** 树题就是**递归三板斧**：① **终止条件**（`if not root: return ...`）② **处理当前节点**（干最少的活，通常是 `root.val` 相关）③ **递归左右子树**（把子问题的返回值**组合**成当前答案）。能想清「函数签名的语义是什么、子树返回什么」这题就结束一半了。**LC226 翻转**：`root.left, root.right = root.right, root.left` 后递归两侧（前序/后序都行，**不能中序**——中序会导致某个子树被反转两次）。**LC104 最大深度**：`1 + max(depth(left), depth(right))`，空节点返回 0。**LC100 相同树**：结构不同或值不同立即返回 `False`，两边都空返回 `True`，否则 `isSameTree(p.left, q.left) and isSameTree(p.right, q.right)`。**LC102 层序遍历**：BFS + `deque`，**关键是每层进循环前先记 `size = len(queue)`**，用 for 循环恰好弹出本层 `size` 个节点、把它们的非空孩子入队，这样才能按层切分结果。**LC98 验证 BST**：**绝不能只比较父子**（反例 `[5,1,4,null,null,3,6]` 中 3 比 5 小却被放过）——正解是**传上下界** `(node, low, high)`，左子树所有值必须 `< node.val` 且 `> low`，右子树必须 `> node.val` 且 `< high`；或**中序遍历**（结果必须**严格递增**，注意用 `<=` 判重是否合法要看题意）。**LC105 构造二叉树**：**前序首元素 = 根**，拿着根的值去**中序**里定位，中序左侧是左子树、右侧是右子树；用 `HashMap{值 → 中序下标}` 把定位从 O(n) 降到 O(1)，整体 O(n)；递归时传**左右子树各自的区间下标**。**LC236 LCA（二叉树）**：**后序**思路——递归左右，若**左右均有返回值**说明两侧各找到一个，当前节点即 LCA；若只有一侧有，向上传递那一侧的结果。**LC235 LCA（BST）**：利用有序性，从根开始走——两个值都小于当前就往左、都大于就往右、**一大一小（或等于）则当前就是 LCA**，O(h) 不用递归整棵树。**面试口述：**「树 + 求某个值/性质」→ 递归，先讲清楚函数语义、终止条件和返回值组合方式；「按层 / 最短 / 每层处理」→ BFS + 队列；「有序 ⇒ BST」→ 中序遍历递增 / 利用 BST 走向剪枝。**坑：** LC98 只比父子（**最经典的树题翻车点**）；LC105 构造时区间下标分裂点算错（左子树大小 = `mid - in_left`，用这个长度去切前序）；LC102 忘了记 `size`（一层全弹完就分不清层了）；LC236 只在一侧找到就直接返回而没考虑「两侧都找到」的情形；空节点处理不一致导致 `None` 参与比较；递归爆栈（极端退化成链时）。

### 重点坑

- [ ] **图论**：DFS 深度过大要换 BFS/显式栈防爆栈；LC207 判环认的是「**灰色**（在当前递归栈上）」而不是「访问过」，只有有向图才能用三色标记，无向图必须记 `parent` 或用 Union-Find；岛屿题 `count++` 只在外层发现新陆地时触发，DFS 只负责沉岛；邻接表别写成邻接矩阵；LC133 克隆图要「**先把自己放进 map 再递归邻居**」，否则环上死循环。
- [ ] **链表**：反转链表**先存 `next` 再改指针**；LC19 快指针走 **`n + 1`** 步（停在待删节点前驱）；循环条件 `while fast and fast.next` 两个都要判；凡是可能动头节点就用 **dummy**，最后返回 `dummy.next`；合并链表收尾别忘 `tail.next = l1 or l2`；递归写法注意长度即深度。
- [ ] **树与递归**：LC98 验证 BST **必须传上下界或走中序**，只比父子必错；LC105 中序定位用哈希，切前序要用「左子树节点个数 = `mid - in_left`」；LC102 BFS 每层先记 `size`；LC236 要判「左右都找到」；LC226 不能用中序；递归无法终止时先查 base case 是否漏写。
- [ ] **通用**：Java 比较 `Integer` 用 `.equals()` 而非 `==`（IntegerCache −128~127 之外失效）；`PriorityQueue` 默认小顶堆，要最大堆用 `Comparator.reverseOrder()`（Python 没有大顶堆 → 存负数）；Python `[[0]*n]*n` 各行共享引用、要用列表推导式；递归/DP 先验证 base case 与边界（空输入 / 单节点 / 首元素）；**口头表达要先给 brute-force、再报复杂度、最后讲优化**。

### 建议刷的新题

- [ ] **链表**：Merge K Sorted Lists（LC23，Hard）— 关联已掌握知识点：LC21 Merge Two Sorted Lists（两两合并，已完成）+ LC253 Meeting Rooms II 的**最小堆**用法（已完成）。**核心：** 把「合并 2 条」升级为「合并 k 条」。① **最小堆法（推荐）**：把 k 条链表的**头节点**全部入堆（存 `(val, node)`），每次弹出最小者接到答案尾部，并把它 `next`（若非空）继续入堆；循环直到堆空，共处理 N 个节点，O(N log k) 时间 / O(k) 空间。② **分治两两合并（Divide & Conquer）**：像归并排序那样把 k 条两两配对合并、逐层收敛，同样是 O(N log k)，但**不需要额外堆结构**、常数更小。③ **顺序两两合并**是 O(N·k)，面试时提一句即可但别当正解。**面试口述：**「合并 k 个有序…… / 从 k 个流里取最小」→ 最小堆，或分治归并；先问清 k 与 N 的量级再选型。**坑：** 把整个链表所有节点全塞进堆（O(N) 空间，失去 k 路的意义）；堆里存 `(val, node)`，Python 若只用 `val` 无法取回节点（或需额外索引）；空链表 / k=0 要提前返回 `None`；接尾巴时注意每次都要前移 `tail`。
- [ ] **链表**：Reorder List（LC143，Medium）— 关联已掌握知识点：LC206 反转链表 + LC141 快慢指针找中点（均已完成）。**核心：** 三步走的「拼装题」，每一步都是已学模板——① **快慢指针找中点**（`slow` 走 1、`fast` 走 2，`slow` 停在中点/中点前一个）；② **反转后半段**（用 LC206 的迭代三指针，且要**先把 `slow.next` 切断**成两条独立链表，否则会成环）；③ **交替穿插合并**（`l1`、`l2` 双指针各取一个接上）。整体 O(n) 时间 / O(1) 空间。**面试口述：**「重排成 L0→Ln→L1→Ln-1…」「把链表折成一半交替」→ 找中点 + 反转后半 + 归并穿插。**坑：** 忘记切断 `slow.next = None` → 形成环并 TLE；奇偶长度时中点的选取（`fast.next` 判断会影响奇偶）；穿插时**先保存两条链的 `next` 再改指针**（同 LC206 的教训）。
- [ ] **树**：Subtree of Another Tree（LC572，Easy）— 关联已掌握知识点：LC100 Same Tree（已完成）+ 树的递归遍历骨架。**核心：** 遍历 `root` 的每个节点，对每个节点调用 `isSameTree(node, subRoot)`；任意一处相同即 `True`。递归式：`isSubtree(root, sub) = isSameTree(root, sub) or isSubtree(root.left, sub) or isSubtree(root.right, sub)`，base case `if not root: return False`。复杂度 O(m·n)（m、n 为两棵树节点数）——面试可提「用**序列化 + KMP / 哈希**可优化到 O(m+n)」，但先写清朴素递归。**面试口述：**「判断一棵树包含另一棵完全相同的子树」→ 外层遍历 + 内层 Same Tree。**坑：** 把「子树」理解成「子结构」（子树要求**整棵包含到叶子**，不能只匹配一部分）；`isSameTree` 里空节点的处理（两边都空 True、只有一个空 False）——这与 LC100 完全一致，别重复造轮子。
- [ ] **树**：Kth Smallest Element in a BST（LC230，Medium）— 关联已掌握知识点：LC98 Validate BST 的**中序遍历思想**（已完成）+ LC102 的遍历框架。**核心：** 抓住 **BST 中序遍历 = 严格递增序列** 这一性质——① **递归中序**：全局 `count`/`k` 递减，`count == 0` 时即为答案，可提前 `return` 剪枝；② **迭代中序（推荐，更易剪枝）**：用显式栈一路 `while cur: stack.append(cur); cur = cur.left`，弹栈时 `k -= 1`，减到 0 立即返回，不需要走完整棵树，O(h + k) 时间 / O(h) 空间；③ **Follow-up（频繁增删的第 k 小）**：给每个节点维护**子树节点数**，借 `left.size` 做二分下潜，O(h)。**面试口述：**「BST + 第 k 小 / 第 k 大 / 有序第几个」→ 中序遍历（第 k 大就是**反向中序**：右→根→左）。**坑：** 先中序收集成数组再取下标——能做但没利用 BST 结构、也没剪枝；`k` 是**引用传递/可变**的（Python 嵌套函数里改外层变量要 `nonlocal`，或直接传返回值）；忘记空树的处理。

## 历史复习记录
- 2026-09-15：图论 BFS/DFS、链表、树与递归
- 2026-09-14：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-13：图论 BFS/DFS、链表、树与递归
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 3 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 0 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
**总共 LeetCode 完成：31 道题。掌握基于这些题型的核心思路，可以应对类似面试问题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
