# 🎯 面试复习清单

## 📅 今日复习（2026-09-16）

### 需要回顾

- [ ] **滑动窗口 & 字符串**：Longest Substring Without Repeating Characters（LC3）、Minimum Window Substring（LC76）、Longest Common Prefix（LC14） — **核心：** 滑动窗口的**通用模板**——外层扩 `right` 把新元素吃进窗口，内层 `while` 收缩 `left` 直到窗口重新合法，收缩过程中顺手更新答案。**第一步永远是问自己：「窗口内要维护的不变量是什么？」**——LC3 是「无重复字符」，LC76 是「已覆盖 t 的全部字符（含重复次数）」。**LC3 无重复最长子串**：用 `dict` 存「字符 → 最后出现的下标」，遇到重复就把 `left` 跳到 `max(left, last[c] + 1)`——**`max` 不能省**，否则窗口左边界会倒退、长度算错；每轮 `max_len = max(max_len, right - left + 1)`。**LC76 最小覆盖子串**：用 `need`（t 的词频）+ `missing`（还缺多少个字符）维护合法性；`right` 每吃进一个字符，若它在 `need` 里且消耗后计数仍 ≥ 0 则 `missing--`；`while missing == 0` 时先尝试更新最优解，再吐出 `left` 并 `left++`，若吐出的是 `need` 中的字符且计数掉回需求以下则 `missing++`。**LC14 最长公共前缀**：严格说不算滑动窗口——横向扫描（拿第一个串当基准，逐字符与其余串比对，一旦不匹配立即截断）、纵向扫描（同时比对所有串的第 i 位）、或排序后只比首尾两串（最坏相同的必在中间）。**面试口述：**「最长/最短的连续子数组/子串满足某条件」→ 滑动窗口（且必须能证明**单调性**：扩右不会让条件由真变假、缩左不会由假变真，否则该上 DP/二分）；「多串公共前缀」→ 纵向扫描或排序比首尾。**坑：** 收缩内层用 `if` 而不是 `while`（可能需要连续收缩多次才合法）；LC3 的 `left` 回退 bug；LC76 的 `missing` 增减方向写反、收缩后忘了 `left++`；字符串题先确认「返回长度还是返回子串本身」；Python `for` 循环里改 `left` 时索引语义要理清。**（本组为轮换复习，09-15 复习图论/链表/树，今日轮到滑动窗口/DP/间隔设计，距上次复习本组隔 2 天）**
- [ ] **动态规划（股票系列）**：Best Time to Buy and Sell Stock I/II/III/IV（LC121、122、123、188）、with Cooldown（LC309）、with Transaction Fee（LC714） — **核心：** 六道题本质是**同一个状态机 DP**，只是约束不同。设 `dp[i][k][0/1]` = 第 i 天、还剩 k 次交易额度、当前**不持有 / 持有**股票的最大收益；**base case**：`dp[0][0] = 0`（不持有、没买过）、`dp[0][1] = -prices[0]`（第一天就买），其余用 `-inf` 而不是 0 初始化（0 会把「不可能状态」当成合法解，卖出/买入次数算错）。**转移**：`dp[i][0] = max(dp[i-1][0], dp[i-1][1] + prices[i])`（不动 or 卖出）；`dp[i][1] = max(dp[i-1][1], dp[i-1][0] - prices[i] - fee)`（不动 or 买入，手续费统一在**买入**时扣一次）。**LC121（k=1）**：可退化成贪心——维护「历史最低价」`min_price` 与 `max_profit`，`max_profit = max(max_profit, prices[i] - min_price)`，O(1) 空间最优雅。**LC122（无限次）**：等价于「累加所有上涨段」，`sum(max(0, prices[i] - prices[i-1]))`；或 DP 里买入从 `dp[i-1][0]` 直接转移（不消耗额度）。**LC123（k=2）/ LC188（k 可变）**：退化成「记录第 j 次买入 / 卖出的最优值」——`buy[j] = max(buy[j], sell[j-1] - price)`、`sell[j] = max(sell[j], buy[j] + price)`，j 从 1..k 正向遍历。**LC309（冷却）**：多一个「冻结」状态，买入只能从 `i-2`（冷却日前）转移，即 `dp[i][1] = max(dp[i-1][1], dp[i-2][0] - prices[i])`。**LC714（手续费）**：在买入（或卖出）时统一扣一次 `fee`，**不能两边都扣**。**空间优化**：全部可压成一维滚动变量，但**必须用临时变量存上一轮的值**（先算 sell 再算 buy、却用了已更新的 sell 就错了）。**面试口述：**「最大收益 / 股票买卖 / 有次数限制 / 有冷却」→ 先写出持有/不持有双状态的状态机，再按约束改转移式。**坑：** DP 初始化为 0 而非 `-inf`；LC188 的 k 可能大于 n/2（此时等价于无限次，不特判会 TLE/MLE）；冷却转移 `i-2` 越界；手续费双重扣费；一维优化时变量更新顺序错；`k=1` 用贪心更简单但面试时要能说出 DP 版本。**（本组为轮换复习，09-15 复习图论/链表/树，今日轮到滑动窗口/DP/间隔设计，距上次复习本组隔 2 天）**
- [ ] **间隔 / 设计题（堆）**：Meeting Rooms II（LC253）、Design Hit Counter（LC362）、Design Tic-Tac-Toe（LC348） — **核心：** **间隔题的第一步永远是排序**，而「按什么排」决定解法——**合并区间 / 找重叠**按 `start` 排序；**贪心删区间 / 找最大不重叠集**按 `end` 排序（越早结束越先选）；**求峰值重叠数 / 最少房间数**按 `start` 排序 + 最小堆存 `end`。**LC253 Meeting Rooms II（已掌握）**：按开始时间排序后逐个处理，用**最小堆存已占用会议室的结束时间**——若堆顶 `end <= 当前 start` 说明有房间腾出来了，`heapq.heappop` 复用；否则必须新开。把当前会议 `end` 入堆；**答案 = 堆的峰值大小**（也可处理完直接返回 `len(heap)`，因为堆只增不减地记录了峰值）。另一路是**扫描线**：把每个会议拆成 `(start, +1)` 和 `(end, -1)` 两个事件，按时间排序后累加计数，过程中最大值即答案。**LC362 Design Hit Counter**：`hit(t)` 记一次、`getHits(t)` 问过去 300 秒内（窗口 `(t-300, t]`）的点击数。队列法——`deque` 存时间戳，每次 `getHits` 前把所有 `<= t - 300` 的弹出，返回 `len(queue)`；或用**桶数组**（时间对 300 取模）+ 记录桶的时间戳防止旧数据污染，做到 `hit` O(1)。**LC348 Design Tic-Tac-Toe**：每步不重扫棋盘——维护 `rows[n]`、`cols[n]`、`diag`、`anti_diag` 四个累加器，玩家 1 落子记 `+1`、玩家 2 记 `-1`，任意计数器绝对值达到 `n` 即该玩家获胜，`move` 严格 O(1)。**面试口述：**「给一组区间问重叠/合并/最少资源」→ 先问排序键（start 还是 end）、再决定用堆还是贪心；「设计类题目问每次操作复杂度要求」→ 先问清 O(1) 还是 O(log n)，O(1) 通常要「增量维护」而不是重算。**坑：** LC253 按 `start` 排序、堆里比的是 `end`，两者别搞混；`end == start` 的边界（前一会议结束时刻正好下一会议开始，能否复用房间要看题意，一般用 `<=`）；扫描线要处理「同一时刻的结束与开始谁先」的顺序问题；设计题别每步全量重算（Tic-Tac-Toe 扫整盘是 O(n²)）；Hit Counter 的过期判断用严格 `<= t - 300`；`deque` 比 `list.pop(0)` 快。**（本组为轮换复习，09-15 复习图论/链表/树，今日轮到滑动窗口/DP/间隔设计，距上次复习本组隔 2 天）**

### 重点坑

- [ ] **滑动窗口**：内层收缩要用 `while` 而非 `if`（可能需连续收缩）；LC3 更新 `left` 必须写 `max(left, last[c] + 1)`，直接赋值会让左边界倒退；LC76 的 `missing` 增减方向别写反、收缩后记得 `left++`；先证明条件满足**单调性**再用滑窗，否则应改 DP/二分；看清返回值是「长度」还是「子串」。
- [ ] **动态规划（状态机）**：不可达状态初始化用 `-inf`（不是 0）；LC309 冷却转移 `i-2` 注意越界；手续费只在买入或卖出**扣一次**；一维空间优化时先用临时变量保存上一轮值再更新；股票系列先画出持有/不持有一维 DP 表再讲优化（面试要能口述状态定义与转移）。
- [ ] **间隔 / 设计题（堆）**：排序键搞错是最常见的翻车点（合并/重叠按 `start`，贪心删区间按 `end`）；LC253 排序用 `start`、堆里比 `end`；扫描线注意同时间点 +1/−1 的先后；设计题先问清复杂度要求，O(1) 要增量维护（Tic-Tac-Toe 行/列/对角累加）；Hit Counter 过期判断 `<= t - 300`。
- [ ] **通用**：Java 比较 `Integer` 用 `.equals()` 而非 `==`（IntegerCache −128~127 之外失效）；`PriorityQueue` 默认小顶堆，要最大堆用 `Comparator.reverseOrder()`（Python 没有大顶堆 → 存负数）；Python `[[0]*n]*n` 各行共享引用、要用列表推导式；递归/DP 先验证 base case 与边界（空输入 / 单节点 / 首元素）；**口头表达要先给 brute-force、再报复杂度、最后讲优化**。

### 建议刷的新题

- [ ] **滑动窗口**：Longest Repeating Character Replacement（LC424，Medium）— 关联已掌握知识点：LC3 的「无重复字符」滑动窗口 + LC76 的窗口内计数（均已完成）。**核心：** 窗口合法的判定式是 **`窗口长度 − 窗口内最高频字符的出现次数 <= k`**（把窗口内其余字符全部替换成最高频字符，代价不超过 k 就合法）。用 `count[26]` 维护窗口内各字符频次，`max_freq` **只增不减**（收缩时不用精确回退，不影响答案正确性）；外层 `right` 右移并更新答案，不合法时 `left++` 收缩。整体 O(n) / O(26) 空间。**面试口述：**「最多替换 k 次使某段变成全同字符，求最长」→ 滑窗 + 「长度 − 最高频 ≤ k」判定。**坑：** 判定式写反（是「长度 − 最高频」，不是「最高频 − 长度」）；收缩时去重算 `max_freq`（多余且易写出 bug，只在扩右时更新即可）；`k >= 窗口长度` 时全窗口合法、别漏判；`count` 下标要用 `ord(c) - ord('A')` 之类的稳定映射。
- [ ] **字符串 / 哈希**：Group Anagrams（LC49，Medium）— 关联已掌握知识点：LC76 的 `need` 词频统计 + LC3 的 `dict` 用法（均已完成）。**核心：** 变位词的本质是「**字符计数完全相同**」，所以每串先算出**规范形式（canonical key）**再分组：① **排序后当 key**（`''.join(sorted(s))`），O(n · k log k)；② **26 长度计数元组当 key**（`tuple(count)`，与字符顺序无关），O(n · k)——是面试的加分答法。用 `defaultdict(list)` 聚合，最后返回 `list(map.values())`。**面试口述：**「把互为一组变位词的串归类」→ 找规范 key + 哈希分组，并主动对比两种 key 的复杂度。**坑：** 把 `list(count)` 直接当 key（**list 不可哈希**，必须转 `tuple`）；用 `set` 当 key（丢掉重复计数信息，`aabb` 与 `ab` 会被错误合并）；空串 / 单字符是合法输入；别只算出 key 却忘了聚合成组。
- [ ] **间隔 / 贪心**：Non-overlapping Intervals（LC435，Medium）— 关联已掌握知识点：LC253 Meeting Rooms II 的区间排序 + 最小堆（已完成）。**核心：** **区间贪心的教科书题**——「最少删掉几个区间使剩下的互不重叠」等价于「**最多能选多少个互不重叠的区间**」，答案 = `n − 最多可选数`。**必须按 `end` 升序排序**（不是 `start`！），然后线性扫描：维护 `last_end`（上一个被选中区间的结束时间），若当前区间 `start >= last_end` 则**选中**它并更新 `last_end = cur.end`；否则说明与已选区间冲突 → 删掉当前这个（`count++`，因为按 end 排序，删「结束更晚」的当前区间代价最小）。O(n log n)（排序主导）/ O(1) 额外空间。**面试口述：**「最多能参加几个不冲突的会议 / 最少删几个区间」→ 按**结束时间**排序的贪心，并补上「越早结束 ⇒ 给后面留的时间越多」的交换论证。**坑：** 排序键写成 `start`（最常见的错，会把 `[1,10],[2,3],[4,5]` 选错）；边界写 `start >= last_end` 用 `>=`（端点相接是否算重叠要按题意确认）；返回的是「删除个数 `n − selected`」而不是「选中个数」；**不要和 Merge Intervals（按 `start` 排序）混淆**。
- [ ] **动态规划**：House Robber（LC198，Easy）/ House Robber II（LC213，Medium）— 关联已掌握知识点：股票系列「持有 / 不持有」的双状态 DP 建模（已完成）。**核心：** LC198 的状态定义——`dp[i]` = 偷前 `i` 家的最大金额：`dp[i] = max(dp[i-1], dp[i-2] + nums[i])`（不偷第 i 家 / 偷第 i 家 + 前 `i-2` 家）；用两个滚动变量压到 O(1) 空间。LC213 是**环形版**（首尾相邻），核心套路是**破环成链**：要么不偷第一家（在 `nums[1..n-1]` 上跑一次 LC198），要么不偷最后一家（在 `nums[0..n-2]` 上跑一次），取两者较大值；`n == 1` 时直接返回 `nums[0]`。**面试口述：**「不能偷相邻两家，求最大金额」→ 打家劫舍 DP（选 / 不选）；追问「首尾也相邻」→ 破环为两条线性链取 max。**坑：** base case（`dp[0] = nums[0]`、`dp[1] = max(nums[0], nums[1])`）；空数组要返回 0；一维滚动时先用临时变量存上一轮值（同股票系列的教训）；LC213 忘了 `n == 1` 特判、以及两个子问题**不能同时包含首尾**（否则环上首尾相邻的约束被破坏）。
- [ ] **堆 / 哈希**：Top K Frequent Elements（LC347，Medium）— 关联已掌握知识点：LC253 的**最小堆**用法 + LC76 的词频统计（均已完成）。**核心：** ① 用 `Counter` 统计词频；② **大小为 k 的最小堆**——遍历词频，堆不足 k 个就压入，满了则与堆顶比较，比堆顶大就弹掉堆顶、压入当前，最终堆内即「前 k 高频」；O(n log k) 时间 / O(n) 空间。③ 更优的加分答法：**桶排序**——按**频次**分桶（频次范围 1..n），从高频桶往低频桶取满 k 个，O(n)。**面试口述：**「前 k 频繁 / 前 k 大」→ 最小堆留 k 个（或桶排序），并主动报出复杂度。**坑：** Python `heapq` 默认**小顶堆**，求「前 k 大」正好用小顶堆（反直觉，别建反）；堆里存 `(freq, value)` 元组，比较的是 `freq`；桶排序的桶下标是**频次**而不是元素本身；`k == 去重后元素个数` 时要能正确返回全部。

## 历史复习记录
- 2026-09-16：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-15：图论 BFS/DFS、链表、树与递归
- 2026-09-14：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-13：图论 BFS/DFS、链表、树与递归
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 3 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 0 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
**总共 LeetCode 完成：31 道题。掌握基于这些题型的核心思路，可以应对类似面试问题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
