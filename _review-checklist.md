# 🎯 面试复习清单

## 📅 今日复习（2026-09-25）

> 对齐 `plans/two-week-plan-2026-09-18.md` **Day 8 · DP 与交易系统**：Coding 主菜是 **LC638 Shopping Offers**（记忆化 DFS / 剪枝，key 用 `tuple(needs)`）+ **背包模板复刷 LC322 完全背包 / LC518 组合数**，外加面经 9 的 LC2327 待核对（先看题干，对不上就换 934/1631 加强版）。Design 走 **M9 Booking & Inventory**（`system-design/booking-and-inventory.md`：防超卖三方案对比、座位锁 TTL、支付超时回滚、热点库存分桶）。热手组按轮换切到「**图论 BFS/DFS / 链表 / 树与递归**」——本组上次为 09-23（隔 2 天），上一组的滑动窗口 / 股票 DP / 间隔&设计堆 昨天 09-24 刚过，不重复；两组严格交替，今天本组回归。**今天的目标是「先写出状态定义 + 转移方程 + 遍历顺序，再动手敲代码」**——背包类题内外层写反必错，且小样例往往测不出来。

### 需要回顾

- [ ] **图论 BFS/DFS**：LC200 Number of Islands、LC133 Clone Graph、LC207 Course Schedule、LC323 Number of Connected Components、LC934 Shortest Bridge — **核心（五题一条线）：** ① **LC200 沉岛**——`grid[r][c] = '0'` **就地标记**（省掉 visited 矩阵），BFS 用 `deque` + `popleft()`，四个方向数组 `dirs = ((1,0),(-1,0),(0,1),(0,-1))` 是模板；② **LC133 克隆图**——**一个 dict 同时当 visited 和「旧→新」映射表**，**必须在「入队时」就写进映射**（出队才写会重复建节点），`mapping[node] = Node(node.val)` 后再补 `neighbors`；③ **LC207 / LC323 判环与连通分量**——Kahn 用 `deque` 存入度 0 的点、出队时把邻接点入度 −1，**最后必须校验 `len(order) == n`**（有环时静默返回部分序列，样例照样过 = 最隐蔽的假解）；Union-Find 走 `find` 路径压缩 + 按秩合并，**分量数 = n − 成功 union 的次数**（不是 `n − 边数`）；④ **LC934（面经出现 6 次，Tier S）**——两阶段：DFS 把 island1 标 `-1` 并把它**邻接的水格**入队 → 从队列**多源 BFS** 逐层扩水（标 `-2`），第一次碰到 `== 1` 即返回；**坑在 off-by-one：问的是「要填几格水」不是「扩了几层」，两者差 1**，返回前在白板上把 step 语义说清。**面试口述：** 图题第一句永远是「**有向还是无向？有没有环？边权非负吗？**」——再决定 BFS / DFS / 拓扑 / Dijkstra / 并查集。**复杂度：** O(V+E)（网格题 O(m·n)）；递归 DFS 的空间是栈深，**链状长路径在 HackerRank / CoderPad（默认 ~1000）会爆栈**，预估深度大就换迭代。**坑：** 用 `list.pop(0)` 当队列（O(N²)）而非 `popleft()`；忘记标记起点 / 标记时机晚于递归；LC200 把 `'1'`（字符）与 `1`（数字）搞混；LC323 的 visited 在 934 里两个洋 / 两个岛共用；DFS 递归深度不预估。

- [ ] **链表**：LC206 Reverse Linked List、LC141 Linked List Cycle、LC21 Merge Two Sorted Lists、LC19 Remove Nth Node From End of List — **核心（三件套 + 一条铁律）：** **铁律 = 只要可能动到头结点，先建 `dummy = ListNode(0, head)`**（LC21 合并、LC19 删倒数第 n 都靠它消掉一堆空判断，返回 `dummy.next`）。**LC206 反转**：三指针 `prev = None, cur = head`，循环里**先存 `nxt = cur.next` → 再 `cur.next = prev` → 再 `prev, cur = cur, nxt`**，顺序错就断链。**LC141 快慢指针**：`fast = fast.next.next` 之前**必须判 `while fast and fast.next`**（只判 `fast` 会空指针），相遇即有环；追问「找环入口」→ 相遇后一个指针回头结点、同步走再相遇处即入口（Floyd）。**LC21 合并**：`cur.next = l1 if l1.val <= l2.val else l2`，最后 `cur.next = l1 or l2`（**别写循环把剩余全搬过来，那是 O(n) 增量的笨写法**）。**LC19 删倒数第 n**：Dummy + 快指针先走 n+1 步、再同步走到 `fast` 为 None，此时 `slow.next = slow.next.next`（先用 Dummy 才不用为「删头结点」单独写分支）。**面试口述：** 「**Dummy Node + 快慢指针 + 三指针反转**，链表题 90% 靠这三样」；再报 O(n)/O(1)。**坑：** LC19 忘 Dummy（删头就错）；LC206 忘先存 `nxt`；LC141 只判 `fast`；把「找中点」（`fast` 从 head 走偶/奇数差异）与「找环」混淆。

- [ ] **树与递归**：LC104 Maximum Depth、LC226 Invert Binary Tree、LC100 Same Tree、LC102 Level Order Traversal、LC98 Validate BST、LC105 Construct from Preorder+Inorder、LC235/LC236 LCA — **核心（递归返回值语义是唯一考点）：** ① **LC104 / LC226 / LC100 三件套**——LC226 **Python 可写 `root.left, root.right = invert(root.right), invert(root.left)`**（元组赋值右边先全部求值，天然安全；**Java 必须用 tmp**，这是语言差异考点）；LC100 三种情况写全：**都 None → True、只有一个 None → False、都有 → val 相等且左右都 SameTree**（漏中间分支 = 空指针）。② **LC102 层序**——`while q:` 里**必须 `for _ in range(len(q))` 固定当前层长度**（直接 `while q` 会把下一层混进本层），右视图 / 锯齿 / 层最大值都是这一段的变体。③ **LC98 校验 BST**——**必须往下传开区间 `(low, high)`**，判据 `low < node.val < high`；**只比父子必然错**，反例 `[5,1,4,null,null,3,6]` 白板默写一遍。④ **LC105 建树**——前序第一个是根，`{val: idx}` 哈希把中序查找从 O(n²) 降到 O(n)，**左右子树区间用下标传递而不是切片**（切片是 O(n) 拷贝，递归里会变成 O(n²)）。⑤ **LC235 / LC236 LCA**——BST 用大小关系二选一往下走 O(h)；普通树后序返回「这一侧找到的节点」，**两边都非空即当前节点为 LCA** O(n)。**面试口述：** 树题先问「是 BST 吗？会退化吗（平衡 vs 链）？递归深度多少？」——BST 能把 O(n) 压到 O(h) 是常见加分点。**坑：** LC98 用 `<=` / `>=` 或只比父子；LC102 不固定层长度；LC226 在 Java 里不用 tmp；LC105 用切片导致超时；深度大时忘写迭代版（栈深 ~1000 在非 LeetCode 平台会爆）。

### 重点坑

- [ ] **背包 / DP 的初始化与内外层顺序（今天主菜，最容易整题报废）**：**LC322 Coin Change（求最少硬币数）**——`dp[i] = min(dp[i], dp[i - c] + 1)`，**初始化 `dp[0] = 0`、其余 `inf`（或 `amount + 1` 当哨兵）**，**千万别全初始化成 0**（否则答案恒为 0 或漏算）；返回时 **`dp[amount] == inf` 必须返回 `-1`**。**LC518 组合数**——**外层遍历 coins、内层金额正序**才是「组合」；**内外层调换得到的是「排列」**（`[1,2]` 与 `[2,1]` 算两次），这是 322 / 518 差异的唯一考点，面试必问。**0-1 背包（LC416 等）内层必须倒序**（保证每个物品只用一次），完全背包（322/518/139 Word Break）内层正序。**LC638 Shopping Offers**——记忆化 DFS 的 key 必须是 **`tuple(needs)`**（list 不可哈希），先算「全部单买」的代价当上界，再对每个 offer **逐项判 `needs[i] >= offer[i]`** 后递归；剪枝：**若单买总价已 ≥ 当前最优就直接返回**。**坑：** 内外层写反（组合变排列，小样例测不出）；`dp` 初始化成 0；忘记 `-1` 分支；`for i in range(coin, amount+1)` 写成 `range(amount+1)` 越界；638 的 key 用 list 导致 `TypeError`；`[[0]*n]*n` 初始化共享引用。
- [ ] **934 的 step 差一位 + 多源 BFS（Tier S，面经出现 6 次，最可能被抽到）**：阶段 1 从一个岛 DFS 标 `-1` 并把 **边界的水格** 收集入队；阶段 2 从队列**多源 BFS** 逐层扩水（标 `-2`），第一次碰到另一个岛（`== 1`）时返回桥长。**坑在 off-by-one：问的是「要填几格水」不是「扩了几层」，两者差 1**——`step` 每扩一层 +1，返回时是否 `+1` 必须在白板上讲清。另：两阶段的标记值（`-1` / `-2`）不能混用；把「起点入队时机」放在 DFS 结束时（而不是 DFS 过程中随时入队）才对。
- [ ] **Kahn 拓扑排序必须验证 `len(order) == n`（Tier S 静默假解）**：课程表 / 依赖解析里，只要图有环，Kahn 就会「安静地」返回一个**不完整**的序列——**不校验长度等于没判环**，而且小样例照样通过（最典型的隐藏错误）。入度表用 `Counter` / 数组都行，但 **queue 必须是 `deque`**；面试追问「邻接表方向能不能反转」→ 存 `node → 依赖它的节点` 可以省掉每次查原图（面经 3 的核心争论，准备一段自圆其说的回答）。
- [ ] **Dijkstra 的两个必备细节**：① **堆里存 `(dist, node)`，出堆时 `if d > dist[u]: continue` 懒删除**（否则会重复处理陈旧条目）；② **只在边权非负时用**，有负权直接说 Bellman-Ford。堆元组**必须整体可比较**——`ListNode` 之类不可比较的对象入堆要带下标 `(val, i, node)`（LC23 的三个写法：堆 / 分治归并 / 两两合并，要能对比复杂度 O(N log k) vs O(N log k) vs O(N·k)）。**HackerRank / CoderPad 没有 `sortedcontainers`，堆和桶都手写。**
- [ ] **BST 校验传开区间 + 链表 Dummy Node（今天两个组都用到）**：LC98 **只比父子是经典假解**，反例 `[5,1,4,null,null,3,6]` 必须能脱口而出，正解往下传 `(low, high)` 开区间；链表题**只要可能动头结点就先建 Dummy**（LC19 删倒数第 n、LC21 合并），LC23 的堆元素必须带下标。
- [ ] **递归深度与平台差异（模拟 / 真实机考最容易翻车）**：LeetCode 的 `sys.getrecursionlimit()` = 550000，但 **HackerRank / CoderPad 默认 ~1000**——链状树 / 长路径（LC124、LC297、染色 DFS、网格最长路径）**预估深度 > 1000 就主动写迭代版**并在面试里说出来（加分点）。同时：`[[0] * n] * n` 各行共享引用（必须列表推导式）；哈希 key 不能是 list（`''.join(sorted(s))` / `tuple(...)`）；Java 比较 `Integer` 用 `.equals()`（IntegerCache 只到 127）。
- [ ] **口述顺序与时间盒（今天照旧）**：动笔前固定说清「**不变量 + 返回值语义 + 边界用例**」，顺序永远是 **brute-force → 时空复杂度 → 优化方向**；背包题先说「状态定义 + 转移方程 + 遍历顺序」，让面试官跟上你的推导。卡住超 30min 立刻换思路并**大声说出当前假设**。

### 建议刷的新题

- [ ] **DP / 完全背包（今天 Day 8 主菜，最高优先）**：Coin Change（LC322，Medium）— 关联已掌握知识点：LC121–LC188 股票系列的「**每天一次决策 → 滚动变量压维**」同构思维 + 今天要复刷的 LC518 组合数版。**核心：** `dp[i] = min(dp[i], dp[i - coin] + 1)`，**初始化 `dp[0] = 0`、其余 `inf`**，外层硬币内层金额正序（完全背包）；答案是 `dp[amount]`，为 `inf` 返回 `-1`；O(amount × len(coins)) / O(amount)。**追问（必答）：** 「和 LC518 的差别」→ **内外层顺序决定组合 vs 排列**；「和 0-1 背包的差别」→ **内层倒序 vs 正序**；「能不能贪心」→ **不能，硬币面额非整除时贪心错**（随口举 `coins=[1,3,4], amount=6`）。**坑：** 初始化成全 0；忘 `-1` 分支；内外层写反；把 `dp[i - coin]` 写成 `dp[i - 1]`。
- [ ] **DP / 回溯 → 完全背包（Day 8 第二道）**：Combination Sum（LC39，Medium）— 关联已掌握知识点：LC322 的完全背包 + LC78/LC90 子集模板的「**传 `start` 下标避免重复组合**」。**核心：** 回溯，参数带 `start`，**允许重复选同一元素 = 递归时传 `i` 而不是 `i + 1`**（这是与 LC40 的唯一区别）；`remaining < 0` 直接 return（**先排序，才能 `break` 剪枝**）。**追问：** 与 LC40（每个数只能用一次 + 去重）的差别；与 LC322 的 DP 版对比（回溯求「所有方案」、DP 求「最少个数」，所以一个输出集合一个输出数字）。**坑：** 递归传 `i + 1` 导致丢方案、传 `0` 导致排列爆炸；忘 `path[:]` 深拷贝；忘排序导致剪枝失效。
- [ ] **链表 + 堆（Tier S，直接衔接今天链表复习）**：Merge k Sorted Lists（LC23，Hard）— 关联已掌握知识点：LC21 合并两个有序链表（Dummy + 尾插）+ 堆元组的可比较性 + 面经 21 的「链表太大放不下本地内存」。**核心：** ① **最小堆**：先把每个链表头 `heappush((val, i, node))`（**必须带下标 `i`**，ListNode 不可比较），每次 pop 后把后继入堆，O(N log k) / O(k)；② **分治归并**：两两配对，O(N log k) 且常数更优；③ **朴素两两合并**：O(N·k)，要能说出为什么慢。**追问：** 「链表超过内存怎么办」→ **外部归并排序**（K 路 + 分块落盘 + 多轮合并，讲清缓冲区大小与 I/O 次数）。**坑：** 堆元素不带下标 → `TypeError: '<' not supported between ListNode`；堆空时 pop 未判；分治写错配对步长。
- [ ] **树 / BST 中序（衔接今天树复习）**：Kth Smallest Element in a BST（LC230，Medium）— 关联已掌握知识点：LC98 校验 BST 的**中序遍历有序性** + LC105 的题解结构 + LC235 的「BST 用大小关系 O(h)」。**核心：** ① 中序（**迭代版用栈**，写了递归也要能写迭代——机考平台栈深限制）+ 计数到 k 提前返回；② **顺带记每个子树 size**，`k` 与 `left_size + 1` 比较直接二分下降 O(h)，这是 follow-up「频繁插入删除怎么办」的答案。**追问：** 「如果树被频繁修改」→ 带 size 的节点 / 平衡 BST（红黑树）；「中序遍历的 Morris 版」→ O(1) 空间（加分）。**坑：** 只写递归版（栈深）；计数从 1 开始而不是 0；`k` 减到 0 与返回时机的 off-by-one。
- [ ] **图 / 反向多源 DFS（图论组的新解法，与 934 互补）**：Pacific Atlantic Water Flow（LC417，Medium）— 关联已掌握知识点：LC200 沉岛 + **LC934 的多源 BFS 思路（今天是它的镜像：从边界反向往内陆推）**。**核心：** **不要从每个格子正向搜**（O((mn)²) 必超时），而是**从两个洋的边界分别反向多源 DFS/BFS**，水往「不小于自己高度」的邻居流，最后取两个 visited 的**交集**；O(m·n) / O(m·n)。**追问：** 为什么要反向（把「能不能流到海」转成「海能不能倒灌回来」，把多源合并成两次遍历）；能不能用并查集（按高度排序后 union，等价但更慢）。**坑：** 正向对每格 DFS；**两个洋共用一个 visited**（必须分开两份再取交集）；判据写成 `>` 而非 `>=`（**相等高度可以流过去**）。

## 历史复习记录
- 2026-09-25：图论 BFS/DFS、链表、树与递归
- 2026-09-24：滑动窗口 & 字符串、动态规划（股票系列）、间隔 & 设计题（堆）
- 2026-09-23：树与递归、图论 BFS/DFS、链表
- 2026-09-22：滑动窗口 & 字符串、动态规划（股票系列）、间隔 & 设计题（堆）
- 2026-09-21：图论 BFS/DFS、链表、树与递归
- 2026-09-20：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-17：图论 BFS/DFS、链表、树与递归
- 2026-09-16：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-15：图论 BFS/DFS、链表、树与递归
- 2026-09-14：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-13：图论 BFS/DFS、链表、树与递归
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 4 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 1 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
> ⚠️ 待同步：`blind-75-overview.md` 里 LC323 Number of Connected Components 仍是 `[ ]`，但笔记 `leetcode/union-find/0323-number-of-connected-components.md` 已存在且复习清单一直按「已掌握」复习 → 勾选后应为 **23 / 76**。
> 📌 Day 7 提醒：Tier S 还剩 **347 / 23 / 146 / 297 / 1631 / 68 / 49 / 210** 未落地，其中 297 / 23 已在 09-23 建议过仍未做；**297 属于 Day 6 已排期但未产出代码**，落在「Day 1–6 补漏」范围内，今天模拟后优先补。

**总共 LeetCode 完成：33 道题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
