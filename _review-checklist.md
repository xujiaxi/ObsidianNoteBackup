# 🎯 面试复习清单

## 📅 今日复习（2026-09-11）

### 需要回顾

- [ ] **图论 BFS/DFS**：Clone Graph（LC133）、Number of Islands（LC200）、Course Schedule（LC207） — **核心：** LC133 深拷贝靠 `HashMap<原节点, 克隆节点>` 当**备忘录**，既建新旧映射又防环——流程是「查表 → 创建新节点并**先入表** → 再递归克隆邻居」；LC200 用**沉岛算法 (Sinking)**：遍历网格遇 `'1'` 就 `count++`，一次 DFS/BFS 把整座岛淹没成 `'0'`，**求连通分量用 DFS（更简洁）、求最短路径用 BFS**；LC207 判断能否修完课 = 有向图**环检测**，用**三色标记法**：`0` 未访问、`1` 访问中（再次碰到 = 有环）、`2` 已完成（再次碰到 = 剪枝直接跳过），也可用 Kahn's Algorithm（BFS 入度拓扑排序）替代。**面试口述：**「拷贝/遍历图结构」→ 先想 HashMap 备忘录防环；「二维网格连通块 / 岛屿 / 区域填充」→ 沉岛 DFS；「先修课 / 编译依赖 / 任务是否可完成」→ 拓扑排序或三色环检测。**坑：** LC133 **必须先入表再递归**，顺序反了遇环 A→B→A 永远查不到表 → StackOverflow；DSF 邻居列表含 null 时要靠 `if (node == null)` 安全返回；LC200 的 DFS 最坏递归深度 = M×N（全陆地），大网格要换 BFS/显式栈防爆栈；LC207 三色里状态 2 只做剪枝，别把它和状态 1 混用否则漏判环。**（本组为轮换复习，上次 09-08 复习滑动窗口/股票 DP/间隔设计，今日轮到图论/链表/树，距上次复习本组隔 3 天）**
- [ ] **链表**：Reverse a Linked List（LC206）、Detect Cycle（LC141）、Merge Two Sorted Lists（LC21）、Remove Nth Node From End（LC19） — **核心：** 四题共用「**Dummy Node + 快慢指针**」两大武器。LC206 反转用**三指针推移** `prev/curr/next_temp`：先存下一站 `next_temp = curr.next`，再反转 `curr.next = prev`，最后整体前移 `prev, curr = curr, next_temp`，返回 `prev`；递归版核心是 `head.next.next = head; head.next = None`。LC141 用 **Floyd 判圈**：快指针每次走 2 步、慢指针 1 步，有环必相遇，`while (fast && fast.next)` 保证 `fast.next.next` 安全。LC21 拉链式双路归并：比较两链当前节点，小的接上，收尾 `curr.next = l1 or l2`。LC19 一次遍历删倒数第 N 个：dummy 起手，快指针先走 **N+1 步**，再快慢同走，fast 到终点时 slow 正好停在**待删节点的前驱**，执行 `slow.next = slow.next.next`。**面试口述：**「反转 / 找中点 / 判环 / 找入环点 / 删倒数第 N 个」→ 全是快慢指针或三指针变体；凡涉及删头节点或可能改头指针的场景，先挂 dummy 省掉边界特判。**坑：** 快慢指针的 `while` 条件**必须**同时判 `fast != null && fast.next != null`，只判一个会 NPE；LC206 循环结束返回的是 `prev` 不是 `curr`；LC19 走 N+1 步（不是 N 步），否则 slow 停在待删节点本身、删错人；LC21 收尾别忘把剩下的整条链接上。**（本组为轮换复习，上次 09-08 复习滑动窗口/股票 DP/间隔设计，今日轮到图论/链表/树，距上次复习本组隔 3 天）**
- [ ] **树与递归**：Maximum Depth（LC104）、Same Tree（LC100）、Invert Binary Tree（LC226）、Level Order Traversal（LC102）、Validate BST（LC98）、LCA of BST（LC235）、LCA of Binary Tree（LC236）、Construct from Preorder/Inorder（LC105） — **核心：** 树的通法是**递归分解 + 明确每层返回值**。LC104 `1 + max(左, 右)`（BFS 数层数也行）；LC100 同步 DFS、**Fail-Fast**（空值/值不同任一不匹配立刻 False）；LC226 交换左右子再递归；LC102 层序 BFS 用 `len(queue)` 快照分层。LC98 两条路：**上下界递归**（下传 `(min, max)` 区间）或**中序遍历严格递增**（BST 中序即升序，是 Tree 题的万能钥匙）。LC235 借 BST 单调性「指路」：p、q 同小往左、同大往右、**分叉点即 LCA**（`(root.val-p.val)*(root.val-q.val) <= 0`），迭代 O(1) 空间。LC236 普通二叉树用**后序汇总**：`if (left && right) return root; return left or right`。LC105 由前序首元素定根 + 中序分割左右子树（哈希表记录中序索引，避免每次 `index()`）。**面试口述：**「求深度 / 翻转 / 判断结构相同」→ 直接递归左右子树；「有效 BST」→ 先想中序是否严格递增；「找公共祖先」→ BST 用值大小分叉，普通树用后序汇总。**坑：** LC98 **只比较父子会错**（如 `[5,1,4,null,null,3,6]`）——必须传上下界或用中序；判空用 `min_val is not None` 而非 `if min_val:`（节点值 0 是 falsy 会被误判）；LC102 分层必须**先取 `len(queue)` 快照**，边遍历边 append 会把下一层混进来；LC236 注意 `if (left && right)` 与 `return left or right` 的先后顺序；LC105 递归**必须先建左子树再建右子树**，preorder 指针才能对得上。**（本组为轮换复习，上次 09-08 复习滑动窗口/股票 DP/间隔设计，今日轮到图论/链表/树，距上次复习本组隔 3 天）**

### 重点坑

- [ ] **图论 DFS/BFS**：LC133 Clone Graph **先入表再递归**（顺序反了遇环死循环 StackOverflow）；三色标记里值为 2 的「已完成」只用于剪枝，只用 0/1 虽能判环但重复遍历、性能退化；LC200 沉岛前先判越界和水，深递归在大网格上会爆栈（改 BFS/显式栈）；图题别忘孤立节点（LC207 无先修关系的课也要能判 true）。
- [ ] **链表**：快慢指针 `while (fast != null && fast.next != null)` 两个条件缺一不可；LC19 快指针走 **N+1** 步而非 N 步；LC206 返回 `prev`；凡可能删/改头节点先挂 **dummy**，`return dummy.next` 而不是 `dummy` 或 `head`；多指针整体前移顺序写错（先移 curr 再存 next_temp）会丢链。
- [ ] **树与递归**：LC98 只比较父子会漏判（必须上下界/中序）；`if min_val:` 误判节点值 0；LC102 分层忘取 `len(queue)` 快照会串层；LC105 preorder 指针 + 先左后右的顺序不能乱；递归函数**先想清楚每层「返回什么」**（深度？节点？布尔？），返回值定义不清是树题最大翻车点。
- [ ] **通用**：Java 比较 `Integer` 用 `.equals()` 而非 `==`（IntegerCache −128~127 之外失效）；`PriorityQueue` 默认**小顶堆**，要最大堆记得 `Comparator.reverseOrder()`；Python 二维数组用列表推导式 `[[0]*n for _ in range(n)]`，`[[0]*n]*n` 各行共享同一引用；递归/DP 先验证 base case（空树 / 空输入 / 第 0 天）。

### 建议刷的新题

- [ ] **树 / BST**：Kth Smallest Element in a BST（LC230，Medium）— 关联已掌握知识点：LC98 Validate BST 的「BST 中序遍历严格递增」（已完成）。**核心：** 中序遍历 + 计数器，遍历到第 k 个直接返回；最优写法是**迭代版中序**（显式栈），到第 k 个就 `return`，不用遍历完整棵树，空间 O(H)。**面试口述：**「BST 里找第 k 小 / 找第 k 大」→ 中序（升序）或反中序（降序）走到第 k 个即停。**坑：** 别用 `inorder[]` 存完整个数组再取 `[k-1]`（能过但浪费空间、面试会追问）；计数器从 1 开始数、k 从 1 起；递归版要处理「早期返回」的传播，迭代版更直观。
- [ ] **树**：Subtree of Another Tree（LC572，Easy）— 关联已掌握知识点：LC100 Same Tree 的同步 DFS + Fail-Fast（已完成）。**核心：** 两层递归——外层遍历 `root` 每个节点当候选根，内层用现成的 `isSameTree(node, subRoot)` 判断是否全等，任一命中即 true；也可把子树序列化成字符串（含 null 占位）后做子串匹配。**面试口述：**「A 是否包含结构/值完全相同的子树」→ 遍历每个起点 + isSameTree。**坑：** 外层命中要**先判 isSameTree 再递归左右**，顺序反了会漏；序列化法必须给空节点加占位符（如 `#,`），否则 `[1,2]` 和 `[1,2,null,3]` 会误判；递归基：`subRoot` 为 null 时 true、`root` 为 null 时 false。
- [ ] **树**：Binary Tree Maximum Path Sum（LC124，Hard）— 关联已掌握知识点：LC236 后序自底向上汇总 + LC104 递归返回深度（已完成）。**核心：** 后序遍历，每层返回**「以当前节点为端点、往下走的单边最大贡献」** `max(0, max(left, right) + node.val)`（负贡献直接舍弃取 0）；同时用全局 `ans` 记录**「左贡献 + 根 + 右贡献」**这条「可拐弯」的最大路径，最终答案是 ans 而不是返回值。**面试口述：**「路径和」类题先问清「能否拐弯、是否必须经过根、能否为空」——路径可拐弯 → 用全局量记拐弯值，递归只回传单边。**坑：** 返回值（单边）和全局答案（双边）**必须分开**，混用是这题最大坑；负节点时 `max(0, ...)` 才能舍弃；初始 `ans = -inf`（不能是 0，否则全负树答案错）；单节点也要能返回自身值。
- [ ] **链表**：Reorder List（LC143，Medium）— 关联已掌握知识点：LC206 反转链表 + LC141/LC19 快慢指针找中点（已完成）。**核心：** 三步走——① 快慢指针找**中点**（slow 停在中间）；② **反转后半段**（套 LC206 模板）；③ **交替合并**前半与反转后的后半（两条链交替接）。空间 O(1)，是最经典的「链表综合题」。**面试口述：**「重排链表 / 交叉合并」→ 拆成「找中点 + 反转 + 归并」三个已会的小模块拼装。**坑：** 找中点后要**断开** `slow.next = null`，否则后半的尾指回前半形成环；奇偶长度交替合并的终止条件不同（用两条链非空驱动）；合并时先存 `next` 再改指针，防止丢链。
- [ ] **链表 / 堆**：Merge K Sorted Lists（LC23，Hard）— 关联已掌握知识点：LC21 双路归并 + LC253 最小堆用法（已完成）。**核心：** 两个主流解法——① **最小堆**：把 k 条链头入堆（`(val, idx)`），每次弹最小接到结果尾部、再把该链下一个入堆，O(N log k)；② **分治两两归并**（divide & conquer）：`mergeTwoLists` 的推广，同复杂度但常数更小、不用堆。**面试口述：**「合并 k 个有序序列」→ 先答堆（O(N log k)、实现直白），再答分治（更省、面试官更爱）。**坑：** Java `PriorityQueue` 默认小顶堆（这里正合用，别加 reverseOrder）；堆里存节点时要自定义比较器比较 `val`（`ListNode` 不比 Comparable）；k=0 返回 null、k=1 直接返回原链；分治法递归 `mid = (l+r)/2` 别死循环。

## 历史复习记录
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 3 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 0 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
**总共 LeetCode 完成：31 道题。掌握基于这些题型的核心思路，可以应对类似面试问题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
