# 🎯 面试复习清单

## 📅 今日复习（2026-09-24）

> 对齐 `plans/two-week-plan-2026-09-18.md` **Day 7 · 阶段模拟 + Leaderboard**：Coding 是 **45min × 2 阶段模拟**（建议盲抽 [LC934 Shortest Bridge] + [LC347 Top K Frequent]，或 [LC1631 Path With Minimum Effort] + [LC146 LRU Cache]），全程计时、不查资料；外加 **Day 1–6 补漏**（哪道没写完 / 没口述顺就重做）。Design 走 **M5 Leaderboard**（`system-design/realtime-leaderboard.md`：跳表 / Redis ZSet 深挖、10M DAU 写合并、分片榜单合并、快照重算、作弊检测）。热手组按轮换切到「**滑动窗口 & 字符串 / 股票 DP / 间隔 & 设计题（堆）**」——本组上次为 09-22（隔 2 天），上一组的图论 / 链表 / 树昨天 09-23 刚过，不重复；两组严格交替，今天本组回归。**今天是模拟日，复习目标不是再学新知识，而是「60 秒口述 + 关键函数默写」**。

### 需要回顾

- [ ] **滑动窗口 & 字符串**：LC3 Longest Substring Without Repeating Characters、LC76 Minimum Window Substring、LC14 Longest Common Prefix — **核心：** 万能模板 = **外层 `while` 扩右指针、内层 `while` 收左指针**，两字真言——**找最长（LC3）：窗口合法就继续走、非法时左指针跟上；找最短（LC76）：窗口不满足就拼命扩、满足时试探收缩**。**LC3**：`last = {}` 存字符最后出现的下标，收缩写成 **`l = max(l, last[c] + 1)`**（**必须带 `max`**，否则 `l` 回退）；`set` 版则 `while c in window: window.remove(s[l]); l += 1`——**一个一个删，不要跳**。答案统一 `r - l + 1`，**不要另设计数变量**（与指针不同步必错）。**LC76**：`need` 记需求、`window` 记当前窗口，用 **`valid` 记录「已满足的字符种类数」**，`valid == len(need)` 即覆盖——**别每轮全量比对两张表（O(K·N)）**；收缩顺序固定为 **「先记录答案 → 再移除 `s[l]` → 再 `l += 1`」**（反了会漏掉窗口右端的合法解）；只有 `need[d] > 0` 时才动 `valid`。**LC14**：纵向扫描——以 `strs[0]` 为基准逐列比，不等或某串到头就截断；**先判空列表**。**复杂度：** 滑窗 O(n)（每字符最多进出各一次）、O(K) 空间；LC14 最坏 O(n·m)。**面试口述（今天只练这个）：** 「滑窗的本质是**单调性**——窗口一旦非法，任何包含它的更大窗口也非法，所以左指针只前进不回退」→ 报「LC3 求最长 / LC76 求最短 / LC424 求最长且带替换预算」是同模板三变体 → 报复杂度。**坑：** LC3 忘 `max`（最经典假解，小样例测不出）；LC76 先移除再记答案、用 `if` 收缩（一次不一定收够）、全量比对两张计数表；**Java 里 `Integer` 计数用 `==` 比较**（IntegerCache 只覆盖 −128~127）必须 `.equals()`；空串 / 单字符 / t 比 s 长没测。

- [ ] **动态规划（股票系列）**：LC121 单笔、LC122 无限次、LC123 两笔、LC188 k 笔、LC309 冷冻期、LC714 手续费 — **核心：** 全系列共用一个**状态机**——`hold`（持有中的最大收益）与 `cash`（不持有的最大收益），初始化 **`hold = -prices[0]`、`cash = 0`**；**买入 / 卖出两个动作的先后顺序决定了要不要快照**。**LC121 单笔**：`hold = max(hold, -p)`（只能买一次，所以是 `-p` 不是 `cash - p`），`cash = max(cash, hold + p)`，O(n)/O(1)。**LC122 无限次**：**必须 `prev_cash` 快照**——更新 `cash` 后要用**旧的** `cash` 去更新 `hold`，否则同一天「买入→卖出→再买入」= 无限套利；等价贪心是 `sum(max(0, p[i] - p[i-1]))`（每个上涨段吃满）。**LC123 两笔**：`buy1/sell1/buy2/sell2` 四状态顺序转移，**`sell1` 更新完才能算 `buy2`**。**LC188 k 笔**：泛化成长度 k 的 `buy[]`/`sell[]`，O(nk)；**关键优化：`k >= n // 2` 时次数约束失效，直接退化到 LC122 贪心 O(n)**（不特判 k 大时 TLE）。**LC309 冷冻期**：卖出后隔一天才能买 → `hold` 用**前天**的 `cash[i-2]`（用昨天 = 没冷冻）。**LC714 手续费**：**只在卖出时扣一次** `cash = max(cash, hold + p - fee)`（买入也扣会来回重复扣）。**面试口述：** 讲成「**一个状态机 + 五种约束（次数 / 冷冻 / 手续费）**」，先写二维 `dp[i][0/1]` 再压到 O(1)——面试官更认这条推导路径；主动补「所有股票题都不需要额外空间，滚动变量即可」。**坑：** LC122 忘快照变量；LC309 用昨天的 `cash`；LC714 手续费扣两次 / 扣在买入；LC188 忘 `k >= n//2` 特判或 `k == 0` 返回 0；`hold` 初始化用 `-inf` 在 Java 里溢出；**做单笔题时把 LC121 的 `-p` 顺手写成 `cash - p`**（语义变成可无限次买入）。

- [ ] **间隔 & 设计题（堆）**：LC253 Meeting Rooms II、LC362 Design Hit Counter、LC348 Design Tic-Tac-Toe — **核心：** **LC253** 等价于「同一时刻最多多少个会议重叠」——① **双指针时间线扫描**：`starts` / `ends` 各自排序，`starts[s] < ends[e]` 就 `used += 1`（开新房），否则 `e += 1`（释放），全程取 `used` 峰值，O(n log n)；**端点相接（`end == start`）不算冲突，判据必须用 `<`**；② **最小堆存结束时间**：每个 interval 先 `while heap and heap[0] <= start: heappop`（腾空房间）再 `heappush(end)`，`len(heap)` 峰值即答案，O(n log n)、O(n)；③ 第三解 **差分 / sweep line**（start 记 +1、end 记 −1，前缀和取峰值）——**三解都要能讲，面试官必问「哪个更省空间 / 时间线扫描为什么不用堆」**。**LC362 Hit Counter**：`deque` 存 `(timestamp, count)`，`getHits(t)` 先 `while q and q[0][0] <= t - 300: popleft()` 再求和；**窗口是 `[t-299, t]`，过期判据是 `<= t - 300`**（差一位就错）；进阶是**桶数组版**（300 个桶 + 环形下标）做到 O(1) 摊还、内存 O(300)。**LC348 Tic-Tac-Toe**：**绝不每次扫全盘（O(n²)）**——维护 `rows[n] / cols[n] / diag / anti_diag`，**正负计分法**（player1 记 +1、player2 记 −1），`abs(v) == n` 即获胜，O(1)/步；副对角线判据 **`row + col == n - 1`**。**设计题通用四问（先问再写）：** ① 哪个操作是热点（读多还是写多）？② 并发吗？③ 数据规模与时间窗口？④ 允许近似 / 过期数据吗？——**问完再谈数据结构，最后报复杂度**。**面试口述：** 设计题一律「**先澄清需求 → 再给 API → 再选数据结构 → 最后报复杂度与扩展点**」，别一上来写代码。**坑：** LC253 把端点相接算成冲突（`>` / `>=` 写反）；堆里忘先清理已结束的会议、或写成最大堆；LC362 过期判据写成 `< t - 300` 或 `<= t - 299`；LC348 每次重扫全盘、用 `sum` 而非 ±1 计分导致两个玩家误判；设计题不问需求就动手、`[[0]*n]*n` 初始化共享引用。

### 重点坑

- [ ] **模拟考时间盒（今天最容易翻车的一点，不是知识而是节奏）**：45min × 2 的模拟里，**前 5 分钟必须把「思路 + 复杂度 + 边界」口述完再动手**（LeetCode 上习惯的先写后想在这里会直接超时）。三个时间节点：5min 出 brute-force + 复杂度；15min 出优化解并开始写；30min 必须开始跑样例。**卡住超 30min 立刻换思路并大声说出「我现在的假设是什么」**——面试官要听的是推理过程，不是沉默。建议盲抽 **934 + 347**（图 DFS + 堆）或 **1631 + 146**（Dijkstra + 设计），模拟完当场写复盘进本文件。
- [ ] **934 的 step 差一位 + 多源 BFS（Tier S，出现 6 次，最可能被抽到）**：两阶段——阶段 1 从一个岛 DFS 标 `-1` 并收集边界点入队；阶段 2 从队列**多源 BFS** 逐层扩水（标 `-2`），第一次碰到另一个岛（`== 1`）时返回桥长。**坑在 off-by-one：问的是「要填几格水」不是「扩了几层」，两者差 1**——`step` 每扩一层 +1，返回时是否 +1 要在白板上说清。另：两阶段的标记值（`-1` / `-2`）不能混用；`'1'`（字符）与 `1`（数字）不能搞混。
- [ ] **146 LRU 的哨兵与「先摘再插」（Tier S，模拟盲抽高危题）**：`HashMap + 双向链表`，**必须建哨兵 `head`/`tail`**（省掉一堆空判断），`get` 命中后要 `moveToHead`，`put` 已存在时**先 `remove` 旧节点再插头部**（只改 value 会导致访问序错乱）；超容量时删 `tail.prev` 并**同时从 map 里 `del`**（只删链表 = 内存泄漏，面试必查）。追问：线程安全版（锁粒度 / `ConcurrentHashMap` + 分段）、`LinkedHashMap(accessOrder=true)` 的等价写法；**HackerRank / CoderPad 没有 `sortedcontainers`**，堆 / 桶要手写。
- [ ] **滑窗左指针的单调性**：LC3 收缩必须写 **`l = max(l, last[c] + 1)`**——直接 `l = last[c] + 1` 会让 `l` **回退**，把已排除的重复字符重新算进窗口，答案偏大（最经典的假解，小样例常测不出来）。LC76 顺序固定「**先记录答案 → 再移除 `s[l]` → 再 `l += 1`**」，用 `while` 不用 `if`。
- [ ] **股票 DP 的状态转移顺序 & 快照变量**：无限次交易版里，同一轮 `cash` 更新后直接拿去更新 `hold` = 同一天买入又卖出 = **无限套利**；必须 `prev_cash` 快照（或固定「先 `hold` 用旧 `cash`、再 `cash` 用新 `hold`」的顺序）。LC309 用**前天**的 `cash`；LC714 手续费**只在卖出扣一次**；LC188 忘 `k >= n // 2` 会 TLE。**写之前先在纸上列二维转移，压缩维度时逐行核对依赖项。**
- [ ] **间隔题的边界与判据（`>=` vs `>`）**：LC253 里 **`end == start` 不算冲突**，判据是 `starts[s] < ends[e]` 才开新房；LC56 Merge Intervals 合并时**必须 `max(res[-1][1], end)`**（直接覆盖会把已被包含的大区间改小，`[[1,10],[2,3]]` 必错）；LC435 经典贪心要**按 end 排序**（按 start 排是错解）。凡间隔题先在纸上画**端点相接**和**完全包含**两种边界。
- [ ] **堆的用法（今天两题都可能用到）**：`heapq` 默认**小顶堆**，要最大堆就存负数；堆里的元组**必须可比较**（Top-K 用 `(freq, num)`、Dijkstra 用 `(dist, node)`，ListNode 入堆要带下标 `(val, i, node)`）；Top-K 用**固定大小 k 的堆 + `heappushpop`**（别 push 完再 pop）；Dijkstra 出堆时**必须 `if d > dist[u]: continue` 懒删除**，且只在边权非负时成立。
- [ ] **通用（模拟日必查）**：**递归深度平台差异**——LeetCode `sys.getrecursionlimit()` = 550000，但 **HackerRank / CoderPad 默认 ~1000**，链状树 / 长路径（LC124、LC297、染色 DFS）预估深度 > 1000 就主动写迭代版；**`[[0] * n] * n` 各行共享引用**（必须列表推导式）；哈希 key 不能是 list（`''.join(sorted(s))` / `tuple(...)`）；Java 比较 `Integer` 用 `.equals()`。**表达顺序固定：brute-force → 时空复杂度 → 优化方向**，动手前先说清「不变量 + 返回值语义」。

### 建议刷的新题

- [ ] **堆 / Top-K（Tier S，今天模拟极可能盲抽到）**：Top K Frequent Elements（LC347，Medium）— 关联已掌握知识点：LC253 的最小堆 + LC1 的哈希计数 + Day 2「堆与 Top K」计划（至今未落地）。**核心：** ① `Counter` 计数 + **固定大小 k 的小顶堆**（`heappushpop`，O(n log k)）；② **桶排序**（按频次分桶，频次上界 n，O(n) 时间）；③ quickselect 平均 O(n)、最坏 O(n²)。**面试口述（面经 25 明确考点）：** 讲完三解主动答「**变种里为什么只能用 heap**」——变种是**流式 / 连续到达**的数据，桶和 quickselect 都要全量重算，堆能**增量维护 Top-K**（并追问：k 很小时堆最优，k ≈ n 时桶 / 排序更划算）。**坑：** 堆元组顺序写成 `(num, freq)`（必须先比频次）；用 `sorted(cnt.items(), key=lambda x: -x[1])[:k]` 糊弄（O(n log n)，必被追问为什么不用堆）；`heapq.nlargest` 用得出却说不清复杂度。
- [ ] **DP / 状态机**：House Robber（LC198，Medium）+ House Robber II（LC213，Medium）— 关联已掌握知识点：**股票系列的两变量滚动状态机**（每天做一次「选 / 不选」的决策，正是 `hold`/`cash` 的同构结构）+ 环形问题的「**拆环成两段线性**」套路。**核心：** LC198 转移 `dp[i] = max(dp[i-1], dp[i-2] + nums[i])`，用 `prev2 / prev1` 两个变量滚到 O(1) 空间；**LC213 环形**——首尾不能同时偷，拆成「偷 `[0, n-2]`」与「偷 `[1, n-1]`」两次线性取 max，并特判 `n == 1`。**追问：** 为什么拆环后取 max 不漏解（首尾必有一个不偷）；若变成树形（LC337 House Robber III）怎么改（后序 DFS 返回 `(偷, 不偷)` 二元组）。**坑：** 空数组返回 0 / `n == 1` 直接返回 `nums[0]` 的分支没写在最前；`dp[i-2]` 下标越界（用滚动变量最稳）；把 `prev1` 更新顺序写反导致用了当天的值。
- [ ] **DP / 子数组**：Maximum Product Subarray（LC152，Medium）— 关联已掌握知识点：LC53 Maximum Subarray（Kadane，已在 `array/0053`）+ 本次的滚动变量技巧。**核心：** 乘法里**负负得正**，所以必须**同时维护 `cur_max` 与 `cur_min`**，遇到 `x < 0` **先交换两者**再更新（这就是全部考点），滚动 O(n)/O(1)。**追问：** 和 LC53 的差别在哪（加法只需一个状态，乘法要两个，因为符号翻转）→ 什么时候必须重开子数组（乘到 0 时公式天然处理：`max(x, x*cur_max, x*cur_min)`）；要求返回**子数组本身**怎么改（额外记录起止下标，遇 0 或符号翻转时更新起点）。**坑：** 只维护一个 max（`[-2,3,-4]` 这类必错）；忘交换；用 `0` 作初始值而不是 `nums[0]`；把「最大值」和「乘积」变量搞混。
- [ ] **间隔（与今天复习组直接衔接）**：Merge Intervals（LC56，Medium）— 关联已掌握知识点：LC253 的「按 start 排序 + 一次扫线」思维。**核心：** 先按 `start` 升序排序（**排序是本题成立的前提，主动说出来**），再一次遍历：`cur.start <= res[-1][1]` 则合并（**`res[-1][1] = max(res[-1][1], cur.end)`，必须取 max**——否则 `[[1,10],[2,3]]` 会把区间改小），否则追加新区间；O(n log n) / O(n)。**追问：** 输入已排序怎么办（省排序，O(n)）；流式到达怎么处理（有界缓冲 + 与尾部合并，或落库后按 start 建索引查重叠）；求「合并后总覆盖长度」（累加 `end - start`）。**坑：** 忘排序；用 `cur.end` 覆盖而非取 max；判据写成 `<` 导致端点相接未合并（本题**要**合并，用 `<=`）；空输入返回 `[]`。
- [ ] **堆 / 流式 Top-K（Tier A）**：Kth Largest Element in a Stream（LC703，Medium）— 关联已掌握知识点：LC215 Kth Largest（Tier C 复刷）+ LC347 的固定大小堆。**核心：** 维护**大小恰为 k 的小顶堆**，`add` 时 `heappushpop(k_sizeheap, val)` 保持 size = k，**堆顶即第 k 大**；初始化 O(n log k)、每次 `add` O(log k)。**追问：** 面经 10 的比赛变种——**答案是「每一轮第 k 大的分数中的最大值」，不是最后一轮的值**（要维护一个全局 max 或每轮比较后记录）；若 k 很大 / 内存受限怎么办（桶 + 时间窗口，或近似 Top-K 的 Count-Min Sketch）。**坑：** 用大顶堆（想 `nlargest` 反而要维护 k 个而非全部）；初始化时把全部元素塞进堆再 pop（O(n log n) 也就算了，还容易忘设 size = k）；`add` 里先 push 再 pop 导致返回值取错；把「堆顶」当成第 k 小（小顶堆存的是**前 k 大中的最小者**）。

## 历史复习记录
- 2026-09-24：滑动窗口 & 字符串、动态规划（股票系列）、间隔 & 设计题（堆）
- 2026-09-23：树与递归、图论 BFS/DFS、链表
- 2026-09-22：滑动窗口 & 字符串、动态规划（股票系列）、间隔 & 设计题（堆）
- 2026-09-21：图论 BFS/DFS、链表、树与递归
- 2026-09-20：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-17：图论 BFS/DFS、链表、树与递归
- 2026-09-16：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-15：图论 BFS/DFS、链表、树与递归
- 2026-09-14：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-13：图论 BFS/DFS、链表、树与递归
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 4 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 1 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
> ⚠️ 待同步：`blind-75-overview.md` 里 LC323 Number of Connected Components 仍是 `[ ]`，但笔记 `leetcode/union-find/0323-number-of-connected-components.md` 已存在且复习清单一直按「已掌握」复习 → 勾选后应为 **23 / 76**。
> 📌 Day 7 提醒：Tier S 还剩 **347 / 23 / 146 / 297 / 1631 / 68 / 49 / 210** 未落地，其中 297 / 23 已在 09-23 建议过仍未做；**297 属于 Day 6 已排期但未产出代码**，落在「Day 1–6 补漏」范围内，今天模拟后优先补。

**总共 LeetCode 完成：33 道题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
