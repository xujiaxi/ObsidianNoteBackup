# 🎯 面试复习清单

## 📅 今日复习（2026-09-22）

> 对齐 `plans/two-week-plan-2026-09-18.md` **Day 5 · 图与最短路**：[LC210 Course Schedule II] + [LC1631 Path With Minimum Effort] + [LC743 Network Delay Time]；Design 走 **M3 Feed / UGC**（`system-design/feed-and-review.md`，全英文产出）。热手组按轮换切到「滑动窗口 & 字符串 / 股票 DP / 间隔 & 设计题（堆）」——本组距上次复习 2 天（09-20），上一组的图论 / 链表 / 树昨天（09-21）刚过，不重复；三组交替，本组今天回归。

### 需要回顾

- [ ] **滑动窗口 & 字符串**：Longest Substring Without Repeating Characters（LC3）、Minimum Window Substring（LC76）、Longest Common Prefix（LC14） — **核心：** 万能模板 = **外层 `while` 扩右指针、内层 `while` 收左指针**，两字真言——**找最长（LC3）：窗口合法就继续走，非法时左指针跟上；找最短（LC76）：窗口不满足就拼命走，满足时试探收缩**。**LC3**：`last = {}` 存字符最后出现的下标，收缩写成 **`l = max(l, last[c] + 1)`**（必须带 `max`）；用 `set` 版则是 `while c in window: window.remove(s[l]); l += 1`——**一个字符一个字符地删，不要跳**。答案统一用 `r - l + 1`，**不要另设计数变量**（与指针不同步必错）。**LC76**：`need` 记需求、`window` 记当前窗口，用 **`valid` 记录「已满足的字符种类数」**，`valid == len(need)` 即覆盖——**不要每轮全量比较两个计数表（O(K·N)）**；收缩循环的顺序固定为 **「先记录答案 → 再移除 `s[l]` → 再 `l += 1`」**（顺序反了会漏掉窗口右端的合法解）；`need[d] > 0` 时才动 `valid`（多出来的冗余字符不影响 valid）。**LC14**：纵向扫描——以 `strs[0]` 为基准逐列比，遇到不等或某串已到头就截断；**先判空列表**。**复杂度：** 滑窗 O(n) 时间（每个字符最多进出各一次）、O(K) 空间（K = 字符集，`int[128]` 或 dict）；LC14 最坏 O(n·m)。**面试口述：** 主动说「滑窗的本质是**单调性**——一旦窗口非法，任何包含它的更大窗口也非法，所以左指针可以只前进不回退」；接着报 LC3/LC76/LC424 是同一模板的三个变体。**坑：** LC3 忘 `max` 导致 `l` 回退（答案偏大，最经典的假解）；LC76 先移除 `s[l]` 再记答案 / 用 `if` 收缩（一次不一定收够）；全量比较 `need` 与 `window`；收缩时忘同步更新 `window` 计数；**Java 里 `Integer` 计数用 `==` 比较**（IntegerCache 只覆盖 −128~127），必须 `.equals()`；空串 / 单字符 / t 比 s 长这些边界没测。
- [ ] **动态规划（股票系列）**：Best Time to Buy and Sell Stock I–IV（LC121、122、123、188）、with Cooldown（LC309）、with Transaction Fee（LC714） — **核心：** 全系列共用一个**状态机**——`hold`（当前持有股票的最大收益）与 `cash`（当前不持有的最大收益）两个变量，初始化 **`hold = -prices[0]`、`cash = 0`**，每天做 `cash = max(cash, hold + p)`、`hold = max(hold, cash - p)`（无限次交易版）——**买入/卖出两个动作的先后顺序决定了要不要快照**。**LC121 单笔**：`hold = max(hold, -p)`（只能买一次，所以是 `-p` 不是 `cash - p`），`cash = max(cash, hold + p)`，O(n)/O(1)。**LC122 无限次**：**必须用 `prev_cash` 快照**——`cash = max(cash, hold + p)` 之后再用**旧的** `cash` 去更新 `hold`，否则同一天「买入→卖出→再买入」变成无限套利；等价贪心是 `sum(max(0, p[i] - p[i-1]))`（每个上涨段都吃满）。**LC123 两笔**：`buy1/sell1/buy2/sell2` 四个状态顺序转移，**注意 `sell1` 更新后才能算 `buy2`**。**LC188 k 笔**：泛化成长度 k 的 `buy[]`/`sell[]` 数组，O(nk)；**关键优化：`k >= n // 2` 时限制失效，直接退化到 LC122 的贪心 O(n)**（不特判会 TLE）。**LC309 冷冻期**：卖出后隔一天才能买 → `hold` 要用**前天**的 `cash[i-2]`（用昨天等于没冷冻）。**LC714 手续费**：**只在卖出时扣一次** `cash = max(cash, hold + p - fee)`（买入也扣会导致来回重复扣费）。**面试口述：** 把整条线讲成「一个状态机 + 五种约束（次数 / 冷冻 / 手续费）」，先写二维 DP `dp[i][0/1]` 再压缩到 O(1)——面试官更认这条推导路径；主动说「所有股票题都不需要额外空间，滚动变量即可」。**坑：** LC122 忘记快照变量；LC309 用昨天的 `cash`；LC714 手续费扣两次 / 扣在买入；LC188 忘记 `k >= n//2` 特判、或 `k == 0` 时返回 0 的分支没写；`hold` 初始化用 `-inf` 在 Java 里溢出；**只算单笔时把 LC121 的 `-p` 写成 `cash - p`**（语义变成可以无限次买）。
- [ ] **间隔 & 设计题（堆）**：Meeting Rooms II（LC253）、Design Hit Counter（LC362）、Design Tic-Tac-Toe（LC348） — **核心：** **LC253** 等价于「同一时刻最多有多少个会议重叠」——① **双指针时间线扫描**：`starts` / `ends` 各自排序，`starts[s] < ends[e]` 就 `used += 1`（开新房间），否则释放 `e += 1`，全程取 `used` 峰值，O(n log n)；**判据用 `>=` 释放——端点相接（`end == start`）不算冲突**；② **最小堆存结束时间**：每个 interval 先 `while heap and heap[0] <= start: heappop`（腾空房间）再 `heappush(end)`，`len(heap)` 峰值即答案，O(n log n)；③ 第三解是**差分 / sweep line**（`+1` 在 start、`-1` 在 end，前缀和求峰值）——**三解都要能讲，面试官必问「哪个更省空间 / 时间线扫描为什么不用堆」**。**LC362 Hit Counter**：`deque` 存 `(timestamp, count)`，`getHits(t)` 先 `while q and q[0][0] <= t - 300: popleft()` 再求和；**窗口是 `[t-299, t]`，所以过期判据是 `<= t - 300`**；进阶是**桶数组版**（300 个桶 + 环形下标）做到 O(1) 摊还、内存 O(300)。**LC348 Tic-Tac-Toe**：**绝不每次扫全盘（O(n²)）**——维护 `rows[n] / cols[n] / diag / anti_diag`，**正负计分法**（player1 记 +1、player2 记 −1，避免混子误判），`abs(v) == n` 即有人获胜，O(1)/步；副对角线判据 **`row + col == n - 1`**。**设计题通用四问（先问再写）：** ① 哪个操作是热点（读多还是写多）？② 并发吗？③ 数据规模与时间窗口？④ 允许近似 / 过期数据吗？——**问完再谈数据结构，最后报复杂度**。**面试口述：** 设计题开场一律「先澄清需求 → 再给 API → 再选数据结构 → 最后给复杂度与扩展点」，不要一上来写代码。**坑：** LC253 把端点相接算成冲突（`>` vs `>=` 写反）；堆里忘了先清理已结束的会议 / 用最大堆存开始时间；LC362 过期判据写成 `< t - 300` 或 `<= t - 299`（差一位）；LC348 每次重扫全盘、用 `sum` 而非 ±1 计分导致玩家 1/2 混子误判；**设计题不问需求就写**、只用数组 `[[0]*n]*n` 初始化（多行共享引用）。

### 重点坑

- [ ] **滑动窗口的左指针单调性（今天最容易翻车的一点）**：左指针**只能前进、不能回退**——LC3 收缩必须写 **`l = max(l, last[c] + 1)`**，直接 `l = last[c] + 1` 会让 `l` 回退、把已经排除的重复字符重新算进窗口，答案偏大（**这是本题最经典的假解，且小样例常常测不出来**）。LC76 的顺序固定「**先记录答案 → 再移除 `s[l]` → 再 `l += 1`**」，用 `while` 不用 `if`（一次不一定收够）；判断合法性用 **`valid` 计数器（字符种类维度）**，不要每轮全量比较两个计数表。
- [ ] **股票 DP 的状态转移顺序 & 快照变量**：无限次交易版里，同一轮的 `cash` 更新后若直接拿它去更新 `hold`，等于「同一天买入又卖出」的**无限套利**——必须 **`prev_cash` 快照**（或者按「先 hold 用昨天的 cash、再 cash 用今天的 hold」的固定顺序写）；LC309 冷冻期要用**前天**的 `cash`；LC714 手续费**只在卖出时扣一次**；LC188 忘 `k >= n // 2` 特判会在 k 很大时 TLE。**写之前先在纸上列好 `dp[i][0/1]` 的二维转移，压缩维度时逐行核对依赖项**。
- [ ] **间隔题的边界与判据（`>=` vs `>`）**：LC253 里 **`end == start` 不算冲突**，判据是 `starts[s] < ends[e]` 才开新房间（写错方向会让答案 +1）；LC56 Merge Intervals 合并时必须 **`max(res[-1][1], end)`**，直接覆盖会把已被包含的大区间改小（`[[1,10],[2,3]]` 必错）；LC435 经典贪心要**按 end 排序**（按 start 排是错解）。凡间隔题先在纸上画**端点相接**和**完全包含**两种边界。
- [ ] **堆的用法（配今天的设计题复习）**：`heapq` 默认**小顶堆**，要最大堆就存负数；堆里放元组**必须保证可比较**（`(freq, num)` 下标放第二位 / `(val, i, node)` 的 `i` 是为了 ListNode 不可比较）；Top-K 用**固定大小 k 的堆 + `heappushpop`**（不要 push 完再 pop，会多一次 re-heapify）；Dijkstra 是 `(dist, node)` 且必须**出堆时 `if d > dist[u]: continue` 懒删除**，边权非负才成立。
- [ ] **设计题：先问需求再动手**：开场必须澄清「哪个操作是热点（读写比）？并发吗？数据规模与时间窗口？允许近似 / 过期吗？」——**不问就写代码是设计轮最典型的失分点**；LC362 的过期判据差一位（窗口 `[t-299, t]` → `<= t - 300`）；LC348 每次重扫全盘（O(n²) → 应该用行列 + 对角 ±1 计数 O(1)）；**HackerRank / CoderPad 没有 `sortedcontainers`**，要手写堆 / 桶数组（面经 4 的提醒）。
- [ ] **今天的 Day 5 主菜（图与最短路）**：`210` 的 Kahn 必须判 **`len(order) == n`** 才算无环（否则静默返回半截答案）；`743` Dijkstra 的堆版要写清 `dist` 初始化为 `inf`、只有松弛成功才入堆；`1631` 的「最小化路径上的最大边」要能**三种解法各写一遍**（Dijkstra 变体把距离换成「路径最大边」/ 二分答案 + BFS / 并查集按边权排序从小加到大）——**面试官明确会追问三者的时间空间对比**。
- [ ] **通用**：递归深度平台差异（LeetCode `sys.getrecursionlimit()` = 550000，但 HackerRank / CoderPad 默认 ~1000 → 预估最深递归 > 1000 就主动写迭代版）；`[[0] * n] * n` 各行共享引用（必须列表推导式）；哈希 key 不能是 list（`''.join(sorted(s))` 或 `tuple(...)`）、`frozenset` 会丢字符计数；Java 比较 `Integer` 用 `.equals()`；**表达顺序固定为：brute-force → 时空复杂度 → 优化方向**，动手前先说清「不变量 + 返回值语义」。

### 建议刷的新题

- [ ] **间隔**：Merge Intervals（LC56，Medium）— 关联已掌握知识点：LC253 的「按 start 排序 + 一次扫线」思维。**核心：** 先按 `start` 升序排序（**排序是本题成立的前提，面试要主动说**），再一次遍历，若 `cur.start <= res[-1][1]` 则合并（**`res[-1][1] = max(res[-1][1], cur.end)`，必须取 max** —— 否则 `[[1,10],[2,3]]` 会把区间改小），否则追加新区间；O(n log n) 时间（排序主导）、O(n) 空间（结果数组，不算额外则 O(1)）。**追问：** ① 输入已按 start 排序怎么办（省掉排序，O(n)）；② 区间是流式到达怎么处理（有界缓冲 + 与尾部合并，或落库后按 start 索引查找重叠）；③ 求「合并后的总覆盖长度」（累加 `end - start`）。**坑：** 忘排序；用 `res[-1][1] = cur.end` 覆盖而非取 max；判据写成 `<` 导致端点相接未合并（本题端点相接**要**合并）；空输入返回 `[]` 而不是 `None`。
- [ ] **间隔**：Insert Interval（LC57，Medium）— 关联 已掌握知识点：LC56 的合并逻辑 + LC253 的端点判据。**核心：** 输入**已排序且互不重叠**，所以不用排序，一次遍历分**三段**处理——① 完全在 `newInterval` 之前的区间直接进结果；② 与 `newInterval` 重叠的区间（判据 `interval.start <= newInterval.end`）并入 `newInterval`（**同时更新 `newInterval.start = min(...)`、`newInterval.end = max(...)`**）；③ 剩下的（完全在之后）全部接上，`newInterval` 插在第一段之后；O(n) 时间、O(n) 空间。**追问：** 若输入可能本身就有重叠（那就退化成 LC56）；若要在有序区间集合里维护**插入 + 查询重叠**，需要什么结构（平衡树 / 跳表，或按 start 建索引 —— 顺着这里就能接上面经的区间设计题）。**坑：** 三段边界混用 `<=` / `<`（重叠判据必须是**闭区间相交**：`interval.start <= newInterval.end` 且 `interval.end >= newInterval.start`）；合并时只改 end 不改 start；插入后忘了保持整体有序；忘了「没有任何重叠」时 `newInterval` 也要落地。
- [ ] **间隔 / 贪心**：Non-overlapping Intervals（LC435，Medium）— 关联已掌握知识点：LC253 的「按结束时间排序」贪心 + 今日区间组。**核心：** 等价于「最多能保留多少个互不重叠的区间」——**按 `end` 升序排序**，扫描时若 `interval.start >= last_end` 则保留并更新 `last_end = interval.end`，否则必须删掉这个（`count += 1`）；答案 = 强制删除的个数；O(n log n) / O(1)。**追问：** 为什么按 end 排序正确（**交换论证：保留结束最早的区间，给后面留出的空间最大**）；如果区间有权重（区间调度加权版）怎么做（按 end 排序后 DP + 二分找前驱，O(n log n)）。**坑：** 按 `start` 排序做贪心（反例：一个超长区间起点很早）；判据写成 `>` 导致端点相接被判为重叠（**端点相接不算重叠**，要用 `>=`）；把「删除数」和「保留数」搞混（答案是 `n - 保留数`）。
- [ ] **滑动窗口 / 字符串**：Longest Repeating Character Replacement（LC424，Medium）— 关联已掌握知识点：LC3 / LC76 的**万能滑窗模板**（外 `while` 扩右、内 `while` 收左）——**拿下这题，滑窗这条线就闭环（LC3 求最长 → LC76 求最短 → LC424 求最长且带替换预算）**。**核心：** 窗口 `[l, r]` 的判据是 `(r - l + 1) - maxFreq > k`（需要替换的字符数超过预算）就收缩左指针；**`maxFreq` 只增不减**——只有更大的窗口才可能刷新答案，收缩时不回退也**不会算错最优解**（这是本题最关键、也最容易被追问的一步）；O(n) 时间、O(26) 空间。**追问：** 为什么 `maxFreq` 不必在收缩时重算？如果 k 极大 / 窗口里全是同一字符会怎样（直接返回 n）？**变种：** 若要求「替换成任意字符」以外的约束（如只能替换成某个固定字符），模板仍然成立、只是判据换成该字符计数。**坑：** 每步 `max(count.values())` 重算 → O(26n) 常数大且必被追问；用 `if` 收缩而不是 `while`（条件可能连续不满足要连收）；把 k 理解成「最多替换 k 个不同字符」；空串 / `k >= len(s)` 边界没测；**注意 `maxFreq` 是「历史最大」**，写的时候别顺手在收缩分支里更新它。
- [ ] **堆 / Top-K**：Top K Frequent Elements（LC347，Medium，**Tier S**）— 关联已掌握知识点：LC253 的最小堆 + LC1 的哈希计数 + Day 2「堆与 Top K」计划（尚未落地）。**核心：** ① `Counter` 计数 + **固定大小 k 的小顶堆**（`heappushpop`，O(n log k)）；② **桶排序**（按频次分桶，频次上界是 n，O(n) 时间）；③ quickselect 平均 O(n) 但最坏 O(n²)。**面试口述（面经 25 的明确考点）：** 讲清三解之后主动答「**变种里为什么只能用 heap**」——变种是**流式 / 连续到达**的数据，桶和 quickselect 都需要全量重算，而堆能**增量维护 Top-K**（并追问：k 很小时堆最优，k ≈ n 时桶/排序更划算）。**坑：** 堆里元组顺序写成 `(num, freq)`（要先比频次，写 `(freq, num)`）；用 `heapq.nlargest(k, ...)` 却说不清复杂度；**Python 里不要用 `sorted(cnt.items(), key=lambda x: -x[1])[:k]` 糊弄（O(n log n)，会被问为什么不用堆）**；HackerRank 没有 `sortedcontainers`，手写 `heapq` 即可。

## 历史复习记录
- 2026-09-22：滑动窗口 & 字符串、动态规划（股票系列）、间隔 & 设计题（堆）
- 2026-09-21：图论 BFS/DFS、链表、树与递归
- 2026-09-20：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-17：图论 BFS/DFS、链表、树与递归
- 2026-09-16：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-15：图论 BFS/DFS、链表、树与递归
- 2026-09-14：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-13：图论 BFS/DFS、链表、树与递归
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 4 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 1 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
> ⚠️ 待同步：`blind-75-overview.md` 里 LC323 Number of Connected Components 仍是 `[ ]`，但笔记 `leetcode/union-find/0323-number-of-connected-components.md` 已存在且复习清单一直按「已掌握」复习 → 勾选后应为 **23 / 76**。

**总共 LeetCode 完成：33 道题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
