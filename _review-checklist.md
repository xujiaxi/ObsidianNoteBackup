# 🎯 面试复习清单

## 📅 今日复习（2026-09-13）

### 需要回顾

- [ ] **图论 BFS/DFS**：Clone Graph（LC133）、Course Schedule（LC207）、Number of Islands（LC200） — **核心：** 图题先想清三件事——① 图的表示（邻接表 or 网格）；② 遍历方式（DFS 求连通分量最简洁、BFS 求最短路径/层序、拓扑排序判依赖环）；③ 防重复访问（visited 集合 / 原地污染 / 三色标记）。**LC200 沉岛（Sinking）**：遍历网格遇到 `'1'` 就 `count++`，随即 DFS 把整座岛标成 `'0'`，省掉额外 visited 数组；base case 是「越界 或 是水」两个条件用 `||` 连起来提前 return；全陆地时递归深度 = M×N，有 StackOverflow 风险 → 大数据换 BFS/迭代。**LC133 Clone Graph**：HashMap<原节点, 克隆节点> 当备忘录，既是「防环死循环」的 visited、又是「新旧映射」；关键是**先 `put` 再递归处理 neighbors**——若调换顺序，A→B→A 的环里 A 还没入表，就会无限递归 StackOverflow。**LC207 Course Schedule**：本质是「有向图判环」；DFS 三色标记（0 未访问 / 1 访问中 / 2 已完成），遇到状态 1 = 有环，遇到状态 2 = 直接剪枝（该子树已确认无环，不用重复走）；也可用 BFS Kahn 拓扑排序——建邻接表 + 入度表，所有入度 0 的入队，出队计数、后继入度 −1、新出现的 0 入度再入队，最后 `processed == numCourses` 则无环。注意 `prerequisites[i] = [a, b]` 是 **b → a**（先修 b 才能修 a）。**面试口述：**「求连通块数量/岛屿/填色」→ DFS；「最短步数/扩散/最短路径」→ BFS；「课程依赖/编译顺序/能否完成」→ 拓扑排序 + 环检测。**坑：** Clone Graph 忘 `put` 或先递归后入表；Course Schedule 邻接表方向反了；三色标记只写 0/1 丢掉状态 2 的剪枝（也能过但慢）；网格 DFS 先判越界再访问 `grid[r][c]`，否则越界索引报错；`grid.length`（行）与 `grid[0].length`（列）别搞混。**（本组为轮换复习，上次 09-12 复习滑动窗口/DP/间隔，今日轮到图论/链表/树，距上次复习本组隔 2 天）**
- [ ] **链表**：Reverse Linked List（LC206）、Linked List Cycle（LC141）、Merge Two Sorted Lists（LC21）、Remove Nth Node From End（LC19） — **核心：** 链表题三大武器：**Dummy 哨兵节点**（统一头节点/空链表边界）、**双指针（快慢/定间距）**、**迭代反转的三指针推移**。**LC206 Reverse**：迭代用 prev / curr / nextTemp 三指针，先存 `next`、再改 `curr.next = prev`、最后整体后移，O(1) 空间是首选；递归版 `head.next.next = head; head.next = None`，代码极简但 O(n) 栈空间、深链有溢出风险。**LC141 Cycle**：Floyd 快慢指针，slow 走 1 步、fast 走 2 步，有环必套圈相遇；循环条件 `while (fast && fast.next)` 是防 `fast.next.next` NPE 的关键。**LC21 Merge Two Sorted**：Dummy + 拉链式比较，谁小接谁，收尾 `curr.next = l1 or l2`（剩余部分直接挂上）；也可递归。**LC19 Remove Nth**：Dummy + 快慢指针**保持固定间距**，fast 先走 **N+1** 步（不是 N 步！），再一起走到底，slow 恰好停在待删节点的**前驱**，`slow.next = slow.next.next` 删除。**面试口述：**「反转/找中点/判环/找交点」→ 指针题；「删除倒数第 k / 找倒数第 k」→ 定间距双指针 + Dummy。**坑：** 反转时必须先存 `next` 再改指向，顺序颠倒会断链；LC19 只走 N 步会把 slow 停在待删节点本身（删错）；不用 Dummy 时删头节点要特判；LC141 循环条件写成 `while (fast.next && fast.next.next)` 会漏判或 NPE；快慢指针找中点时偶数长度取到的是第二个中点，看题目要求取哪个。**（本组为轮换复习，上次 09-12 复习滑动窗口/DP/间隔，今日轮到图论/链表/树，距上次复习本组隔 2 天）**
- [ ] **树与递归**：Maximum Depth（LC104）、Same Tree（LC100）、Invert Binary Tree（LC226）、Level Order Traversal（LC102）、Construct Binary Tree（LC105）、Validate BST（LC98）、LCA of BST（LC235）、LCA of Binary Tree（LC236） — **核心：** 树的题 90% 是「递归定义 + 分治」：先想清楚**每个节点要向上返回什么**、**base case 是什么**（空节点返回什么），其余交给递归。**LC104 MaxDepth**：`1 + max(depth(left), depth(right))`，空节点返回 0；也可 BFS 数层数。**LC100 Same Tree**：同步 DFS 两棵树，fail-fast 把「两空→True」「一空或值不等→False」合并判断，主干只走成功路径。**LC226 Invert**：交换左右子节点后递归（前序/后序都可）。**LC102 Level Order**：BFS 标准模板——每层开始先记 `size = len(q)`，`for _ in range(size)` 消费当层、子节点入队，得到按层分组结果；必须用 `deque`/`LinkedList`（`pop(0)` 是 O(n)）。**LC105 Construct**：前序第一个必是根、中序中根的左边是左子树、右边是右子树；用哈希表把「值→中序索引」映射掉 O(n) 查找，维护全局 `pre_idx` 一直自增，**必须先递归左子树再递归右子树**（前序是 [根, 左, 右]）。**LC98 Validate BST**：两条路——上下界递归（向下传 (min,max) 区间，到达节点立即检查，能 fail-fast 剪枝）或中序遍历（BST 中序严格递增，比较 prev）；注意用 `min_val is not None` 而非 `if min_val`（节点值可能为 0）。**LC235 LCA of BST**：靠 BST 大小关系指路——p、q 都小于根 → 往左；都大于 → 往右；否则（一左一右或其一为根）当前即 LCA；迭代 O(1) 空间。**LC236 LCA of Binary Tree**：普通树不能靠值，用**后序汇总**——左右子树各自返回「找到的节点」，左右都非空 → 当前节点是 LCA，否则把非空那边往上抛；base case `if not root or root == p or root == q: return root`。**面试口述：**「树题先问：能不能分解成左右子树的同类子问题？每个节点该返回什么？」；LCA → 后序汇总；BST → 中序遍历 or 值域上下界。**坑：** 忘写 base case 或返回值错（空树返回 0/None/True 要分清）；LC98 上下界法用 `<=`/`>=` 严格判断，别用 truthy 判空（0 值踩坑）；中序遍历写 `return self.inorder(...)` 而不是只调用（Python 默认返回 None 被当 False）；LC105 先右后左会构造错；Python 二维数组用列表推导式 `[[0]*n for _ in range(n)]`，`[[0]*n]*n` 各行共享同一引用。**（本组为轮换复习，上次 09-12 复习滑动窗口/DP/间隔，今日轮到图论/链表/树，距上次复习本组隔 2 天）**

### 重点坑

- [ ] **图论**：Clone Graph 先 `put` 再递归（否则环里 StackOverflow）；Course Schedule 邻接表方向 `[a, b]` 是 b→a，别搞反；三色标记必须有状态 2 才能剪枝；网格 DFS 先判越界再取 `grid[r][c]`；求连通分量用 DFS、求最短路径用 BFS。
- [ ] **链表**：反转先存 `next` 再改指针；LC19 用 Dummy 且 fast 先走 **N+1** 步（不是 N 步）；LC141 循环条件 `while (fast && fast.next)` 防 NPE；Dummy 节点统一头节点/空链表边界；递归解法注意系统栈深度。
- [ ] **树与递归**：base case 想清楚（空节点返回 0/None/True 视题意）；LCA 后序汇总 `left && right → root`；LC98 用 `is not None` 判空而非 truthy（0 值）；中序遍历必须 return；LC105 先左后右；BFS 层序必须用 deque。
- [ ] **通用**：Java 比较 `Integer` 用 `.equals()` 而非 `==`（IntegerCache −128~127 之外失效）；`PriorityQueue` 默认小顶堆，要最大堆用 `Comparator.reverseOrder()`；Python `[[0]*n]*n` 各行共享引用、要用列表推导式；递归/DP 先验证 base case 与边界（空输入 / 单节点 / 首元素）。

### 建议刷的新题

- [ ] **链表 / 堆**：Merge K Sorted Lists（LC23，Hard）— 关联已掌握知识点：LC21 合并两个有序链表（已完成）+ LC253 最小堆（已完成）。**核心：** ① **最小堆法**——把 k 个链表头压入小顶堆，每次弹出最小节点接到结果尾部，若该节点有 `next` 再压回堆，O(N log k)；② **分治法**——两两合并（复用 LC21 的 mergeTwo），O(N log k)；③ 顺序两两合并是 O(kN)，别用。**面试口述：**「k 个有序序列合并 / k 路归并」→ 最小堆 or 分治归并。**坑：** 堆里比较的是节点 `val`，要自定义比较器；Java `PriorityQueue` 默认小顶堆正好合用；空链表数组、k=1 边界；别用顺序合并导致 O(kN)。
- [ ] **链表 / 双指针**：Reorder List（LC143，Medium）— 关联已掌握知识点：LC206 反转链表 + LC141 快慢指针找中点（已完成）。**核心：** 三步走——① 快慢指针找到中点并**断开**成两半；② 反转后半段（LC206 模板）；③ 交替合并前后两半（拉链式）。整体 O(n) / O(1)。**面试口述：**「把链表 L0→Ln→L1→Ln-1… 重排」→ 找中点 + 反转后半 + 交替合并。**坑：** 找中点后一定要切断 `slow.next = null`（否则两半还连着会成环/死循环）；奇数长度时前半比后半多一个，合并循环条件用 `while second:`；注意反转/合并时的指针别名。
- [ ] **树 / BST**：Kth Smallest Element in a BST（LC230，Medium）— 关联已掌握知识点：LC98 Validate BST 的中序遍历（已完成）。**核心：** BST 中序遍历是**升序**序列，边走中序边计数，数到第 k 个即答案；可中序收集成数组取 `arr[k-1]`（O(n) 空间），或中序递归/迭代过程中提前停止（O(h) 空间 + 早停）。进阶可维护每个子树 size 字段做 O(h) 查找。**面试口述：**「BST 第 k 小 / 第 k 大」→ 中序遍历计数；「频繁查询」→ 给节点加子树 size。**坑：** 中序迭代版要先一路把左孩子压栈；计数从 0 还是 1 起要理清；k 超界按题意返回；别用全排序 O(n log n)。
- [ ] **树**：Subtree of Another Tree（LC572，Easy）— 关联已掌握知识点：LC100 Same Tree（已完成）。**核心：** 双重递归——外层遍历 root 的每个节点，对每个节点调用「以它为根的子树是否与 subRoot 完全相同」（复用 LC100 的 sameTree 逻辑），命中即 True。最坏 O(m·n)。**面试口述：**「判断一棵树是否包含另一棵树」→ 遍历 + 同构判等（sameTree 作子程序）。**坑：** 注意「子树」要求结构与值都完全相同（不是「包含/子结构」即可）；`sameTree` 的 fail-fast 条件别写反；进阶可用序列化 + KMP 优化到 O(m+n)。
- [ ] **图 / 网格**：Pacific Atlantic Water Flow（LC417，Medium）— 关联已掌握知识点：LC200 Number of Islands 的网格 DFS（已完成）。**核心：** 正向「逐格判断能否同时流到两个洋」太慢，改用**逆向思维**——从两个洋的边界各自 DFS/BFS 往内陆爬（只能走向「高度 ≥ 当前」的格子），分别标记两个可达集合，两集合**交集**即为答案。多源 DFS，O(M×N)。**面试口述：**「同时满足两个可达性的格子」→ 反向从目标边界做多源 DFS 取交集。**坑：** 方向判断——从边界往内陆爬要用 `next >= cur`（等于也能流）；两个洋的边界（上/左 = Pacific，下/右 = Atlantic）别标反；两个 visited 用两个集合；DFS 先标记再递归防重复。

## 历史复习记录
- 2026-09-13：图论 BFS/DFS、链表、树与递归
- 2026-09-12：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-11：图论 BFS/DFS、链表、树与递归
- 2026-09-08：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-07：图论 BFS/DFS、链表、树与递归
- 2026-09-06：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-05：图论 BFS/DFS、链表、树与递归
- 2026-09-04：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-03：图论 BFS/DFS、链表、树与递归
- 2026-09-02：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-09-01：图论 BFS/DFS、链表、树与递归
- 2026-08-31：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-30：图论 BFS/DFS、链表、树与递归
- 2026-08-29：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-28：图论 BFS/DFS、链表、树与递归、数组 & 二分查找
- 2026-08-27：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-26：图论 BFS/DFS、链表、树与递归
- 2026-08-25：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-24：图论 BFS/DFS、链表、树与递归
- 2026-08-23：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-22：图论 BFS/DFS、链表、树与递归
- 2026-08-21：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-20：图论 BFS/DFS、链表、树与递归
- 2026-08-19：滑动窗口 & 字符串、动态规划（股票系列）、间隔 / 设计题（堆）
- 2026-08-18：图论 BFS/DFS、链表、树与递归
- 2026-08-17：滑动窗口 & 字符串、动态规划（股票系列）、数组 & 二分查找
- 2026-08-16：图论 BFS/DFS、链表、树与递归
- 2026-08-15：动态规划（股票系列）、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-08-14：链表、树与递归、滑动窗口 & 字符串
- 2026-08-13：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-12：动态规划（股票系列）、链表、树与递归
- 2026-08-11：数组 & 二分查找、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-08-10：链表、树与递归、间隔 / 设计题（堆）
- 2026-08-09：图论 BFS/DFS、数组 & 二分查找、动态规划（股票系列）
- 2026-08-08：链表、树与递归、滑动窗口 & 字符串
- 2026-08-07：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-06：滑动窗口 & 字符串、动态规划（股票系列）、树与递归
- 2026-08-05：间隔 / 设计题（堆）、数组 & 二分查找、图论 BFS/DFS
- 2026-08-04：动态规划（股票系列）、树与递归、链表
- 2026-08-03：图论 BFS/DFS、间隔 / 设计题（堆）、滑动窗口 & 字符串
- 2026-08-02：树与递归、链表、数组 & 二分查找
- 2026-08-01：图论 BFS/DFS、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-30：链表、间隔 / 设计题（堆）、数组 & 二分查找
- 2026-07-29：树与递归、滑动窗口 & 字符串、动态规划（股票系列）
- 2026-07-28：数组 & 二分查找、链表、图论 BFS/DFS
- 2026-07-27：树与递归、滑动窗口 & 字符串、间隔题 / 设计题（堆）
- 2026-07-26：链表、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-25：滑动窗口 & 字符串、数组 & 二分查找、设计题
- 2026-07-24：树与递归、图论 BFS/DFS、动态规划（股票系列）
- 2026-07-23：数组 & 二分查找、链表
- 2026-07-22：树与递归、滑动窗口 & 字符串
- 2026-07-20：图论 BFS/DFS、链表
- 2026-07-19：动态规划、数组 & 二分查找
- 2026-07-18：树与递归、图论 BFS/DFS、滑动窗口 & 字符串
- 2026-07-17：数组 & 二分查找、链表
- 2026-07-16：树与递归、图论 BFS/DFS
- 2026-07-15：链表、滑动窗口 & 字符串
- 2026-07-14：数组 & 二分查找、图论 BFS/DFS
- 2026-07-13：树与递归、动态规划
- 2026-07-12：滑动窗口、链表
- 2026-07-11：图论 BFS/DFS、二分查找、数组基础
- 2026-07-07：滑动窗口、链表、树 DFS/BFS
- 2026-07-05：图论 BFS/DFS、二分查找、数组基础
- 2026-07-04：树与递归、链表、动态规划入门 — 股票系列
- 2026-07-03：数组 & 二分查找、图论 BFS/DFS、字符串 & 滑动窗口

## 📊 LeetCode 刷题进度

| 专题 | 已完成 | 目录 |
|------|--------|------|
| Tree | 8 | `tree/` |
| Dynamic Programming | 5 | `dynamic-programming/` |
| Linked List | 4 | `linked-list/` |
| Graph | 3 | `graph/` |
| Array | 2 | `array/` |
| Binary Search | 2 | `binary-search/` |
| Design | 2 | `design/` |
| Sliding Window | 2 | `sliding-window/` |
| Greedy | 1 | `greedy/` |
| Heap | 1 | `heap/` |
| String | 1 | `string/` |
| Backtracking | 0 | `backtracking/` |
| Bit Manipulation | 0 | `bit-manipulation/` |
| Hash Table | 0 | `hash-table/` |
| Math | 0 | `math/` |
| Prefix Sum | 0 | `prefix-sum/` |
| Sorting | 0 | `sorting/` |
| Stack & Queue | 0 | `stack-queue/` |
| Sweep Line | 0 | `sweep-line/` |
| Trie | 0 | `trie/` |
| Two Pointers | 0 | `two-pointers/` |
| Union Find | 0 | `union-find/` |

**Blind 75 完成：22 / 76**（见 `knowledge/blind-75-overview.md`）
**总共 LeetCode 完成：31 道题。掌握基于这些题型的核心思路，可以应对类似面试问题。**

## 待复习（按优先级）

- [x] **树基础** — LC226 + LC105 + LC235 + LC236 + LC102 + LC104 + LC98
- [x] **滑动窗口** — LC3 无重复字符最长子串 + LC76 最小覆盖子串
- [x] **链表基础** — LC206 + LC141 + LC21 + LC19
- [x] **图 DFS/BFS** — LC133 Clone Graph + LC207 Course Schedule + LC200 Number of Islands
- [x] **二分查找** — LC153 旋转数组最小值 + LC33 搜索旋转排序数组
- [x] **数组基础** — LC1 Two Sum + LC121 Best Time to Buy and Sell Stock
